# PROJECT_LOG.md — יומן הפרויקט

זהו הזיכרון של הפרויקט. כל סוכן חדש מתחיל מקריאתו, וכל משימה שהושלמה מוסיפה רשומה בפורמט:
`## [תאריך] — [שם המשימה]` עם: מה נעשה / למה / קבצים / החלטות / הצעד הבא.
רשומות חדשות נוספות **למעלה**.

---

## 26.08.2026 (ג') — 🔴 אמוג'י שנשלח ללקוחות ולא נתמך בטלפונים ישנים [Claude Opus]

- **הדיווח:** "האייקונים מוצגים כסימן שאלה". התחיל מהודעת וואטסאפ, והתברר כבעיה רוחבית.
- **הסיבה:** ‏**🩷** (לב ורוד, `U+1FA77`) הוא **Unicode 15 — 2022**, ונתמך רק מ-Android 13 / iOS 16.4. במכשירים ישנים הוא מוצג כריבוע או כסימן שאלה. בנוסף **🫓** (מצה, Unicode 13) בלוח החגים.
- **⚠️ ההיקף היה גדול מהצפוי — 19 מופעים ב-11 קבצים, רובם בדרך החוצה ללקוח:** חתימת **כל המיילים היוצאים** (הרשמה, איפוס סיסמה, קוד אימות דו-שלבי, התראת מחיקה שנתית, תפוגת פרו לספק), הדוח השנתי המופק כקובץ, ומסך ההוספה למסך הבית. **הקשה מכולן:** ההנחיה הקבועה ל-AI כללה את התו כדוגמה — כלומר הודעות שהוועד מנסח לשליחה **להורים** קיבלו אותו גם הן.
- **הפתרון:** ‏🩷 → **💗** (`U+1F497`, Unicode 6.0 — 2010): אותה משמעות, אותו גוון ורוד, ונתמך בכל מכשיר בשימוש היום. ‏🫓 → **🍷** לפסח, מאותה סיבה.
- **החלטה — לתקן במקור ולא בהודעה אחת:** קל היה להחליף את התו בהודעת הוואטסאפ ולסיים. הבעיה הייתה בחתימה שיוצאת מהמערכת בכל מייל, ולכן היא תוקנה בכל מקום.
- **בדיקה שנוספה לתהליך:** סריקה של טווחי Unicode 13+ בכל קבצי המקור. הרצה חוזרת אחרי התיקון החזירה **נקי**.
- **קבצים:** 11 — ‏`AiService`, ‏`AuthService`, ‏`VendorService`, ‏`TwoFactorChannel`, ‏`VendorProExpiryService`, ‏`YearEndCleanupService`, ‏`AddToHomeScreen`, ‏`AnnualReportPage`, ‏`holidays.js`, ושני קבצי טסט. `npm test` 336/336; `npm run build` נקי; `dotnet build` 0 שגיאות.

## 26.08.2026 (ב') — 1.10 הפך לסוף קשיח לכולם [Claude Opus]

- **עדכון החלטה, באותו יום:** בעלת המוצר ביקשה ש**המנוי של כולם יסתיים ב-1.10** למי שלא רכש. זה **הופך** את ההחלטה מהבוקר, שבה 1.10 היה **רצפה** ותקופת ניסיון אישית ארוכה יותר גברה עליו.
- **הכלל החדש תלוי במועד ההרשמה ולא באורך הניסיון:** נרשם/ה **לפני** 1.10 → הפרו נגמר **בדיוק ב-1.10** (מאריך למי שכבר פג, ומקצר למי שנרשם/ה בסוף ספטמבר). נרשם/ה **אחרי** 1.10 → חוזרים למודל הרגיל של חודש.
- **למה לא פשוט "כולם עד 1.10":** ניסוח כזה היה משאיר לקוחה שנרשמת בנובמבר **בלי שום תקופת ניסיון** — המבצע היה הופך לחסם. התלייה במועד ההרשמה שומרת על המודל הרגיל אחרי שהמבצע נגמר.
- **החלטה — פרמטרים חובה:** ‏`EffectiveTrialEnd`/`IsTrialActive`/`IsActive` קיבלו `registeredAt` **חובה**, והמהדר איתר את שני אתרי הקריאה (`AccessScope`, ‏`GroupService`). ברירת מחדל הייתה משאירה נקודת אכיפה אחת מחשבת לפי הכלל הישן — כלומר גן שנראה פרו במסך ונחסם בפועל.
- **קבצים:** `backend/Services/ProPolicy.cs`, `backend/Services/AccessScope.cs`, `backend/Services/GroupService.cs`.
- **אימות (5/5 מול שרת רץ):** חשבון שנרשם ב-1.7 והניסיון שלו פג ב-1.8 → **פרו עד 1.10** · חשבון שנרשם ב-25.9 עם ניסיון עד 25.10 → **נחתך ל-1.10** (זה השינוי) · חשבון שנרשם ב-15.10 → **שומר על חודש שלו** עד 15.11 · **מנוי בתשלום אינו מושפע**. `npm test` 336/336; `npm run build` נקי.

## 26.08.2026 — פרו חינם לכולם עד 1.10 + באנר שמראה מה פתוח ועד מתי [Claude Opus]

- **החלטת בעלת המוצר:** (א) הפרו פתוח **לכולם ללא עלות עד 1.10.2026**; (ב) התראה בראש המסך לכל בעל/ת מוסד שהפרו פתוח לו/ה ללא עלות.
- **הפער שהבאנר סוגר:** עד עכשיו הפרו נפתח **בשקט**, וההודעה היחידה הגיעה כשהוא **נגמר**. מי שלא ידע שהוא מקבל משהו לא ידע גם מה הוא עומד לאבד — והנעילה נחוותה כתקלה ולא כסוף מבצע. הבאנר מציג מה פתוח, **עד מתי**, וכמה ימים נותרו.
- **החלטה — המבצע כרצפה ולא כדריסה:** ‏`ProPolicy.EffectiveTrialEnd` מחזיר את **המאוחר** מבין הניסיון האישי למבצע. מי שנרשמת ב-20.9 שומרת על חודש מלא (עד 20.10) ולא מקוצרת ל-1.10. דריסה פשוטה הייתה **מקצרת** תקופת ניסיון של נרשמות חדשות — בדיוק ההפך מהכוונה.
- **החלטה — מקור אחד לתאריך:** אותו שדה שקובע את הנעילה בפועל הוא שמוצג בבאנר (`TrialEndsAt` מחושב מ-`EffectiveTrialEnd`). אחרת הבאנר היה יכול להבטיח תאריך אחד והמערכת לנעול באחר.
- **ספירת הימים לפי תאריך מקומי** ולא לפי שעות: משתמשת שנכנסת בערב לא אמורה לראות יום פחות ממי שנכנסה באותו יום בבוקר.
- **קבצים:** חדשים — `src/components/TrialActiveBanner.js`(+טסט); שונו — `backend/Services/ProPolicy.cs`, `backend/Services/GroupService.cs`, `src/pages/HomePage.js`, `src/styles/trial-notice.css`.
- **אימות (8/8 מול שרת רץ):** חשבון שהניסיון האישי שלו **פג ב-1.7** מדווח **פרו פעיל** בזכות המבצע, מסומן כניסיון ולא כרכישה, והתאריך המוצג הוא **1.10.2026** · **יצירת סקר עוברת (201)** — כלומר המנעול באמת פתוח ולא רק הדגל · חשבון שהניסיון שלו נמשך עד 20.11 **לא קוצר** · מנוי בתשלום נשאר פרו אך **אינו** מסומן כניסיון חינם. `npm test` 336/336; `npm run build` נקי.
- **לקח:** שני טסטים נכשלו כי כתבתי אותם ב-UTC בעוד הפונקציה משווה **ימים מקומיים** — התאריך מוצג למשתמשת ב-`toLocaleDateString`, ולכן הספירה חייבת להסכים איתו. הפונקציה הייתה נכונה; הבדיקה לא.

## 23.08.2026 — 🔴 דחיית המחיקה השנתית של 30.08.2026 + סקרים נכנסו למחיקה [Claude Opus]

- **⏰ דחוף מבחינת עיתוי:** בעלת המוצר ביקשה ש**ב-30.8 הקרוב לא יימחקו נתונים לאף אחד** — הוועדים נכנסו לממשק רק בשבועות האחרונים. התאריך היה **בעוד 7 ימים**, וחלון התראת המייל (14 יום) כבר נפתח ב-16.8 — כלומר חלק מהוועדים **כנראה כבר קיבלו מייל** שנתוני השנה שלהם יימחקו ב-30/08/2026.
- **מה נעשה:** Migration ‏`SkipFirstYearCleanup` דוחה כל גן שמועד הניקוי שלו נופל ב-2026 אל **30.08.2027**, ומאפס את `CleanupWarnedAt`. האיפוס אינו קוסמטי: בלעדיו גן שכבר קיבל התראה השנה **לא היה מקבל אותה שוב לעולם**, כי הדגל נבדק פעם אחת למחזור.
- **החלטה — מיקוד בשנת 2026 בלבד** ולא "כל הגנים": מי שכבר מוגדר ל-2027 ומעלה לא נדחף עוד שנה קדימה בטעות. זו דחייה **חד-פעמית**; המדיניות (30.8 בכל שנה) לא השתנתה.
- **סקרים נוספו למחיקה השנתית** (בקשת בעלת המוצר): הם נתוני שנה כמו השאר. ההצבעות והאפשרויות נמחקות **לפני** הסקר עצמו — סדר הפוך היה נכשל על מפתח זר. עודכנו גם תיעוד השירות ו**גוף מייל ההתראה**, שאחרת היה ממשיך לפרט רשימה חלקית.
- **ניסוח ההודעה תוקן:** במקום "שום דבר לא נמחק" → **"המעבר למסלול החינמי לא מוחק כלום"**. הניסוח הקודם היה הבטחה מוחלטת שהמערכת מפרה ב-30.8 — כשהמחיקה השנתית חלה על כולם, גם על מנויים בתשלום.
- **קבצים:** `backend/Migrations/20260822213439_SkipFirstYearCleanup.cs`, `backend/Services/YearEndCleanupService.cs`, `src/components/TrialEndedNotice.js`(+טסט).
- **אימות (11/11):** גן עם מועד ב-2026 **נדחה ל-2027 ודגל ההתראה אופס** · גן שכבר ב-2027 **לא נדחף ל-2028** · גן בלי תאריך **לא נגע** · סקר, הצבעותיו ואפשרויותיו **נמחקים יחד בלי יתומים** · אנשי צוות, קטגוריות גבייה והגן עצמו **נשמרים**. ‏`dotnet ef database update` על מסד נקי עבר עד הסוף; `npm test` 327/327; `npm run build` נקי.
- **⚠️ נותר להחלטת בעלת המוצר:** אם כבר יצאו מיילי התראה על מחיקה ב-30/08/2026 — כדאי לשלוח הודעת תיקון לוועדים שקיבלו אותם.
- **לקח בבדיקה:** שלוש ריצות כשלו על עמודות שלא קיימות (`PollOptions.Votes`, ‏`PollVotes.OptionId`, ‏`StaffMembers.FullName`) — ניחשתי סכימה במקום לקרוא אותה. הריצה הרביעית התחילה בקריאת `PRAGMA table_info`. בנוסף, הרצת השרת נכשלה ב-`hostpolicy.dll`; לבדיקת מיגרציה עדיף `dotnet ef database update` ולא הרמת שרת שלם.

## 22.08.2026 — מודל תמחור חדש: חודש פרו מלא, ואז מסלול חינמי [Claude Opus]

- **החלטת בעלת המוצר:** חודש ניסיון שבו **כל פיצ'רי הפרו פתוחים**. בסופו הודעה שמציעה לחדש ב-**₪149 לשנה** או **להמשיך במסלול החינמי** — ו**כל מה שנוצר בפרו נשמר**, פשוט לא ניתן להמשיך להשתמש בפיצ'רים.
- **🔴 השינוי המהותי:** עד היום סיום התוקף **חסם את החשבון** (401 בכניסה + הפניה למסך "תוקף המנוי הסתיים"). זה סתר את המודל החדש מהיסוד — חסימה מנתקת את המשתמשת מהנתונים שלה עצמה. כעת סיום הניסיון **מוריד למסלול החינמי** ואינו חוסם דבר.
- **⚠️ הדלת שהייתה מפילה את השינוי:** תוקף ה-JWT נגזר מ-`SubscriptionValidUntil`. גם אחרי ביטול החסימה בכניסה, ה-token היה פג יחד עם הניסיון — כלומר החסימה הייתה חוזרת בדלת האחורית, רק בשגיאה אחרת. התוקף נותק ממנוי (`SubscriptionPolicy.TokenExpiry`).
- **פרו בתקופת הניסיון:** ‏`ProPolicy` מפריד כעת בין `IsPurchased` (רכישה/פתיחה ידנית) לבין `IsTrialActive` (חודש ראשון), ו-`IsActive` הוא האיחוד. תוקף הניסיון שייך ל**בעל/ת הגן** ולא למי שמבצע/ת את הפעולה — אחרת חבר/ת ועד שהוזמן/ה היה/הייתה מכריע/ה לפי הניסיון האישי שלו/ה במקום לפי מצב הגן.
- **החלטה — פרמטר חובה ולא ברירת מחדל:** ‏`ToResponse` ב-`GroupService` קיבל פרמטר `ownerTrialUntil` **חובה**, כדי שהמהדר יחייב עדכון של כל 10 אתרי הקריאה. ברירת מחדל הייתה משאירה מסכים שמדווחים "חינם" בעוד אחרים מדווחים "פרו" — בדיוק הבלבול ש-ProPolicy נועד למנוע.
- **קבצים:** חדשים — `src/components/TrialEndedNotice.js`(+טסט), `src/styles/trial-notice.css`; שונו — `backend/Services/ProPolicy.cs`, `backend/Services/AccessScope.cs`, `backend/Services/GroupService.cs`, `backend/DTOs/GroupResponseDto.cs`, `backend/Auth/SubscriptionPolicy.cs`, `backend/Services/JwtTokenService.cs`, `backend/Services/AuthService.cs`, `src/services/authService.js`(+טסט עודכן), `src/services/institutionsService.js`, `src/pages/HomePage.js`.
- **אימות (17/17 מול שרת רץ) — הבדיקה המרכזית היא שהנתונים שורדים את המעבר:** בתוך הניסיון הגן מדווח `isPro:true, isTrial:true` ו**יצירת סקר עוברת (201)**; אחרי סיום הניסיון — **הכניסה עדיין עובדת (200, בלי נעילה)**, יצירת סקר **נחסמת (402)**, הגן מדווח חינם — ו**הסקר שנוצר בתקופת הניסיון עדיין נקרא דרך ה-API ולא נמחק מהמסד**. שאר המערכת ממשיכה לעבוד. אחרי רכישה — פרו חוזר ו-201 שוב.
- **טסט קיים עודכן במכוון:** `isSubscriptionExpired` ציפה ל"נעילה" בתאריך שעבר — זו בדיוק ההתנהגות שהוסרה. הטסט מתעד עכשיו את הכלל החדש. `npm test` 325/325; `npm run build` נקי.

## 22.08.2026 — 🔴 חשבון המנהלת נחסם בחומת המנוי + מסך חידוש עם שם המוסד [Claude Opus]

- **הדיווח:** בכניסה מוצג "צריך לחדש מנוי". שתי טענות של בעלת המוצר: (א) חשבון המנהלת לא אמור להיחסם לעולם; (ב) מי שכן צריך לחדש — שיקבל אפשרות לחדש **למוסד בשם** או להירשם מחדש.
- **🔴 החומרה האמיתית:** חשבון המנהלת הוא גם החשבון היחיד שממנו אפשר **לפתוח מנויים** (מסך המנויים). חסימתו בגלל תוקף מנוי נועלת את בעלת המערכת מחוץ למערכת שלה **בלי שום דרך חזרה דרך הממשק** — התיקון היחיד היה עריכה ידנית במסד.
- **הכלל רוכז במקום אחד** (`SubscriptionPolicy.IsExempt/IsActiveFor/EffectiveValidUntil`) ולא נבדק בכל אתר בנפרד, כי הוא נדרש ב**שלוש** דלתות: כניסה רגילה, כניסת Google, ו**תוקף ה-JWT**.
- **⚠️ הדלת השלישית היא זו שהייתה מפילה תיקון חלקי:** תוקף ה-token נגזר מ-`SubscriptionValidUntil`. בלי חריג שם, המנהלת הייתה מקבלת token **שפג כבר ברגע ההנפקה** — כלומר "מותר לה להיכנס" ומיד 401 בכל בקשה. אומת בקריאת ה-exp מתוך ה-JWT עצמו ולא מהתשובה של השרת.
- **מסך החידוש נבנה מחדש:** מציג את **שם המוסד** (ובריבוי מוסדות — שכולם יחזרו לפעול, בשמות), אומר במפורש ש**שום דבר לא נמחק**, ומוסיף מסלול **הרשמה מחדש** למי שאינה רוצה לשלם עכשיו — עם אזהרה ברורה שזה חשבון נפרד שהנתונים אינם עוברים אליו. קודם היו רק "חידוש" ו"התנתקות", כלומר מי שלא שילמה נשארה בלי שום מסלול.
- **קבצים:** `backend/Auth/SubscriptionPolicy.cs`, `backend/Services/JwtTokenService.cs`, `backend/Services/AuthService.cs`, `src/services/authService.js`, `src/pages/SubscriptionExpiredPage.js`(+טסט חדש).
- **אימות (8/8 מול שרת רץ):** לשני החשבונות נדחף תוקף שפג (2020) — ועד רגיל **נחסם ב-401** עם ההודעה הנכונה, ומנהלת **נכנסה ב-200**; ה-token שלה **אינו פג** ותקף לשנה קדימה; והיא הגיעה בפועל ל-`/api/admin/usage` ול-`/api/admin/subscriptions`. `npm test` 318/318; `npm run build` נקי.
- **❓ נותר להחלטת בעלת המוצר (הסיבה לדיווח המקורי):** ההרשמה מעניקה **חודש ניסיון** (`TrialUntil`), בעוד כלל ה-30.8 חל על מנוי **בתשלום** (`ValidUntil`). כלומר החסימה נכונה לקוד אך מפתיעה. שינוי אורך הניסיון הוא החלטה עסקית ולא באג, ולכן לא שונה מיוזמתי.

## 21.08.2026 — פתיחת מסלול פרו לגן ידנית ממסך המנהלת [Claude Opus]

- **הבקשה (בעלת המוצר):** בנתוני שימוש, ליד שמות המוסדות שנרשמו — אפשרות לחבר להם מסלול פרו.
- **הפער שנסגר:** פרו לוועד נפתח עד היום **רק** אוטומטית אחרי תשלום (webhook מ-GROW). לא הייתה שום דרך לתת מסלול ידנית — לגן פיילוט, ללקוחה שמשלמת בהעברה, או כדי לבדוק פיצ'ר. הפעולה עוברת דרך **אותו** `IProActivationService` שמפעיל אחרי תשלום, כדי שלא ייווצרו שני מסלולי הפעלה שמתנהגים אחרת.
- **החלטות:** (1) **per-gan ולא לפי מייל** — לחשבון אחד יכולים להיות כמה גנים, ופרו הוא לגן. שיטת ה-webhook לפי מייל פותחת פרו לכל הגנים של החשבון; ידנית זה היה מוגזם. (2) **סגירה נשאלת לאישור, פתיחה לא** — פתיחה בטעות היא אי-נוחות שמתקנים בלחיצה, סגירה בטעות מורידה פיצ'רים ללקוחה משלמת. (3) **הכיבוי מאפס גם את תאריך התוקף** — תאריך שנשאר תלוי בלי מסלול פעיל נקרא כאילו המנוי עדיין בתוקף. (4) **ספקים לא קיבלו את הכפתור** — להם כבר יש `PUT /api/vendors/{id}/pro` במסך הספקים, ושתי דרכים לאותה פעולה מזמינות חוסר עקביות.
- **תוקן אגב עבודה:** ‏`ProActivationService` רשם **כתובות מייל וטלפון ללוג** בשלוש נקודות — אותו ליקוי שנסגר ב-17.08 במקומות אחרים (לוגים נשמרים 7 ימים אצל ספק האירוח ואינם ניתנים למחיקה לפי בקשה). הוחלף בדומיין בלבד.
- **קבצים:** `backend/Controllers/AdminController.cs`, `backend/DTOs/SubscriptionsDto.cs`, `backend/Services/ProActivationService.cs`, `src/services/subscriptionsService.js`, `src/pages/admin/SubscriptionsCard.js`(+טסטים), `src/styles/subscriptions.css`.
- **אימות (16/16 מול שרת רץ) — הבדיקה המרכזית היא שהמנעול באמת נפתח ולא רק דגל התחלף:** יצירת סקר (פיצ'ר פרו) → **402 לפני** · **201 אחרי הפתיחה** · **402 שוב אחרי הסגירה**. בנוסף: ועד רגיל שמנסה לפתוח לעצמו → **403** · אנונימי → **401** · גן לא קיים → **404** · התוקף נקבע לשנה, ו-`months: 1` נתן 31 יום · הכיבוי איפס גם את התאריך · הרשומה מופיעה כ-active במסך המנויים. `npm test` 313/313; `npm run build` נקי.
- **⚠️ הערה על הרצה:** ריצה אחת של מערך הטסטים החזירה "1 suite failed" בלי אף טסט אדום — כלומר חבילה שלא הצליחה לעלות תחת עומס, ולא כשל אמיתי. ריצה חוזרת החזירה 76/76 ו-313 טסטים.

## 20.08.2026 — 🔴 באג: "מילוי »" של הוואטסאפ לא הוביל לשום מקום [Claude Opus]

- **הדיווח (בעלת המוצר):** בצ'ק-ליסט של הספק, הלחיצה על "מילוי »" ליד "מספר וואטסאפ ליצירת קשר" לא הגיעה לשדה.
- **הסיבה:** הפריט הוגדר עם `view: "products"`, אבל שדה הוואטסאפ שייך ל**פרטי העסק** — ומסך המוצרים מרנדר את אותו `VendorForm` עם `hideBusinessDetails`. כלומר הניווט **כן** עבד, והספק הועבר למסך שבו השדה כלל אינו קיים. מבחוץ זה נראה ככפתור מקולקל.
- **התיקון:** הפריט מפנה ל-`settings` — המסך היחיד שמרנדר את פרטי העסק (`goTo("settings")` כבר נתמך; תפריט הצד משתמש בו).
- **נבדקו גם השאר** ולא רק הפריט שדווח: מוצר/תמונה → `products` ✔ · אמצעי תשלום → `payments` ✔ · הגדרת כניסה → `home` ✔. רק הוואטסאפ היה שגוי.
- **הגנה מפני חזרה:** נוסף טסט שנועל את יעד הניווט של **כל** פריט למסך שבו השדה שלו באמת מוצג. באג מהסוג הזה שקט — הכפתור עובד, רק מוביל למקום הלא נכון — ולכן צריך בדיקה שקושרת יעד לשדה.
- **קבצים:** `src/services/vendorProgress.js`, `src/services/vendorProgress.test.js`. `npm test` 309/309.

## 20.08.2026 — דוח הספק: טווח תאריכים, מיקום ברשימה, ומועדי עדכון [Claude Opus]

- **הבקשה (בעלת המוצר):** בדוח הספק — טווח תאריכים לבחירה כדי להשוות שנה לשנה, המיקום ברשימת המתנות באזור המשלוח שלו, ומתי היו העריכה האחרונה וההוספה האחרונה של מוצר.
- **🔴 המכשול שקבע את התכנון:** `Vendor.Views` הוא **מונה יחיד שרק עולה**. אפשר לדעת ממנו "כמה בסך הכל" ואי אפשר לדעת "כמה בחודש שעבר" — ולכן השוואה שנה-מול-שנה של צפיות **בלתי אפשרית מהנתונים הקיימים, ולא ניתן לשחזר אותה למפרע**. נוספה טבלה `VendorViewDays` (שורה לכל ספק×יום, לא לכל צפייה), והצבירה מתחילה מעכשיו. הדוח מציג במפורש **ממתי** הספירה קיימת — אחרת תקופה ישנה מציגה 0 צפיות, ומי שקורא מסיק "לא נכנס אליי איש" במקום "לא נמדד".
- **🔴 מלכודת שנתפסה בכתיבה:** שמירת כרטיס ספק **מוחקת ובונה מחדש את כל המוצרים** (אין מזהה מוצר בבקשה). לכן חותמת `CreatedAt` תמימה הייתה מתאפסת בכל שמירה, ו"המוצר האחרון שהוספת" היה מציג תמיד את מועד העריכה — נתון חסר ערך. `MapProducts` מקבל כעת את הרשימה הקודמת ומוריש תאריכים לפי התאמה (תמונה → שם+מחיר → שם). מגבלה מתועדת: שינוי גם השם וגם התמונה ייחשב מוצר חדש.
- **החלטות:** (1) **המיקום מחושב בדיוק בסדר שהוועד רואה** (מקודמים ואז א׳-ב׳) — כל סדר אחר היה נותן מספר שלא תואם למה שהספק רואה כשהוא פותח את הרשימה בעצמו, כלומר מטעה. (2) **טווחים מוכנים בלחיצה** לפי **שנת פעילות ספטמבר–אוגוסט**, כי זו השנה שהוועדים עובדים לפיה, ואילוץ מילוי ידני בכל השוואה היה הופך את התכונה למשהו שלא משתמשים בו. (3) **שירות נפרד** (`ISupplierReportService`) ולא הרחבת `VendorService` — זה צירוף קריאה בלבד ממקורות שונים.
- **קבצים:** חדשים — `backend/Models/VendorViewDay.cs`, `backend/DTOs/SupplierReportDto.cs`, `backend/Services/SupplierReportService.cs`, Migration `AddSupplierReportDetails`, `src/services/supplierReportService.js`, `src/components/SupplierReportRange.js`(+טסט), `src/styles/supplier-report.css`; שונו — `VendorProduct` (+`CreatedAt`), `VendorService`, `PublicVendorsController`, `AppDbContext`, `Program.cs`, `src/components/SupplierReports.js`.
- **אימות (24/24 מול שרת רץ):** שלושה ספקים באותה עיר → הדירוג יצא **3 מתוך 3** לפי הסדר האמיתי (מקודם, ואז א׳-ב׳) · שתי צפיות → **שורה יומית אחת עם ספירה 2**, לא שתי שורות · טווח 2020 → 0 צפיות **מסומן כלא-מכוסה** · **שמירה שלא נגעה במוצר לא איפסה את תאריכו** · מוצר חדש קיבל תאריך נוכחי בעוד הישן שמר על שלו · פנייה נספרת בטווח ברירת המחדל ולא בטווח ישן · טוקן לא תקין → **404**. `npm test` 307/307; `npm run build` נקי.

## 19.08.2026 — חלון 5 ימים בדוח השימוש [Claude Opus]

- **מה נעשה:** לצד "נרשמו ב-30 הימים האחרונים" נוסף **"נרשמו ב-5 הימים האחרונים"** (בקשת בעלת המוצר), בשני המשפכים — ועדים וספקים. הנימוק: 30 יום מראים מגמה, אבל אחרי פנייה לספקים או פרסום צריך לדעת מה קרה **עכשיו**, וקפיצה של יומיים נבלעת בממוצע חודשי.
- **מה היה קל לפספס:** קיים "איפוס תצוגה" ששומר צילום של המספרים ומציג רק מה שנוסף מעליו. בלי להוסיף את החלון החדש ל-`usageBaseline`, המספר של 5 הימים היה ממשיך להציג את המצב המלא אחרי איפוס — כלומר האיפוס היה נראה שבור דווקא במספר החדש. נוסף שם, ונוסף טסט ייעודי.
- **קבצים:** `backend/DTOs/UsageStatsDto.cs`, `backend/Services/UsageStatsService.cs`, `src/pages/admin/UsageFunnel.js`, `src/services/usageBaseline.js`, `src/pages/UsageStatsPage.test.js`.
- **אימות (11/11 מול שרת רץ):** שלוש הרשמות "עכשיו" → שתיהן 3; הזזת הרשמה ל-10 ימים אחורה → **יצאה מחלון 5 ונשארה ב-30** (2 מול 3); הזזה ל-60 יום → מחוץ לשניהם (1 מול 2); הסך הכולל לא הושפע; ועד רגיל מקבל **403**. `npm test` על המסך: 4/4.
- **🔴 לקח מבצעי חשוב — בדיקות רצו על מסד הפיתוח האמיתי:** ההרצות הועברו עד היום עם `ConnectionStrings__DefaultConnection`, אבל המפתח בקוד הוא **`ConnectionStrings__Default`** (‏`Program.cs`). המשתנה פשוט לא נקלט, והשרת נפל ל-`Data Source=vaadygo.db` — כלומר **בדיקות "מבודדות" כתבו למסד הפיתוח**. בנוסף `dotnet run` טוען את `launchSettings.json` ודורס `ASPNETCORE_URLS`, ולכן גם הפורט לא היה מה שחשבתי. תוקן: שם המפתח הנכון + `--no-launch-profile`, ואומת שקובץ המסד המבודד באמת נוצר לפני שסומכים על התוצאה. ששת חשבונות הבדיקה שנוצרו בטעות (`twofa*@example.com`, ‏`boss/lead1/lead2@example.com`) **נמחקו** יחד עם שורות ה-2FA התלויות בהם; שורות הבדיקה הוותיקות (`*.test.local`) לא נגעתי בהן.

## 17.08.2026 — הנחיית פרטיות גלויה בעוזרת ה-AI [Claude Opus]

- **מה הניע את זה:** בירור ה-DPA של גוגל העלה ממצא מהותי. המערכת פונה ל-**Gemini API** (‏`generativelanguage.googleapis.com`), ושם התנאים **שונים מהותית בין המסלול בתשלום לחינמי**: במסלול החינמי גוגל משתמשת בתוכן הנשלח *"to provide, improve, and develop Google products and services"*, ו**בוחנים אנושיים רשאים לקרוא ולתייג** את הקלט והפלט. ה-DPA של גוגל חל על המסלול בתשלום בלבד.
- **הפער שנמצא בקוד:** בקובץ העוזרת הייתה הערת פרטיות — *"נשלח רק הטקסט של השיחה — לא שמות/טלפונים"* — אבל היא **הערה בקוד בלבד, שהמשתמשת מעולם לא ראתה**. וההערה גם אופטימית מדי: המערכת אכן אינה שולחת שמות מעצמה, אך אינה יכולה למנוע ממי שמקלידה לכתוב "תנסחי הודעה לאמא של נועם". במסלול החינמי טקסט כזה עשוי להיקרא בידי אדם.
- **מה נעשה:** נוספה הנחיה **גלויה** מעל כפתור השליחה, עם חלופה מעשית ("אפשר לכתוב 'הודעה להורים' במקום שם"). מוקמה שם ולא בתחתית העמוד כי היא רלוונטית ברגע אחד — לפני שהטקסט יוצא לשירות חיצוני.
- **קבצים:** `src/pages/AiAssistantPage.js`, `src/styles/ai.css`.
- **⚠️ נותר לבעלת המוצר:** לבדוק אם מפתח ה-Gemini נמצא במסלול בתשלום. אם לא — המעבר הוא הפעלת חיוב בפרויקט, ובהיקף השימוש הנוכחי העלות זניחה מול ההבדל בתנאים. הועבר למסמך עורכת הדין.

## 17.08.2026 — הלוגים מנוקים ממידע אישי ומאסימוני גישה [Claude Opus]

- **מה הניע את זה:** תשובת Railway לפנייה של בעלת המוצר. שני משפטים בה שינו את התמונה: הלוגים נשמרים **7 ימים**, ו**אין מחיקה לפי בקשה**. כלומר כל מה שנכתב ללוג הוא נתון ש**איננו יכולים למחוק** — גם לא כשמשתמשת מבקשת שיימחקו נתוניה. זה הופך את השאלה "מה בעצם נכנס ללוג?" מסקרנות לבדיקה מתחייבת.
- **מה נמצא (שני ליקויים אמיתיים):**
  1. **כתובות מייל של משתמשים** נכתבו ללוג בכל שליחה (`ResendEmailSender`), וגם בעליית השרת (`Program.cs`). כתובת מייל היא מידע אישי.
  2. **🔴 אסימוני גישה חד-פעמיים בכתובת הבקשה.** כל שגיאה לא מטופלת רשמה את הנתיב המלא — וחלק מהנתיבים **מכילים את המפתח עצמו**: קישור עריכה של ספק, הזמנה לגן, וסקר. מי שקורא כתובת כזאת מהלוג יכול להשתמש בה. זהו **בדיוק אותו ליקוי** שנמצא ונסגר בכלי ניטור השגיאות בצד הלקוח — הפעם בצד השרת.
- **מה תוקן:** המייל הוחלף בדומיין בלבד (`gmail.com`) — מספיק לאבחון "כל הגוגל נכשל", ואינו מזהה אדם; גוף שגיאה מהספק **מצונזר** מכתובות מייל, כי בשגיאות ולידציה הוא מצטט את הכתובת ומחזיר אותה ללוג מהדלת האחורית; והנתיב מצונזר מאסימונים.
- **החלטה — זיהוי לפי צורה ולא לפי מיקום או אורך:** כל האסימונים במערכת הם `Guid.NewGuid().ToString("N")` = 32 תווים הקסדצימליים בדיוק. הכלל הראשון שכתבתי ("מקטע ארוך אחרי `vendors`") היה מצנזר גם `extract-products` והופך את הלוג לחסר ערך לאבחון. הכלל הסופי מדויק.
- **אימות:** נבדק מול צורות הנתיבים האמיתיות — קישור ספק · קישור ספק + `/leads` · סקר + `/vote` · הזמנה + `/accept` → **כולם מצונזרים**; ‏`extract-products` · `forgot-password` · `catalog/17` · `students/42` → **כולם נשמרו**. בנוסף נסרקו **כל** קריאות הלוג בשרת: הערכים היחידים שנכתבים הם מזהים, מונים, סטטוסים, דומיין וגוף מצונזר.
- **קבצים:** `backend/Middleware/ErrorHandlingMiddleware.cs`, `backend/Services/ResendEmailSender.cs`, `backend/Program.cs`.
- **⚠️ פתוח לעורכת הדין (לא ניתן לסגירה טכנית):** ה-DPA של Railway נמסר "as-is" ללא תיקונים, ו-**Exhibit A שלו מצהיר שאינו מכסה עיבוד מידע רגיש/מיוחד**. המערכת שומרת **אלרגיות = מידע רפואי**. זו נקודה משפטית ולא הנדסית, והיא הועברה למסמך.

## 17.08.2026 — אימות דו-שלבי: הפער שההצפנה לא סוגרת [Claude Opus]

- **מה נעשה:** נוסף אימות דו-שלבי לחשבון המנהלת — קוד חד-פעמי אחרי הסיסמה. **למה זה לא כפילות מול ההצפנה:** ההצפנה מגינה על *הקובץ* מפני מי שמשיג עותק שלו; מי שיודע את הסיסמה נכנס מהדלת הקדמית ורואה הכול מפוענח. בחשבון שרואה את כל הגנים, כל הילדים וכל האלרגיות — הסיסמה הייתה נקודת הכשל היחידה שנותרה.
- **בקשת בעלת המוצר שעיצבה את הארכיטקטורה:** "אפשר לשלוח גם למייל וגם לטלפון... במידה ואין גישה למייל באותו רגע". מרגע שיש יותר מערוץ אחד, הבחירה ביניהם היא לוגיקה בפני עצמה — ולכן `ITwoFactorChannel` (מייל/SMS) בדפוס `IPaymentGateway`, ולכן `resend` מקבל ערוץ ומאפשר מעבר **תוך כדי** ההתחברות בלי להתחיל מחדש.
- **⚠️ פער שנאמר לבעלת המוצר מראש ולא הוסתר:** SMS עולה כסף ודורש חשבון אצל ספק, ואין כזה היום. המימוש (Twilio) נכתב ונרשם ב-DI מאחורי `Sms__Provider=twilio`, אבל **לא נבדק מול השירות עצמו** — אין חשבון. עד אז `IsConfigured=false` והערוץ **אינו מוצע בממשק**: הבטחה לערוץ שלא יגיע גרועה מלא להציע אותו. שליחה אמיתית ראשונה מחייבת אימות ידני.
- **החלטות:** (1) **קודי גיבוי הם חלק מההפעלה ולא תוספת** — התכונה מוסיפה תלות בגורם חיצוני שיכול להישבר; בלי מסלול חילוץ, תקלה בתיבת המייל נועלת את בעלת המערכת מחוץ למערכת שלה לצמיתות. לכן `EnableAsync` מנפיק 10 קודים באותה פעולה, ואין רגע שבו האימות דלוק בלעדיהם. (2) **הקוד וקוד הגיבוי באותו שדה** — ברגע לחוץ אין מקום לחפש מסך אחר. (3) **"זכור מכשיר" מסומן כברירת מחדל** — תכונת אבטחה שמעצבנת בכל כניסה נכבית, ואז ההגנה שווה אפס. (4) **גם כניסת Google עוברת אתגר**, אחרת השתלטות על חשבון ה-Google הייתה דלת עוקפת. (5) **202 ולא 401/200** לכניסה שממתינה לקוד — הלקוח מפרש 401 כ"סיסמה שגויה". (6) **כיבוי/הנפקה מחדש דורשים סיסמה** גם ממשתמשת מחוברת. (7) **SHA-256 ולא PBKDF2** לאסימוני מכונה (`SecureToken`): PBKDF2 מגריל מלח בכל גיבוב ולכן אי אפשר לחפש לפיו, ואין מה להאט בניחוש אסימון של 256 ביט.
- **כלי בדיקה שנוסף מהכרח:** קוד שנשלח במייל נשמר מגובב, ולכן **אי אפשר לאמת את הזרימה בלי לקרוא את המייל**. נוסף `FileEmailSender` (כותב מיילים לקבצים) — **מותנה ב-`IsDevelopment()` ולכן אינו קיים בייצור גם בטעות של משתנה סביבה**. בלעדיו הבדיקה היחידה האפשרית הייתה "השרת לא קרס".
- **קבצים:** חדשים — `backend/Auth/SecureToken.cs`, `backend/Services/{SmsSender,TwoFactorChannel,ITwoFactorService,TwoFactorService,FileEmailSender}.cs`, `backend/Models/{TwoFactorChallenge,TwoFactorBackupCode,TrustedDevice}.cs`, `backend/DTOs/TwoFactorDtos.cs`, `backend/Controllers/TwoFactorController.cs`, Migration `AddTwoFactorAuth`, `src/services/twoFactorService.js`, `src/components/TwoFactorPrompt.js`(+טסט), `src/pages/settings/TwoFactorCard.js`(+טסט), `src/styles/two-factor.css`; שונו — `AuthService`/`AuthController`/`Program.cs`/`AppDbContext`/`User`/`LoginDto`/`GoogleLoginDto`/`AccountService` (מחיקת חשבון מנקה גם את שלוש הטבלאות החדשות), `src/services/authService.js`, `src/pages/LoginPage.js`, `src/pages/SettingsPage.js`.
- **אימות (מול שרת רץ, 33/33):** כניסה מחזירה 202 בלי טוקן · סיסמה שגויה עדיין 401 · אתגר לא ניתן לשימוש חוזר · **אסימון מכשיר מזויף אינו פוטר מהקוד** · קוד גיבוי עובד **פעם אחת בלבד** והמונה יורד ל-9 · שליחה חוזרת מבטלת את האתגר הישן · הנפקה מחדש מבטלת את הקודים הקודמים · הגדרות 2FA דורשות טוקן (401) · הפעלת SMS בלי ספק נדחית (400) · היעד מוצג מצונזר. `npm test` 299/299; `npm run build` נקי; `dotnet build` 0 שגיאות.
- **⚠️ לקח:** ריצת הבדיקה הראשונה נכשלה ב-6 מקומות — **לא באג במוצר אלא הגבלת הקצב שעבדה כמתוכנן** (10 ניסיונות/דקה). מסקנה: סקריפט בדיקה שנוגע בנקודות כניסה חייב מכסה נפרדת לכל קריאה, אחרת 429 מתחזה לכשל תכונה.
- **עדכון באותו יום — נפתח גם לוועדים:** בעלת המוצר שאלה "אין את זה ללקוחות?" והתכונה נפתחה לכל מנהלת ועד. **בבחירתה ולא בכפייה** — כפייה בכל כניסה מהטלפון בין הסידורים מובילה לנטישה, ואז ההגנה שווה אפס גם למי שכן הייתה רוצה אותה. הפער בין ההחלטות היה גדול (רשות מול חובה) ולכן נבנתה הרשות, וההחלטה על חובה נותרה מפורשת אצל בעלת המוצר. השינוי עצמו הוא הסרת תנאי התפקיד בלבד — הצד השרתי היה גנרי ופר-משתמש מלכתחילה. **נוסף שומר:** חשבון בלי סיסמה אינו יכול להפעיל אימות, כי כיבוי דורש אימות סיסמה והחשבון היה נועל את עצמו לצמיתות (זרימת ההרשמה הנוכחית תמיד קובעת סיסמה, אך המודל מתיר חשבון כזה). האימות מקצה-לקצה רץ ממילא על חשבון בתפקיד `Member` — 33/33.

## 17.08.2026 — חיווי "מצב ההגנות" + הצפנת רשומות ותיקות [Claude Opus]

- **מה נעשה:** בעלת המוצר הגדירה את מפתח ההצפנה ושאלה "איך אני יודעת שזה עבד?" — ובצדק: המערכת מתנהגת **בדיוק אותו דבר** עם הצפנה ובלעדיה, וההבדל היחיד הוא מה כתוב בקובץ המסד. נוסף `GET /api/admin/security` (SuperAdmin) + כרטיס **"מצב ההגנות"** במסך הניהול, שמציג **דגלים בלבד** (לעולם לא ערכים): הצפנה במנוחה, מפתח JWT חזק, CORS לייצור, גיבוי אוטומטי (+ מתי היה האחרון), וסליקה. כשהגנה כבויה כתוב **בדיוק איזה משתנה להגדיר**.
- **הפער שהתגלה תוך כדי — והושלם:** ההצפנה חלה על כל שמירה מרגע הפעלת המפתח, אבל **רשומות ותיקות נשארות כטקסט** עד שמישהו יערוך אותן — כלומר דווקא רוב הנתונים נשארים חשופים. נוסף `IEncryptionBackfillService` + `POST /api/admin/encrypt-existing` וכפתור שמופיע **רק** כשההצפנה דלוקה ויש מה להצפין, עם מספר הרשומות הממתינות. הפעולה אידמפוטנטית (ערך מוצפן מדולג), רצה בטרנזקציה אחת, ומסרבת לרוץ בלי מפתח.
- **🔴 באג אמיתי שנתפס רק בגלל בדיקה מול הקובץ עצמו:** אחרי ההצפנה למפרע, הערך הגלוי **המשיך לשכב על הדיסק** — SQLite שומר את הגרסה הישנה של השורה ביומן הכתיבה (WAL) עד לנקודת ביקורת. כלומר הפעולה דיווחה על הצלחה אך **לא השיגה את מטרתה**. תוקן: אחרי ההצפנה מורץ `PRAGMA wal_checkpoint(TRUNCATE)` שמסיר את העותק הישן.
- **קבצים:** חדשים — `backend/DTOs/SecurityStatusDto.cs`, `backend/Services/SecurityStatusService.cs`, `backend/Services/EncryptionBackfillService.cs`, `src/services/securityStatusService.js`, `src/pages/admin/SecurityStatusCard.js`(+טסט), `src/styles/security-status.css`; שונו — `backend/Controllers/AdminController.cs`, `backend/Program.cs`, `src/pages/UsageStatsPage.js`.
- **החלטות:** (1) **דגלים ולא ערכים** — החיווי לעולם לא מחזיר מפתחות. (2) **סליקה כבויה אינה מסומנת כליקוי** — זו החלטה עסקית ולא פער אבטחה. (3) **כפתור ולא הרצה אוטומטית בעלייה** — כתיבה גורפת על כל הרשומות בכל פריסה היא סיכון מיותר; הפעולה מפורשת, נראית, וגיבוי אוטומטי רץ ממילא לפניה.
- **אימות (מול שרת רץ + הקובץ עצמו):** נוצרה רשומה **בלי** מפתח → אומת שהיא גלויה בקובץ; הופעל המפתח → החיווי הראה `pendingEncryption:1`; הרצת הפעולה → `updated:1`, החיווי ירד ל-0, הרצה חוזרת החזירה `updated:0` (אידמפוטנטי), הנתון נשאר קריא ב-API, ו**בדיקה בינארית של כל קובצי המסד אישרה שהטקסט הגלוי נעלם**. גישת ועד רגיל לנקודת הקצה → **403**. `npm test` 290/290; `npm run build` נקי; `dotnet build` 0 שגיאות.
- **⚠️ לקח לתהליך:** במהלך העבודה בוזבז זמן על בדיקות שנתנו תשובות שגויות — `pkill -f` מ-Git Bash **אינו הורג** את תהליך ה-.NET בחלונות, ולכן בדקתי מול שרת ישן ובניתי מול בינארי נעול. הכלל: לעצור תמיד עם `Stop-Process` של PowerShell ולאמת שאפס תהליכים נותרו.

## 17.08.2026 — שלוש החלטות של בעלת המוצר: ת"ז יורדת · מחיקה ב-30.8 · הצפנה במנוחה [Claude Opus]

- **(1) תעודת זהות הוסרה מהמערכת** (מזעור מידע — החלטת בעלת המוצר). זה השדה שהעלה יותר מכל את רמת האבטחה הנדרשת: מזהה ייחודי של קטין. הוסר מהמודל, משני ה-DTO, מ-`StudentService`, מטופס התלמיד וממיפוי הייבוא, ו-Migration `RemoveStudentIdNumber` **מוחקת את העמודה** — כלומר גם התעודות שכבר נשמרו. **תופעת לוואי שטופלה:** זיהוי הכפילויות בייבוא התבסס על ת"ז; הוא עבר לזיהוי לפי שם מלא — מסלול שכבר היה קיים עבור תלמידים בלי ת"ז בקובץ. עמודת ת"ז בקובץ של משרד החינוך פשוט אינה נקראת יותר.
- **(2) מדיניות שמירה: מחיקת נתוני השנה ב-30 באוגוסט** (במקום 1 בספטמבר). זו גם התשובה למדיניות השמירה שעורכת הדין ביקשה: נתוני ילדים אינם נשמרים מעבר לשנת הלימודים. `YearEndCleanupService` עודכן, ו-Migration `MoveCleanupToAugust30` מזיזה גם את הגנים הקיימים — רק אלה שהמועד שלהם הוא 1.9 (מועד שהוגדר ידנית אינו נדרס).
- **(3) הצפנת שדות רגישים במנוחה.** נוסף `FieldEncryption` — **AES-256-GCM** (הצפנה + אימות שלמות), nonce אקראי לכל ערך, בפורמט `enc:v1:<base64>`. מוצפנים: **אלרגיות** (מידע רפואי), **כתובת מגורים**, **טלפוני ומיילי ההורים**. שם ושם משפחה **לא** — הם מוצגים, ממוינים ומחופשים בכל מסך, והצפנתם הייתה שוברת חיפוש בלי להוסיף הגנה ממשית. המפתח מגיע מ-`Encryption:Key` בלבד (משתנה סביבה) ולעולם לא מהמסד.
- **למה זה משנה:** מסד הנתונים הוא קובץ יחיד על דיסק בענן. עד היום, מי שמשיג עותק שלו — גיבוי שדלף, גישה לחשבון הענן — קרא הכול. מעכשיו השדות הרגישים ביותר בלתי-קריאים בלי המפתח.
- **קבצים:** חדשים — `backend/Auth/FieldEncryption.cs`, `Migrations/*RemoveStudentIdNumber.*`, `Migrations/*MoveCleanupToAugust30.*`; שונו — `Models/Student.cs`, `DTOs/StudentWriteDto.cs`, `DTOs/StudentResponseDto.cs`, `Services/StudentService.cs`, `Services/YearEndCleanupService.cs`, `Program.cs`, `appsettings.json`, `src/components/StudentForm.js`, `src/services/studentsImport.js`(+טסט).
- **החלטות:** (1) **קידומת `enc:v1:`** — מאפשרת לזהות מה כבר מוצפן, כך שרשומות קיימות ממשיכות להיקרא ואין צורך בהמרה חד-פעמית מסוכנת של כל המסד. (2) **בלי מפתח ההצפנה כבויה** — סביבת פיתוח עובדת בלי הגדרה, והפעלה בייצור היא הוספת משתנה אחד. (3) **כשל פענוח מחזיר מחרוזת ריקה ולא זורק** — מסך תלמיד לא אמור לקרוס בגלל שדה אחד; אובדן קריאוּת של שדה עדיף על נפילת מערכת. (4) הוחלט **לא** להצפין שם/שם משפחה — הבחנה מודעת ומתועדת, לא השמטה.
- **אימות (מול שרת רץ + בדיקת הקובץ עצמו):** נוצר תלמיד עם אלרגיה, כתובת וטלפון; ה-API החזיר אותם **קריאים** כרגיל (201 + ערכים נכונים), ובדיקה בינארית של קובץ המסד הראתה **שהערכים אינם מופיעים בטקסט גלוי** ושקיימים סימני `enc:v1:` (הכתיבה נמצאה ב-`vaadygo.db-wal`, כצפוי במצב WAL). הרצה **בלי מפתח**: ה-API החזיר 200 והשדות המוצפנים הוצגו ריקים — בלי קריסה ובלי "זבל". `dotnet build` 0 שגיאות; `npm test` 287/287; `npm run build` נקי.
- **⚠️ נדרש מבעלת המוצר להפעלה בייצור:** לייצר מפתח (`openssl rand -base64 32`) ולהגדיר ב-Railway (שירות `soothing-clarity`) משתנה `Encryption__Key`. **עד שיוגדר — ההצפנה כבויה והמידע נשמר כטקסט, כמו היום.** ⚠️ **המפתח הוא נכס קריטי:** אובדנו = אובדן קריאוּת השדות המוצפנים. יש לשמור עותק במקום בטוח מחוץ ל-Railway.
- **הצעד הבא:** אימות דו-שלבי לחשבון המנהלת (הסעיף האחרון שנותר מרשימת הפרטיות).

## 17.08.2026 — פרטיות: Sentry הדליף טוקנים · זכות עיון · נוהל אירוע [Claude Opus]

מענה לרשימת פערי הפרטיות של בעלת המוצר — כל מה שניתן לקדם **בלי** להמתין לתשובותיה.

- **🔴 הממצא המרכזי — Sentry היה פעיל ולא ידוע.** בדיקה של קובץ ה-JS שבאוויר גילתה **מפתח Sentry אמיתי (DSN)** מוגדר בייצור — כלומר שירות ניטור חיצוני קיבל דיווחי תקלות מהדפדפנים של המשתמשות. שתי בעיות: (א) הוא **לא נכלל** ברשימת ספקי המשנה שנמסרה לעורכת הדין; (ב) Sentry שולח עם כל תקלה את **כתובת הדף** — ובכתובות של VaddyGo יש סודות: `/supplier/<טוקן>` (קישור עריכה של ספק), `/join/<טוקן>` (הזמנה לגן) ו-`/poll/<טוקן>`. כלומר **טוקני גישה דלפו לצד שלישי**, ומי שרואה אותם בלוח הבקרה של Sentry יכול להשתמש בהם. תוקן ב-`monitoring.js`: `beforeSend` מחליף כל טוקן ב-`<redacted>`, אותו ניקוי חל על ה-breadcrumbs, לוגים של הדפדפן אינם נשלחים כלל (`beforeBreadcrumb` מסנן `console`), ו-`sendDefaultPii=false` מפורש. 5 בדיקות חדשות נועלות את הניקוי.
- **זכות העיון (סעיף 8 ברשימה) — מומשה.** עד היום הייתה רק מחיקה; אפשר היה למחוק הכול אבל לא לראות מה נשמר. נוסף `IDataExportService` + `GET /api/account/export` (דורש token, מחזיר תמיד את המידע של השולחת בלבד) וכרטיס "הורדת המידע שלי" בהגדרות. הקובץ נוצר בדפדפן ויורד ישירות — לא נשמר אצלנו ולא נשלח במייל.
- **נוהל אירוע אבטחת מידע (סעיף 9) — נכתב.** `SECURITY_INCIDENT.md`: מה נחשב אירוע, שלב עצירה מיידי עם טבלת פעולות לפי סוג האירוע, תיעוד, פנייה לעורכת הדין תוך 24 שעות, יידוע לקוחות, ולקח. כולל רשימת החולשות הידועות שיש להביא בחשבון באירוע.
- **סעיפים שנבדקו והתבררו כסגורים:** גיבויים (סעיף 3) — קיימים מ-09.08; הגבלת קצב התחברות (חלק מסעיף 10) — קיימת מ-10.08; שלמות מחיקת חשבון (סעיף 11) — אומתה: 16 טבלאות נמחקות, כולל `GroupMembers` ו-`GroupInvites`. ספקים **אינם** נמחקים עם חשבון ועד — וזו ההתנהגות הנכונה: ספק הוא לקוח נפרד ולא נתון של הוועד. תיעוד מיושן שהזכיר את Anthropic במקום Gemini (סעיף 12) — תוקן.
- **קבצים:** חדשים — `SECURITY_INCIDENT.md`, `backend/Services/DataExportService.cs`, `src/pages/settings/ExportDataCard.js`, `src/services/monitoring.test.js`; שונו — `src/services/monitoring.js`, `src/services/authService.js`, `src/pages/SettingsPage.js`, `backend/Controllers/AccountController.cs`, `backend/Program.cs`.
- **החלטות:** (1) **הניקוי ב-`beforeSend` ולא בנקודת הקריאה** — כך גם תקלות ש-Sentry תופס אוטומטית (ולא עוברות דרך הקוד שלנו) מנוקות. (2) **הייצוא אינו כולל סיסמאות, גיבובים, טוקנים או מפתחות סליקה** — הוא נועד לעיון, לא להעתקת אמצעי הזדהות. (3) נוסף **כלל מחייב** ב-`DataExportService`: כל טבלה חדשה עם `GroupId` צריכה להתווסף גם לייצוא וגם למחיקה — שני צדדים של אותה זכות.
- **אימות (מול שרת רץ):** ייצוא בלי טוקן → **401**; עם טוקן → קובץ עם 14 מקטעים וכותרת הורדה תקינה; בדיקה אוטומטית על תוכן הקובץ אישרה **אפס** דליפות של גיבוב סיסמה, טוקני גישה או מפתחות סליקה. `npm test` — 287/287; `npm run build` נקי; `dotnet build` 0 שגיאות.
- **הצעד הבא — דורש החלטה של בעלת המוצר:** מזעור מידע (סעיף 1) — אילו שדות להפסיק לשמור; מדיניות שמירה (סעיף 2) — כמה זמן שומרים נתוני ילד שעזב; אזור אירוח (6), DPA (5) ו-DPO (7). **דורש בנייה ואושר להצעה:** הצפנת שדות רגישים במנוחה (4) ואימות דו-שלבי למנהלת (10).

## 10.08.2026 — אבטחה (סבב ג'): תקרת אחסון לכרטיס ספק + חסימת הרשמה המונית [Claude Opus]

הסעיף האחרון שנותר מהביקורת — ובבדיקה התברר שיש לו שני צדדים.

- **מה נעשה:** תמונות המוצרים נשמרות **בתוך המסד** כ-base64, ולתמונה בודדת הייתה תקרה — אבל לא לסך הכולל. המסד הוא **קובץ אחד** על הדיסק של Railway, ולכן מילוי שלו מפיל את **כל** המערכת, לא רק את הכרטיס החורג. נוסף `VendorLimits`: עד **300 מוצרים** ועד **25MB תמונות** לכרטיס (‏`Vendors:MaxProducts`/`Vendors:MaxImageBytes`), נאכף בכל שלושת מסלולי השמירה (יצירה ע"י המנהלת, עריכה ע"י המנהלת, עריכה עצמית של הספק — שתי האחרונות דרך `ApplyWrite`). חריגה זורקת `PayloadTooLargeException` שממופה ב-Middleware ל-**413** עם הודעה שאומרת מה למחוק.
- **הצד השני שהתגלה תוך כדי:** מכיוון ששמירה מחליפה את **כל** רשימת המוצרים, כרטיס בודד ממילא מוגבל לגודל בקשה אחת (~30MB, ברירת המחדל של Kestrel). כלומר הסיכון האמיתי אינו ספק אחד שמנפח כרטיס — אלא **הרשמה עצמית פתוחה**: מי שירצה יכול לפתוח אלפי כרטיסי ספק ולמלא כל אחד. הרשמת/כניסת הספקים הועברו מהמדיניות הרגילה (60/דקה) למדיניות המחמירה **10/דקה** — כמו אצל הוועד.
- **קבצים:** חדשים — `backend/Services/VendorLimits.cs`, `backend/PayloadTooLargeException.cs`; שונו — `backend/Services/VendorService.cs`, `backend/Middleware/ErrorHandlingMiddleware.cs`, `backend/Controllers/PublicVendorsController.cs`.
- **החלטות:** (1) **תקרות נדיבות בכוונה** — 300 מוצרים × תמונה מכווצת (~80KB) ≈ 24MB, כלומר קטלוג אמיתי לא מתקרב אליהן; הן קיימות לעצור שימוש חריג, לא להגביל ספק. (2) **413 ולא 400** — זה מצב "התוכן גדול מדי" ולא "הקלט שגוי", וההודעה מסבירה מה למחוק. (3) **מדידה לפי אורך המחרוזת** ולא פענוח base64 — מדויק מספיק לתקרה ולא צורך זיכרון. (4) **התקרה נבדקת ב-`ApplyWrite`** — נקודה אחת שדרכה עוברות שתי דרכי העריכה, כך שאין מסלול שנשכח.
- **אימות (מול שרת רץ):** 301 מוצרים → **413** "אפשר עד 300 מוצרים"; ~28MB תמונות → **413** "סך התמונות חורג מ-25MB"; **קטלוג ריאלי של 60 מוצרים עם תמונות (~4.8MB) נשמר כרגיל (201)** — כלומר ספק אמיתי אינו נפגע; 12 הרשמות ספק ברצף → **7 עברו ואז 429**. `dotnet build` 0 שגיאות; `npm test` 282/282; `npm run build` ירוק.
- **סטטוס הביקורת: כל הסעיפים נסגרו.** מה שנשאר הוא מגבלה מוצהרת ולא סעיף פתוח: הגבלת הקצב היא לפי IP, ולכן אינה מגנה מפני מתקפה מבוזרת — לכך נדרשת שכבה בפלטפורמה (Cloudflare/Railway).

## 10.08.2026 — אבטחה (סבב ב'): מחיקת חשבון מלאה · תפוגה לקישור הספק · חוזק סיסמה [Claude Opus]

שלושת הסעיפים שנותרו מהביקורת, לפי סדר העדיפויות שקבעה בעלת המוצר (4 → 1 → 2).

- **(4) מחיקת חשבון — הושלמה.** ‏**תיקון לדיווח קודם שלי:** טעיתי כשאמרתי שאין לוועד מחיקת חשבון — היא קיימת מזמן (`DELETE /api/account` + `DeleteAccountCard` בהגדרות). הפער האמיתי היה אחר ורציני יותר: המחיקה **לא כללה טבלאות שנוספו אחריה** — הסקרים (`Polls`/`PollOptions`/`PollVotes`, נוספו הבוקר) והפניות לספקים (`Leads`). כלומר לקוחה שביקשה למחוק את חשבונה השאירה במסד את שאלות הסקר, ההצבעות ופרטי הפניות שלה. תוקן, ונוסף **כלל מפורש בראש המתודה**: כל טבלה חדשה עם `GroupId` חייבת להתווסף שם — כי טבלה שנשכחת לא מייצרת שגיאה, רק משאירה מידע אישי בשקט.
- **(1) קישור העריכה של הספק — תפוגה.** הקישור שנשלח בוואטסאפ העניק גישה מלאה לכרטיס **לנצח**; הודעה שהועברה או טלפון שאבד = גישה קבועה. נוסף `Vendor.EditTokenIssuedAt` + `VendorTokenPolicy` (חלון של 30 יום, `Vendors:EditTokenDays`), וכל **שבע** נקודות האחזור לפי טוקן עוברות עכשיו דרך בדיקת תוקף אחת. כדי שספק אמיתי לא ייתקע: **כל התחברות (רגילה או Google) מחדשת את החלון**, ו"הפקת קישור" אצל המנהלת מנפיקה טוקן **חדש** כשהישן פג — כלומר משמשת גם ככפתור ביטול לקישור שהודלף. המיגרציה מעניקה לספקים הקיימים חלון שמתחיל בזמן הפריסה, כך שאיש לא נחסם ברגע העלייה.
- **(2) חוזק סיסמה.** המינימום בשרת היה 6 תווים — `123456` התקבל. נוסף `PasswordPolicy` (‏**8 תווים** + רשימת סיסמאות נפוצות + חסימת תו חוזר), ונאכף ב-**כל שבעת** המסלולים: הרשמה/שינוי/איפוס של הוועד, והרשמה/הגדרת-פרטים/שינוי/איפוס של הספק. כל ההודעות והוולידציות בלקוח יושרו ל-8.
- **קבצים:** backend חדשים — `Auth/PasswordPolicy.cs`, `Services/VendorTokenPolicy.cs`, `Migrations/*AddVendorEditTokenIssuedAt.*`; שונו — `Models/Vendor.cs`, `Services/AccountService.cs`, `Services/AuthService.cs`, `Services/VendorService.cs`, `Controllers/PublicVendorsController.cs`, 4 קובצי DTO. frontend — 6 מסכי סיסמה יושרו ל-8 תווים.
- **החלטות:** (1) **אורך + רשימה שחורה, בלי דרישת תווים מיוחדים** — דרישות כאלה דוחפות ל"Aa1!" בסוף סיסמה חלשה; זו גם ההמלצה העדכנית של NIST. (2) **טוקן בלי חותמת = פג** (fail-closed), והמיגרציה נותנת חותמת לקיימים — כך אין חור שנשאר פתוח בשקט. (3) **התחברות מחדשת את החלון** — התפוגה אמורה לפגוע בקישור נטוש/מודלף, לא בספק פעיל. (4) **אותה מדיניות סיסמה לספקים ולוועד** — אחרת צד הספקים היה דלת אחורית.
- **אימות (מול שרת רץ):** *מחיקה* — נוצר משתמש עם גן, סקר והצבעה; אחרי `DELETE /api/account` הסקר הציבורי חזר **404** (לפני כן 200). *תפוגה* — בהרצה עם `Vendors__EditTokenDays=0` קישור טרי החזיר **404** מיד, והפקה חוזרת החזירה **טוקן שונה** (הישן מת); בהרצה רגילה: פתיחת פורטל 200 → הגדרת מייל+סיסמה 200 → התחברות → הפורטל נפתח שוב **200** (ספק פעיל לא ננעל). *סיסמאות* — `12345678`/`password`/`aaaaaaaa` נדחו כ"נפוצה מדי", `short7` נדחתה על אורך, וסיסמה חזקה התקבלה (200); גם בצד הספק סיסמה חלשה נדחתה. `dotnet build` 0 שגיאות; `npm test` 282/282; `npm run build` ירוק.
- **מה נשאר פתוח:** תקרה לנפח התמונות המצטבר של ספק (יש תקרה לתמונה בודדת בלבד) — הסעיף היחיד שנותר מהביקורת.

## 10.08.2026 — אבטחה: הגבלת קצב על הכניסה + כותרות אבטחה [Claude Opus]

- **מה נעשה:** ביקורת אבטחה יזומה (בקשת בעלת המוצר) מצאה שתי פרצות פתוחות, ושתיהן נסגרו. **(1) ניחוש סיסמאות ללא הגבלה:** `/api/auth/login`, ההרשמה, איפוס הסיסמה וכניסת הספקים היו פתוחים ללא שום תקרת ניסיונות — אפשר היה לנסות אלפי סיסמאות בדקה. הסיסמה המינימלית במערכת היא 6 תווים, ולכן זה איום מעשי ולא תיאורטי. נוסף `RateLimitPolicies` (‏Rate Limiter המובנה של .NET) עם שתי מדיניויות לפי כתובת IP: **auth — 10 ניסיונות בדקה** (כניסה/הרשמה/איפוס) ו-**public — 60 בקשות בדקה** (הצבעה בסקר, פורטל הספק, קטלוג). חסימה מחזירה **429** עם הודעה בעברית. **(2) כותרות אבטחה חסרות:** נוסף `SecurityHeadersMiddleware` — `X-Frame-Options: DENY` (מונע הטמעת המערכת ב-iframe זר, clickjacking), `X-Content-Type-Options: nosniff` (מונע הרצת תמונת מוצר שהועלתה כאילו הייתה סקריפט), `Referrer-Policy` (מונע דליפת כתובות שמכילות **טוקן של ספק** לאתרים חיצוניים), ו-`Strict-Transport-Security` בייצור בלבד.
- **למה:** אלה שתי החולשות הבסיסיות ביותר שנותרו אחרי שבידוד הנתונים, ה-JWT והפרו כבר טופלו. הראשונה מאפשרת השתלטות על חשבון ועד עם נתוני ילדים; השנייה נדרשת כמעט בכל ביקורת אבטחה בסיסית.
- **קבצים:** חדשים — `backend/Services/RateLimitPolicies.cs`, `backend/Middleware/SecurityHeadersMiddleware.cs`; שונו — `backend/Program.cs`, `backend/Controllers/AuthController.cs`, `PublicPollsController.cs`, `PublicVendorsController.cs`.
- **החלטות:** (1) **חלוקה לפי IP ולא לפי משתמש** — בשלב הכניסה עוד אין משתמש מזוהה. ב-Railway הבקשה עוברת proxy, ולכן ה-IP נלקח מ-`X-Forwarded-For`; היעדר IP נופל למכסה משותפת (מחמיר = בטוח). (2) **שתי מכסות נפרדות** — 10/דקה על הכניסה היה חונק את ההצבעה בסקר (הורים רבים באותו רגע), ו-60/דקה על הכניסה היה רופף מדי. (3) **הכותרות בקוד ולא בהגדרת הפלטפורמה** — כך ההגנה נוסעת עם הפריסה ולא תלויה בהגדרה שאפשר לשכוח. (4) **HSTS בייצור בלבד** — בפיתוח מקומי אין תעודה, והכותרת הייתה שוברת את העבודה המקומית.
- **מגבלה מוצהרת:** ההגבלה היא לפי IP, ולכן מגנה מפני ניחוש גס ממקור אחד — לא מפני מתקפה מבוזרת מהרבה כתובות. לזה נדרשת שכבה בפלטפורמה (Cloudflare/Railway).
- **אימות (מול שרת רץ):** 14 ניסיונות כניסה עם סיסמה שגויה → **10 פעמים 401 ואז 429** עם ההודעה בעברית; הצבעה בסקר המשיכה לעבוד באותו זמן (מכסה נפרדת); **המשתמשת האמיתית השתחררה אחרי 45 שניות והתחברה בהצלחה (200)** — כלומר החסימה אינה נועלת לקוחה אמיתית; הכותרות מוחזרות בכל תשובה, ו-HSTS הופיע **רק** בהרצת ייצור. `dotnet build` 0 שגיאות; `npm test` 282/282; `npm run build` ירוק.
- **מה עדיין פתוח (לפי סדר חשיבות):** (א) **טוקן העריכה של הספק אינו פג לעולם** — קישור שהועבר בוואטסאפ מעניק גישה קבועה לכרטיס; שווה תפוגה/רענון. (ב) **סיסמה מינימלית 6 תווים** בלבד בשרת (בלקוח יש מד חוזק) — שווה להעלות ל-8 ולחסום סיסמאות נפוצות. (ג) אין תקרה לנפח התמונות המצטבר של ספק (יש תקרה לתמונה בודדת). (ד) מחיקת חשבון/נתונים ביוזמת הלקוח (GDPR) קיימת לספק אך לא לוועד.

## 10.08.2026 — הסליקה נשארת מנותקת (החלטת בעלת המוצר) + שער שמונע "תשלום מדומה" [Claude Opus]

- **מה נעשה:** בעלת המוצר החליטה **לא לחבר סליקה כרגע** — ~₪500/חודש אינם מוצדקים לפני שיש ספקים משלמים (חיזוק להחלטתה מ-05.08). זה חשף **פגם שנוצר ברשומה הקודמת**: כפתור "מעבר לתשלום מאובטח" היה מוביל ספק אמיתי אל **עמוד התשלום המדומה** (‏`Payments:Provider=mock`) — כלומר הבטחה לגבייה שלא באמת מתרחשת. תוקן בשלוש שכבות: (1) `VendorResponseDto.ProCheckoutAvailable` מחושב בשרת (`Provider != mock`) ומוחזר לפורטל הספק; (2) `SupplierUpgrade` מציג כפתור תשלום **רק** כשהדגל דלוק, ואחרת חוזר לנוסח הקודם ("לשדרוג — דברו איתנו") עם הוואטסאפ ככפתור הראשי; (3) `POST /pro-checkout` מחזיר **503** עם הודעה ידידותית גם אם קוראים לו ישירות, כדי שלא ייפתח תשלום מדומה בשום מסלול.
- **למה:** ספק שמגיע לעמוד תשלום שאינו גובה — או משלם ולא מקבל מסלול, או מאבד אמון במערכת. עדיף לומר "עדיין לא זמין" מאשר להעמיד פנים. הקוד עצמו נשאר מוכן: ברגע שיוגדרו מפתחות אמיתיים הכל נדלק בלי שינוי קוד.
- **קבצים:** backend — `DTOs/VendorDtos.cs`, `Services/VendorService.cs`, `Services/VendorProPaymentService.cs` (`IsAvailable`), `Controllers/PublicVendorsController.cs`. frontend — `components/SupplierUpgrade.js`(+טסט).
- **החלטות:** (1) **מקור ההחלטה הוא השרת ולא הלקוח** — הלקוח לא יכול "לנחש" אם יש סליקה; כך אין מצב שהכפתור מופיע בטעות. (2) **שער כפול** (הסתרה בממשק + 503 בשרת) — הסתרה לבדה אינה הגנה. (3) הדגל נגזר מ-`Payments:Provider` שכבר קיים — בלי הגדרה חדשה שאפשר לשכוח.
- **אימות (מול שרת רץ):** במצב ברירת המחדל (mock) הפורטל מקבל `proCheckoutAvailable:false` וקריאה ישירה ל-`pro-checkout` מחזירה **503**; בהרצה עם `Payments__Provider=payplus` הדגל מתהפך ל-**true**. `dotnet build` 0 שגיאות; `npm test` 282/282; `npm run build` ירוק.
- **הצעד הבא:** כשיהיו ~5 ספקים משלמים ומפתחות סליקה — להגדיר את משתני הסביבה (ראו הרשומה על הסליקה), ולאמת מול תיעוד PayPlus את שמות שדות ה-IPN (‏`TODO(sandbox)` ב-`PayPlusGateway`). עד אז: פתיחת פרו לספק נעשית ידנית במסך הניהול.

## 10.08.2026 — מסך מנויים למנהלת: מי משלם ועד מתי [Claude Opus]

- **מה נעשה:** אחרי שנבנו שני ערוצי ההכנסה (פרו לוועדים ופרו לספקים), לא הייתה למנהלת שום דרך לראות מי משלם ומתי המנוי פג. **שרת:** `ISubscriptionsService`/`SubscriptionsService` + `GET /api/admin/subscriptions` (בתוך `AdminController` הקיים, ולכן כבר מוגן ב-`[Authorize(Roles="SuperAdmin")]`), שמחזיר שורה לכל ועד ולכל ספק עם סטטוס מחושב: **פעיל · פג בקרוב (תוך 30 יום) · פג · לא מנוי** + מונה מסכם. **לקוח:** `SubscriptionsCard` במסך ניהול VaddyGo — סיכום ("X מנויים פעילים · Y פגים בתוך 30 יום") ושתי רשימות עם תגית צבעונית ותאריך תפוגה בשפה פשוטה ("10.08.2027 (עוד 365 ימים)").
- **למה:** בקשת בעלת המוצר. זו השאלה העסקית היומיומית — מה נכנס ומה עומד להיפסק — וההתראה "פג בקרוב" היא מה שמאפשר לפנות ללקוח **לפני** שהמנוי נסגר במקום אחרי.
- **קבצים:** backend חדשים — `DTOs/SubscriptionsDto.cs`, `Services/SubscriptionsService.cs`; שונו — `Controllers/AdminController.cs`, `Program.cs`. frontend חדשים — `services/subscriptionsService.js`, `pages/admin/SubscriptionsCard.js`(+טסט), `styles/subscriptions.css`; שונה — `pages/UsageStatsPage.js`.
- **החלטות:** (1) **הסטטוס מחושב בשרת** ולא בלקוח — אחרת המסך היה עלול להראות "פעיל" בזמן שהשרת כבר חוסם את הפיצ'רים (אותו `ProPolicy` שאוכף בפועל). (2) **מנוי בלי תאריך תפוגה מוסבר במילים** ("ללא תאריך תפוגה") ולא במקף — זו פתיחה ידנית של המנהלת, ומקף היה נראה כמו נתון חסר. (3) הסטטוס מקבל **צבע** ולא רק טקסט, כי הוא נושא משמעות כספית (כסף שעומד להיפסק). (4) נוסף לכרטיס בתוך מסך הניהול הקיים ולא כעמוד נפרד — אותה שאלה, אותו מקום.
- **אימות (מול שרת רץ):** ועד רגיל → **403**; בלי טוקן → **401**; המנהלת → הנתונים הנכונים, כולל הספק ששילם בסליקה (תוקף 10.08.2027, 365 ימים). הוגדר מנוי שפג בעוד 10 ימים → הסטטוס התהפך ל-`expiring` והמונה `expiringSoonCount` עלה ל-1. `dotnet build` 0 שגיאות; `npm test` 281/281; `npm run build` ירוק.
- **הצעד הבא:** לבעלת המוצר — להגדיר את מפתחות הסליקה האמיתיים (ראו הרשומה הקודמת). אופציונלי בהמשך: התראה יזומה (מייל/וואטסאפ) כשמנוי מתקרב לתפוגה, במקום להיכנס למסך ולבדוק.

## 10.08.2026 — סליקה לשדרוג פרו של ספקים (הספק משלם בעצמו) [Claude Opus]

- **מה נעשה:** עד היום ספק שרצה פרו שלח וואטסאפ למנהלת, והיא פתחה לו ידנית. עכשיו הוא משלם בעצמו: **שרת** — `IVendorProPaymentService`/`VendorProPaymentService` שפותח תשלום דרך `IPaymentGateway` הקיים (אותה תשתית של סליקת הגבייה), עם מזהה עסקה `vendorpro:{vendorId}:{guid}` שנשמר על הספק; `POST /api/public/vendors/{token}/pro-checkout` (‏`AllowAnonymous` — לספק אין חשבון, רק טוקן) מחזיר את כתובת עמוד התשלום; ו-`Vendor.ProValidUntil` + `Vendor.ProTransactionRef` + Migration `AddVendorProSubscription`. **ה-webhook הקיים נותב לפי קידומת המזהה** — כך נשארת כתובת webhook **אחת** להגדיר אצל ספק הסליקה, והיא משרתת גם את תשלומי הגבייה וגם את מנויי הספקים. **לקוח** — `startVendorProCheckout` ו-`SupplierUpgrade` עם כפתור "מעבר לתשלום מאובטח" (הוואטסאפ נשאר כאופציה משנית).
- **למה:** בקשת בעלת המוצר. פרו לספקים (₪1,200/שנה) הוא ערוץ ההכנסה העיקרי; פתיחה ידנית לכל ספק אינה מתרחבת ומעכבת תשלום ברגע שהספק רוצה לשלם.
- **קבצים:** backend חדשים — `Services/VendorProPaymentService.cs`, `Migrations/*AddVendorProSubscription.*`; שונו — `Models/Vendor.cs`, `DTOs/VendorDtos.cs`, `Controllers/PublicVendorsController.cs`, `Services/CardPaymentService.cs` (ניתוב ה-webhook), `Program.cs`, `appsettings.json`. frontend — `services/vendorsService.js`, `components/SupplierUpgrade.js`(+טסט), `pages/SupplierEditPage.js`.
- **החלטות:** (1) **הפרו נפתח רק מה-webhook** (אישור משרת-לשרת) ולעולם לא מחזרת הדפדפן — חזרה לכתובת אפשר לזייף, ואז כל אחד היה פותח מנוי בלי לשלם. (2) **מזהה העסקה נמחק אחרי הפעלה** — כך שידור חוזר של אותו webhook לא מאריך את המנוי פעמיים (אומת). (3) **webhook אחד לכל המערכת**, מנותב לפי קידומת — פחות הגדרות אצל ספק הסליקה ופחות מקום לטעות. (4) **חידוש לפני תפוגה מאריך מהתאריך הקיים** ולא מהיום, כדי שלא "יאבדו" ימים ששולמו. (5) מחיר ותקופה מ-`Payments:SupplierProPrice`/`SupplierProMonths` — שינוי מחיר בלי נגיעה בקוד. (6) פרטי הכרטיס לעולם לא עוברים דרכנו (עמוד מתארח אצל הסליקה) — כתוב במפורש בממשק כדי לבסס אמון.
- **אימות (מול שרת רץ, ספק סליקה בסימולציה):** פתיחת תשלום עם טוקן הספק בלבד → 200 עם כתובת תשלום וסכום 1,200; webhook **בלי הסוד** → נדחה (400); **עם הסוד** → 200 והספק סומן פרו עד 10.08.2027; **שידור חוזר של אותו webhook** → נדחה; **מזהה עסקה מזויף** → נדחה. `dotnet build` 0 שגיאות; `npm test` 278/278; `npm run build` ירוק.
- **⚠️ נדרש מבעלת המוצר להפעלה בייצור:** ‏(א) לפתוח חשבון סליקה ולהגדיר ב-Railway‏ (`soothing-clarity`): `Payments__Provider=payplus`, מפתחות `Payments__PayPlus__*`, ו-`Payments__WebhookSecret`; (ב) להגדיר אצל ספק הסליקה כתובת webhook: `https://soothing-clarity-production.up.railway.app/api/payments/card-webhook`. **עד אז** `Payments:Provider=mock` — כלומר הכפתור עובד מקצה לקצה אבל לא מחייב כסף אמיתי. **תזכורת:** לפי החלטתך מ-05.08 סליקה עולה ~₪500/חודש — כדאי לוודא שהעלות מוצדקת מול מספר הספקים המשלמים.
- **הצעד הבא:** לוודא מול PayPlus את שמות השדות וחתימת ה-IPN (מסומן `TODO(sandbox)` ב-`PayPlusGateway`) ברגע שיהיו מפתחות אמיתיים.

## 10.08.2026 — אכיפת מסלול פרו בשרת (ערוץ ההכנסה מוגן באמת) [Claude Opus]

- **מה נעשה:** עד היום מסלול הפרו היה **דגל בדפדפן בלבד** — כלומר לא הגנה אלא תצוגה: מי שידע לגעת ב-localStorage (או שמצא את קוד הפתיחה שנארז בקובץ ה-JS הציבורי) קיבל את כל כלי הפרו בחינם. מעכשיו הדגל יושב בשרת ונאכף שם. **שרת:** `Group.IsPro` + `Group.ProValidUntil` + Migration `AddGroupPro`; `ProPolicy` — הכלל היחיד ל"פרו פעיל" (דגל דלוק **וגם** התוקף לא עבר; null = בלי תפוגה); `IAccessScope.IsGroupProAsync`; `PUT /api/groups/{id}/pro` (‏`[Authorize(Roles="SuperAdmin")]`); ו-**חסימה בפועל** ב-`PollService.CreateAsync` וב-`AiController.Ask`. נוסף `ProRequiredException` שממופה ב-Middleware ל-**402 Payment Required** (ולא 403) — כדי שהלקוח יבחין בין "אין הרשאה" לבין "צריך לשדרג". **לקוח:** `GroupResponseDto.isPro` מסונכרן למוסדות, `plan.isPro()` קורא קודם את האמת מהשרת, ומתג המנהלת הפך מכלי-בדיקות מקומי ל**כלי ניהול אמיתי** שכותב לשרת (וחל על כל חברות הוועד ובכל מכשיר).
- **למה:** בקשת בעלת המוצר, אחרי שדווח לה שקוד הפתיחה חשוף לכולם. פרו הוא ערוץ ההכנסה של VaddyGo — בלי אכיפה בשרת אין באמת מה למכור.
- **קבצים:** backend חדשים — `Services/ProPolicy.cs`, `ProRequiredException.cs`, `Migrations/*AddGroupPro.*`; שונו — `Models/Group.cs`, `DTOs/GroupResponseDto.cs`, `Services/AccessScope.cs`, `Services/GroupService.cs`, `Services/IGroupService.cs`, `Services/PollService.cs`, `Controllers/GroupsController.cs`, `Controllers/AiController.cs`, `Middleware/ErrorHandlingMiddleware.cs`. frontend — `services/groupsService.js`, `services/institutionsService.js`, `services/plan.js`, `pages/admin/ProTestControl.js`(+טסט).
- **החלטות:** (1) **402 ולא 403** — "צריך לשדרג" הוא מצב עסקי, לא כשל הרשאה; ההבחנה מאפשרת ללקוח להציע שדרוג במקום שגיאה. (2) **`SetProAsync` בלי בדיקת בעלות** (בשונה מכל שאר עדכוני הגן) — המנהלת פותחת פרו למוסדות של לקוחות שאינם שלה; ההרשאה נאכפת בקונטרולר ברמת ה-Role. (3) **תוקף nullable** — תומך גם במנוי שנתי וגם בפתיחה ידנית ללא תפוגה, בלי שדה נוסף. (4) **הפתיחה המקומית נשארה כרשת-ביטחון בלבד** למוסד שעדיין לא סונכרן לשרת — היא כבר לא פותחת נתונים, כי השרת חוסם בכל מקרה. (5) **הצבעת ההורים בסקר נשארה פתוחה** — הפרו חוסם את *יצירת* הסקר, לא את ההשתתפות בו.
- **⚠️ השפעה על קיים:** מוסדות קיימים מקבלים `IsPro=false` וייחסמו מסקרים ומהעוזרת עד שהמנהלת תפתח להם פרו. זו המטרה, אבל יש לפתוח פרו לגן של בעלת המוצר אחרי הפריסה.
- **אימות (מול שרת רץ):** בלי פרו — יצירת סקר **402**; ועד רגיל שמנסה לפתוח לעצמו פרו — **403**; המנהלת פותחת — **200**, ואז יצירת סקר **201**; פרו עם תוקף שעבר — הלקוח מקבל `isPro:false` וגם סקרים וגם AI חוזרים **402**; פרו פעיל — ה-AI עובר את השער ומחזיר 503 (אין מפתח Gemini, כצפוי); **הצבעה ציבורית של הורים נשארה 200**. `dotnet build` 0 שגיאות; `npm test` 274/274; `npm run build` ירוק.
- **הצעד הבא:** חיבור סליקה לשדרוג פרו של ספקים (בקשת בעלת המוצר), ובהמשך — הפיכת התשלום המאומת לטריגר אוטומטי ש`SetProAsync` נקרא ממנו, במקום פתיחה ידנית.

## 10.08.2026 — תיקון באג: השרת עדיין זרק מוצרים בלי שם (השלמת בקשת בעלת המוצר) + ביקורת הרשאות ספקים [Claude Opus]

- **מה נעשה:** **באג שנמצא בעבודה של עצמי.** ב-08.08 יושמה בקשת בעלת המוצר "לאפשר שמירת מוצר ללא שם": הוסר `[Required]` מ-`VendorProductDto.Name` והוסר הסינון בלקוח. אבל נותר **מסלול שלישי שפוספס** — `VendorService.MapProducts` סינן בשקט `\.Where(p => !string.IsNullOrWhiteSpace(p.Name))`. התוצאה: ספק ששמר מוצר עם תמונה ומחיר בלי שם, קיבל "נשמר בהצלחה" — **והמוצר נמחק בשרת**. הסינון הוחלף בבדיקת `HasContent` (שם / תיאור / מחיר>0 / תמונה / תיקייה) — בדיוק אותו כלל שכבר קיים בלקוח ב-`services/vendorProducts.js`, כך ששני הצדדים מסכימים מה שווה לשמור. שורה ריקה לגמרי עדיין לא נשמרת.
- **למה:** הבקשה של בעלת המוצר לא באמת בוצעה — ה-UI הבטיח הצלחה והנתונים אבדו בשקט. זה הכשל החמור מסוגו (איבוד עבודה בלי הודעה), ובדיוק מה שהיא ביקשה לתקן.
- **בנוסף — ביקורת ההרשאה שנשארה פתוחה בשלב 10** ("אכיפת SuperAdmin על ניהול ספקים"): נבדק שהסעיף כבר מיושם ו**אומת בפועל**, ולכן נסגר.
- **קבצים:** `backend/Services/VendorService.cs`, `ROADMAP.md`, `PROJECT_LOG.md`.
- **החלטות:** הכלל "מה שווה לשמור" מוגדר עכשיו **פעמיים בכוונה** — בלקוח (חוויית משתמש מיידית) ובשרת (מקור האמת, כי אסור לסמוך על הלקוח). זו אינה כפילות אסורה אלא ולידציה דו-צדדית לפי `ARCHITECTURE.md`; שניהם מתועדים כמצביעים זה על זה.
- **אימות (מול שרת רץ):** יצירת ספק עם 3 מוצרים — בלי שם (מחיר+תמונה) · עם שם · שורה ריקה לגמרי → נשמרו **2 הראשונים**, הריקה נזרקה; ה-GET מאשר שהמוצר חסר-השם נשמר. אותו דבר דרך **קישור העריכה של הספק**. ביקורת הרשאות עם משתמש `Member`: יצירה/מחיקה/פרו/מומלץ/קישור-עריכה → **403** בכל אחד; צפייה → 200. ואומת שספק **אינו** יכול להעניק לעצמו `isPro`/`featured` דרך הקישור שלו (נשלחו במפורש ב-PUT וחזרו `false`) — `ApplyWrite` אינו נוגע בשדות האלה. `dotnet build` 0 שגיאות; `npm test` 265/265; `npm run build` ירוק.
- **הצעד הבא:** לבעלת המוצר — לבדוק בייצור: פורטל ספק → הוספת מוצר עם תמונה בלי שם → שמירה → רענון הדף; המוצר צריך להישאר ולהופיע כ"מוצר N".

## 09.08.2026 — ביצועים: טעינת מסכים לפי דרישה (הצרור הראשי ירד ב-45%) [Claude Opus]

- **מה נעשה:** עד היום **כל** מסכי המערכת נארזו לקובץ JavaScript אחד. המשמעות: הורה שפותח קישור לסקר (`/poll/:token`) או לקטלוג ספק בנייד הוריד גם את מסכי הגבייה, התלמידים, הספקים, הדוחות והמנהלת — קוד שלא ישמש אותו לעולם. כל המסכים עברו ל-`React.lazy` + `Suspense` (עם ספינר קצר), חוץ ממסך הבית ומסכי הכניסה/הרשמה שנשארו בטעינה מיידית כדי שהמסך הראשון לא יהבהב. **התוצאה: הצרור הראשי ירד מ-196.5KB ל-108.6KB (gzip) — 45% פחות**, ו-43 חבילות נפרדות נטענות רק לפי הצורך.
- **למה:** VaddyGo היא Mobile-First, וחלק מהמשתמשים הם **הורים שאינם משתמשי המערכת** — הם נכנסים פעם אחת מקישור בוואטסאפ, לרוב ברשת סלולרית. הורדה של האפליקציה כולה בשבילם היא בזבוז שמאט את הכניסה בדיוק ברגע הקריטי (ההצבעה בסקר).
- **קבצים:** `src/App.js` (ייבוא lazy + עטיפת שני בלוקי הניתוב ב-Suspense), `src/setupTests.js`, `src/App.test.js`.
- **החלטות:** (1) **מסך הבית והכניסה נשארו מיידיים** — הם המסך הראשון שנראה, וספינר שם היה מרגיש כמו איטיות ולא כמו שיפור. (2) הפיצול נעשה **ברמת הניתוב בלבד** — בלי לגעת בקוד של אף מסך, ולכן אין סיכון לשבירת פיצ'ר. (3) הספריות הכבדות (Excel/PDF) כבר היו בטעינה עצלה ונשארו כך.
- **אימות:** `npm run build` — Compiled successfully; מדידת gzip לפני/אחרי: **196.5KB → 108.6KB**. `npm test` — 265/265, **שתי הרצות מלאות רצופות** ירוקות.
- **תופעת לוואי שטופלה:** אחרי הפיצול, כל בדיקה שמרנדרת מסך ממתינה גם לטעינת החבילה שלו, וכמה בדיקות התחילו להיכשל באקראי **על קוד תקין** כשכל הסוויטות רצות במקביל (זה גם מסביר כישלון אקראי שנצפה קודם ב-`GiftsPage.test.js`). הסיבה בפועל: מגבלת ההמתנה של Testing Library היא **שנייה אחת** בלבד. תוקן ב-`setupTests.js` — `jest.setTimeout(20000)` ו-`configure({ asyncUtilTimeout: 5000 })`. אלה **תקרות ולא המתנות**: בדיקה שעוברת מסיימת מיד. חשוב כי CI אדום חוסם פריסה לכל הסוכנים.
- **הצעד הבא:** אופציונלי — למדוד את זמן הטעינה בפועל בנייד (Lighthouse) על `/poll/:token` בייצור, ולשקול פיצול נוסף אם `HomePage` מתגלה ככבד.

## 09.08.2026 — שלב 11: גיבויים אוטומטיים + הקשחת CORS לייצור [Claude Opus]

- **מה נעשה:** (1) **גיבוי אוטומטי של מסד הנתונים** — `IDatabaseBackupService`/`SqliteBackupService` + משימת רקע `DatabaseBackupBackgroundService`: רצה **בעלייה** (כדי שאחרי כל פריסה יהיה גיבוי טרי) ואז **כל 24 שעות**. הגיבוי נעשה ב-`VACUUM INTO` — הפקודה הרשמית של SQLite ליצירת עותק **עקבי** של מסד חי, בלי לעצור את השרת ובלי הסיכון של העתקת קובץ באמצע כתיבה (מצב WAL). היעד: תיקיית `backups` ליד קובץ המסד — ב-Railway בתוך ה-Volume ב-`/data`, ולכן הגיבויים שורדים פריסה מחדש. נשמרים 7 האחרונים והישנים נמחקים, כדי שהדיסק לא יתמלא. (2) **הקשחת CORS** — בייצור, `"*"` ברשימת המקורות **מונע מהשרת לעלות** (fail-closed, אותו דפוס של שער ה-`Jwt:Key`), וייצור שנשאר בטעות עם `localhost` בלבד רושם אזהרה מפורשת בלוג.
- **למה:** שלב 11 ב-ROADMAP (הקשחת ייצור). עד היום לא היה שום גיבוי — תקלה בקובץ ה-SQLite הייתה מוחקת את כל נתוני הוועדים ללא שחזור. זו גם ההגנה שמחליפה בינתיים את מעבר ה-DB שנדחה.
- **קבצים:** חדשים — `backend/Services/IDatabaseBackupService.cs`, `backend/Services/SqliteBackupService.cs`, `backend/Services/DatabaseBackupBackgroundService.cs`; שונו — `backend/Program.cs` (רישום DI + שערי CORS), `backend/appsettings.json` (סקשן `Backup`).
- **החלטות:** (1) **`VACUUM INTO` ולא העתקת קובץ** — העתקה רגילה של מסד חי במצב WAL עלולה לייצר גיבוי פגום; זו הדרך הנתמכת רשמית. (2) **הגיבוי לא מפיל את השרת** — כשל נרשם בלוג בלבד; גיבוי שנכשל אסור שיוריד את המערכת לוועדים. (3) **CORS: fail-closed רק על `"*"`** — היעדר הגדרה נופל ל-localhost, שהוא *יותר* מחמיר ולא פחות, ולכן שם מזהירים במקום להפיל ייצור שכבר עובד. (4) הגיבוי מדלג בשקט על מסד שאינו SQLite ועל מסד-בזיכרון (בדיקות) — במעבר עתידי ל-PostgreSQL הגיבוי יגיע מהפלטפורמה. (5) **מעבר ל-PostgreSQL לא בוצע** — דחוי בהחלטת בעלת המוצר (21.07.2026): לא דחוף, אינו סוגר פרצה, וכרוך בסיכון לאובדן נתונים.
- **אימות:** `dotnet build` 0 שגיאות. סמוק-טסטים מול שרת רץ: עלייה יצרה גיבוי אמיתי (`vaadygo-<חותמת>.db`, 172KB) עם שורת לוג; עם `Backup__KeepCount=3` ו-6 קבצים בתיקייה — נשארו בדיוק 3 החדשים והישנים נמחקו; ייצור עם `Cors__AllowedOrigins__0=*` **סירב לעלות** עם ההודעה בעברית; ייצור עם הדומיין האמיתי עלה כרגיל; ייצור שנשאר על localhost עלה **עם האזהרה** בלוג. `npm test` — 265/265; `npm run build` — Compiled successfully.
- **הצעד הבא:** לבעלת המוצר — לוודא ב-Railway שה-Volume ממופה ל-`/data` ושה-connection string מצביע לשם (`Data Source=/data/vaadygo.db`), כדי שהגיבויים יישמרו ב-Volume. אופציונלי: הורדת גיבוי מדי פעם למחשב האישי (גיבוי מחוץ לשרת). כל שלבי ה-ROADMAP הפתוחים הושלמו.

## 09.08.2026 — שלב 12 (ה'): סקרים אמיתיים — ההורים עונים בקישור, התוצאות מתעדכנות לבד [Claude Opus]

- **מה נעשה:** הסקרים עברו מספירה ידנית ב-localStorage לסקר אמיתי מקצה לקצה. **שרת:** מודלים `Poll` (שאלה, `PublicToken`, `IsClosed`, שיוך לגן), `PollOption` (טקסט + מונה) ו-`PollVote` (הצבעה בודדת) + Migration `AddPolls` (אינדקס ייחודי על הטוקן, ואינדקס ייחודי על `PollId+VoterKey`), `IPollService`/`PollService`, ושני קונטרולרים דקים: `PollsController` (הוועד — רשימה/יצירה/סגירה/מחיקה, מוגבל לגן שלו דרך `IAccessScope`) ו-`PublicPollsController` (‏`[AllowAnonymous]` — ההורה מזוהה **רק** בטוקן הסקר, ויכול רק לקרוא ולהצביע). **לקוח:** `pollsService` נכתב מחדש מול השרת (עם נפילה מקומית), מסך `/poll/:token` (`PublicPollPage`) שההורים פותחים מהוואטסאפ — בוחרים אפשרות, שולחים, ורואים תוצאות ותודה; ומסך הוועד (`PollsPage`) מציג תוצאות חיות, "העתקת הקישור", וכפתור סגירת סקר.
- **למה:** בקשת בעלת המוצר (08.08.2026, שלב 12). עד היום הוועד היה צריך לספור ידנית תשובות שהגיעו בוואטסאפ — עבודה כפולה ומקור לטעויות.
- **קבצים:** backend חדשים — `Models/Poll.cs`, `DTOs/PollDtos.cs`, `Services/IPollService.cs`, `Services/PollService.cs`, `Controllers/PollsController.cs`, `Controllers/PublicPollsController.cs`, `Migrations/*AddPolls.*`; שונו — `AppDbContext.cs`, `Program.cs`. frontend חדשים — `src/pages/PublicPollPage.js`(+טסט); שונו — `src/services/pollsService.js`(+טסט נכתב מחדש), `src/pages/PollsPage.js`(+טסט), `src/App.js` (נתיב ציבורי), `src/styles/polls.css`.
- **החלטות:** (1) **מניעת הצבעה כפולה לפי מזהה-דפדפן אקראי** (`voterKey` ב-localStorage של ההורה) ולא לפי זיהוי אישי — מונע את הבעיה האמיתית (רענון/לחיצה חוזרת) בלי לאסוף פרטים מהורים שאינם משתמשי המערכת. מי שממש ירצה יוכל להצביע ממכשיר נוסף; לוועד הורים זו רמת ההגנה הנכונה. (2) **הטוקן פותח סקר אחד בלבד** ורק לקריאה והצבעה — לא לעריכה, לא למחיקה, ואין ממנו גישה לרשימת הסקרים או לנתוני הגן. (3) **התוצאות מוסתרות מההורה עד שהצביע**, כדי לא להטות את בחירתו. (4) **סקר מקומי (נוצר בלי שרת) מסומן במפורש** ונשארת בו הספירה הידנית — לא מציגים לו קישור שאינו קיים. (5) הוספה יכולת **סגירת סקר** — התוצאות נשארות לצפייה אבל לא נכנסות עוד תשובות.
- **אימות:** `dotnet build` 0 שגיאות; סמוק-טסט מלא מול שרת רץ: יצירת סקר → קריאה ציבורית **בלי טוקן הזדהות (200)** → הצבעה נספרת (200) → הצבעה שנייה מאותו מכשיר נחסמת (400, "כבר הצבעתם") → הורה שני נספר → הוועד רואה 2 קולות → סגירת סקר (200) → הצבעה בסקר סגור נחסמת (400) → טוקן שגוי 404 → `/api/polls` בלי הזדהות 401 → **ועד אחר מקבל רשימה ריקה ו-404 על מחיקה** (בידוד נתונים). `npm test` — 261/261 (68 suites, 12 בדיקות חדשות); `npm run build` — Compiled successfully.
- **הצעד הבא:** **שלב 12 הושלם.** לבעלת המוצר: לבדוק את הזרימה בייצור (יצירת סקר → שליחת הקישור לעצמה בוואטסאפ → מענה → ראיית התוצאה). הבא בתור: שאריות שלב 11 (מעבר ל-PostgreSQL, גיבויים אוטומטיים, CORS לדומיין הייצור).

## 09.08.2026 — שלב 12 (ד'): מסך נתוני שימוש למנהלת (משפך הרשמה בשני הצדדים) [Claude Opus]

- **מה נעשה:** **שרת** — `AdminController` דק (`GET /api/admin/usage`, ‏`[Authorize(Roles="SuperAdmin")]`) → `IUsageStatsService`/`UsageStatsService` + `UsageStatsDto`/`FunnelDto`. לכל צד מוחזר משפך: נרשמו · השלימו · נעצרו באמצע · נרשמו ב-30 הימים האחרונים. **ועד "השלים"** = יש לו כבר גן (`Group`) — כלומר סיים את אשף ההרשמה. **ספק "השלים"** = הכרטיס שלו מוכן להצגה לוועדים: וואטסאפ + לפחות מוצר אחד + לפחות תמונה אחת + אמצעי תשלום. כל הספירות ב-`CountAsync` במסד — לא נטענות ישויות. נוסף `Vendor.CreatedAt` (**nullable**) + Migration `AddVendorCreatedAt`, ונחתם בשתי נקודות יצירת הספק. **לקוח** — `usageStatsService` (`getUsageStats` + `funnelPercent`), מסך `/admin/usage` (`UsageStatsPage`) ורכיב `UsageFunnel` גנרי (אריחים + בר התקדמות + שורות הסבר), עם כניסה בתפריט הצד תחת "ניהול VaddyGo" — מוצגת רק ל-SuperAdmin.
- **למה:** בקשת בעלת המוצר (08.08.2026, שלב 12) — לדעת כמה נרשמו וכמה נעצרו באמצע, בשני הצדדים, כדי לזהות איפה אנשים נוטשים.
- **קבצים:** backend חדשים — `Controllers/AdminController.cs`, `Services/IUsageStatsService.cs`, `Services/UsageStatsService.cs`, `DTOs/UsageStatsDto.cs`, `Migrations/*AddVendorCreatedAt.*`; שונו — `Models/Vendor.cs`, `Services/VendorService.cs`, `Program.cs` (רישום DI). frontend חדשים — `src/services/usageStatsService.js`, `src/pages/UsageStatsPage.js`(+טסט), `src/pages/admin/UsageFunnel.js`; שונו — `src/App.js` (נתיב), `src/components/SideMenu.js` (כניסת מנהלת).
- **החלטות:** (1) **`Vendor.CreatedAt` הוגדר nullable** — לספקים שנוצרו לפני השדה אין תאריך אמיתי, והם פשוט לא נספרים בקצב ההצטרפות; עדיף "לא ידוע" מתאריך מומצא (המיגרציה מוסיפה עמודה ריקה בלבד — אפס סיכון לנתונים קיימים). (2) **אין נפילה מקומית** בשירות הזה (בניגוד לשאר השירותים בפרויקט) — נתוני שימוש חייבים להיות אמיתיים; מספר שנבנה בדפדפן היה מטעה בהחלטה עסקית. (3) הגנה **בשתי שכבות**: העמוד חוסם מי שאינה SuperAdmin ואף לא פונה לשרת, והשרת אוכף שוב ברמת ה-Role. (4) הנתונים **מצטברים בלבד** — בלי שמות משתמשות, מיילים או פרטי ילדים.
- **אימות:** `dotnet build` 0 שגיאות; סמוק-טסט מול שרת רץ: `/api/admin/usage` החזיר **401** בלי טוקן, **403** לחבר ועד רגיל, ו-**200** למנהלת עם המספרים הנכונים (`registered:1, completed:0, stopped:1`). `npm test` — 253/253 (67 suites, 3 בדיקות חדשות); `npm run build` — Compiled successfully. הבדיקה החדשה גם חשפה ותיקנה פנייה מיותרת לשרת ממי שאינה מנהלת.
- **הצעד הבא:** המשימה האחרונה בשלב 12 — סקרים אמיתיים: ההורים עונים דרך קישור ציבורי והתוצאות מתעדכנות באפליקציה (דורש מודל `Poll` בשרת + נקודות קצה ציבוריות).

## 09.08.2026 — שלב 12 (ג'): דוח ספק למנהלת — התקדמות, סטטיסטיקות ושיתוף [Claude Opus]

- **מה נעשה:** למנהלת (SuperAdmin) נוסף כפתור 📊 בכל שורת ספק במסך המתנות, שפותח **דוח ספק** (`VendorReportModal`): בר התקדמות "השלמת הכרטיס X מתוך 5" באחוזים, רשימת הפריטים עם ✓/⏱ (מה הושלם ומה לא), הסטטיסטיקות הקיימות (צפיות · פניות · מוצרים + עוגת מוצרים לפי קטגוריה), וכפתורי **שיתוף**: וואטסאפ ישיר לספק עם הדוח כטקסט מוכן, או העתקה ללוח. נוסף מודול `services/vendorProgress.js` — `vendorChecklist` / `vendorProgress` / `vendorReportText` — וה-`SupplierChecklist` של הספק חובר אליו במקום להחזיק עותק משלו של הכללים. נוסף אייקון-קו `chart` ל-`Icon.js`.
- **למה:** בקשת בעלת המוצר (08.08.2026, שלב 12). לספקים כבר היה דוח בפורטל שלהם, אבל המנהלת — שמגייסת אותם ומלווה אותם — לא יכלה לראות איפה כל ספק עומד ולא היה לה מה לשלוח לו כדי שישלים את הכרטיס.
- **קבצים:** חדשים — `src/services/vendorProgress.js`(+טסט), `src/pages/gifts/VendorReportModal.js`(+טסט). שונו — `src/pages/GiftsPage.js` (כפתור הדוח + החלון), `src/components/SupplierChecklist.js` (עבר למקור האמת המשותף), `src/components/Icon.js` (אייקון `chart`).
- **החלטות:** (1) **מקור אמת אחד להתקדמות** — הצ'ק-ליסט של הספק והדוח של המנהלת נגזרים מאותה פונקציה; אחרת שני הצדדים היו מציגים מספרים שונים ברגע שמישהו משנה קריטריון. (2) הדוח **לא ממציא נתונים חדשים** — הוא מרכיב מחדש את מה שכבר קיים (`views`/`leads`/`products`), ולכן לא נדרש שינוי שרת. (3) שיתוף = טקסט (וואטסאפ/העתקה) ולא קובץ — זה מה שבפועל נשלח לספקים, ואין תלות בספריית PDF. (4) הכפתור מוצג רק ל-SuperAdmin, כמו שאר ניהול הספקים; לוועד רגיל אין גישה.
- **אימות:** `npm test` — 250/250 עוברים (66 suites, 7 בדיקות חדשות); `npm run build` — Compiled successfully.
- **הצעד הבא:** המשך שלב 12 — מסך נתוני שימוש למנהלת, ואז סקרים אמיתיים בקישור ציבורי (המשימה הגדולה, דורשת שרת).

## 09.08.2026 — שלב 12 (ב'): שם מוצר אינו חובה + חלון "נשמר בהצלחה" [Claude Opus]

- **מה נעשה:** עד היום מוצר בלי שם **נמחק בשקט** בשמירה — הלקוח סינן אותו (`filter(p => p.name.trim())`) והשרת דחה אותו (`[Required]`). ספק שצילם מוצרים בטלפון וניסה לשמור לפני שנתן שמות איבד את העבודה. עכשיו: (1) **בשרת** הוסר `[Required]` מ-`VendorProductDto.Name` (מגבלת 80 תווים נשארה); לא נדרשה מיגרציה — העמודה כבר קיימת וברירת המחדל היא מחרוזת ריקה. (2) **בלקוח** נוסף מודול-אמת יחיד `services/vendorProducts.js` עם `productDisplayName` (שם, ואם אין — "מוצר N"), `withDisplayNames` (ממספר לפי הרשימה **המלאה** של הספק, כדי שהמספור לא יתחיל מחדש בכל תיקייה) ו-`hasProductContent` (מה שווה לשמור). כל המסכים שמציגים מוצרים עברו להשתמש בו: טופס העריכה, כרטיס הספק לוועד, הקטלוג הציבורי, פורטל הספק ודוחות הספק — שם המוצרים חסרי-השם היו **נעלמים מהתצוגה** לגמרי. (3) **חלון אישור:** נוסף רכיב גנרי `SuccessDialog` (Modal + הודעה + "הבנתי"), ואחרי שמירה מוצלחת בטופס הספק הוא נפתח עם "השינויים נשמרו ✓".
- **למה:** בקשת בעלת המוצר (08.08.2026, שלב 12). איבוד עבודה שקט הוא הכשל החמור מבין השניים; חלון האישור נוסף כי הודעת ה-toast הקיימת חולפת מהר ולא נקלטה.
- **קבצים:** backend — `DTOs/VendorDtos.cs`. חדשים — `src/services/vendorProducts.js`(+טסט), `src/components/SuccessDialog.js`(+טסט), `src/pages/gifts/VendorForm.test.js`. שונו — `src/pages/gifts/VendorForm.js`, `src/pages/gifts/VendorPanel.js`, `src/pages/CatalogPage.js`, `src/components/SupplierHome.js`, `src/components/SupplierReports.js`, `src/components/SupplierChecklist.js`.
- **החלטות:** (1) **שורה ריקה לגמרי לא נשמרת** — "הוספת מוצר" שלא מולא כלל לא ייצור רשומת רפאים; מספיק שדה אחד (תמונה/מחיר/תיאור/תיקייה) כדי שהמוצר יישמר בלי שם. (2) המספור "מוצר N" הוא **תצוגה בלבד**, נגזר מהסדר ולא נשמר בשרת — כך אין שמות מזויפים במסד. (3) `SuccessDialog` נבנה כרכיב גנרי ב-`components/` (ולא הודעה מקומית) כדי שכל טופס עתידי יוכל להשתמש בו; כפתורו נקרא "הבנתי" ולא "סגירה", כדי לא להתנגש בשם הנגיש של כפתור ה-✕ של `Modal`. (4) הצ'ק-ליסט של הספק ("מוצר ראשון") נמדד מעכשיו לפי קיום מוצר, לא לפי קיום שם.
- **אימות:** `npm test` — 243/243 עוברים (64 suites, 8 בדיקות חדשות); `npm run build` — Compiled successfully; `dotnet build` — 0 שגיאות.
- **הצעד הבא:** המשך שלב 12 — דוחות ספקים למנהלת, מסך נתוני שימוש, וסקרים אמיתיים בקישור ציבורי.

## 09.08.2026 — שלב 12 (א'): סינון ספקים לפי מיקום, הסרת "צילום קטלוג" מהפורטל, נוסח "עדכון יתרה" [Claude Opus]

- **מה נעשה:** שלוש מבקשות בעלת המוצר מ-08.08. (1) **סינון ספקים לפי קטגוריה ומיקום** — נבנה רכיב גנרי חדש `FilterChips` (שורת צ'יפים לסינון רשימה לפי ערך יחיד: כפתור "הכל" + כפתור לכל אפשרות, `aria-pressed` לנגישות, `baseClass` כדי לרכוב על עיצוב קיים בלי לשכפל CSS). `GiftsPage` עבר להשתמש בו פעמיים — קטגוריה ומיקום — ושני הסינונים פועלים **יחד**; כשאין ספק שעונה לשניהם מוצג מצב ריק ("אין ספקים שמתאימים לסינון"). מוצגות רק קטגוריות/ערים שקיימות בפועל אצל הספקים. (2) **"צילום קטלוג" הוסר מפורטל הספק** — מוצג מעתה רק בעריכת המנהלת (`!showFab`), כי בפורטל הספק כבר קיים "הוספת מוצר ▾ → צילום מוצר". (3) **כותרת חלון עדכון היתרה** שונתה ל"כדאי לעדכן כדי שנמשיך לנהל נכון".
- **למה:** מקבץ בקשות ישיר מבעלת המוצר (שלב 12 ב-ROADMAP). הסינון הכפול נדרש כי רשימת הספקים גדלה וקטגוריה לבדה לא מספיקה כדי למצוא ספק רלוונטי באזור.
- **קבצים:** חדשים — `src/components/FilterChips.js` + `FilterChips.test.js`. שונו — `src/pages/GiftsPage.js`, `src/pages/gifts/VendorForm.js`, `src/pages/home/ExpenseModal.js`, `src/styles/gifts.css` (תווית שורת סינון), `src/pages/gifts/GiftsPage.test.js`.
- **החלטות:** הסינון נבנה כרכיב **גנרי ב-`components/`** ולא כקוד מקומי במסך המתנות — כדי שכל מסך עתידי שצריך סינון (תלמידים, הוצאות, סקרים) ישתמש בו במקום לשכפל. הסינון נשאר **בצד הלקוח** על הרשימה שכבר נטענה — אין קריאת API נוספת ואין שינוי שרת. הכפתור "צילום קטלוג" לא נמחק מהקוד אלא הוסתר בפורטל בלבד, כדי לא לשבור את זרימת המנהלת.
- **אימות:** `npm test` — 235/235 עוברים (61 suites, 6 בדיקות חדשות); `npm run build` — Compiled successfully.
- **הצעד הבא:** המשך שלב 12 — שמירת מוצר ללא שם + חלון "נשמר בהצלחה", ואז דוחות ספקים למנהלת, מסך נתוני שימוש, וסקרים אמיתיים בקישור ציבורי.

## 03.08.2026 — מסלול פרו לוועד: תשתית + כתרי-פרו + עמוד שדרוג (שלב א') [Claude Opus]

- **מה נעשה:** נבנתה ההבחנה **חינם ↔ פרו** לצד הוועד, כפי שאושר במסמך התמחור. `services/plan.js` מרכז את ההיגיון: `isPro()`, רשימת `PRO_FEATURES`, ומתג-אב **`PRO_ENFORCED = false`** — כך ש**שום פיצ'ר לא נחסם** (החלטת בעלת המוצר: גישה שלבית, בלי paywall בשלב זה). נוסף רכיב `ProBadge` (תגית "פרו" עם כתר זהב) + אייקון-קו `crown` ל-`Icon.js`, והוצב ליד פיצ'רי הפרו במסכים האמיתיים: **עוזרת AI, תזכורת גורפת לחייבים, בקשת תשלום גורפת, ניהול צוות והרשאות, ניהול קבלות, וניהול כמה מוסדות** (בתפריט הצד). נבנה `pages/UpgradePage.js` (נתיב `/upgrade`, ₪149/שנה, רשימת ההטבות ממקור-אמת יחיד `PRO_FEATURES`), עם כניסת "שדרוג לפרו" בתפריט הצד (מוסתרת ממנוי פרו).
- **למה:** בקשת בעלת המוצר לאחר מחקר השוק — קיים מתחרה חינמי ישיר, ולכן הליבה נשארת חינם וה"פרו" הוא שכבת ערך מוסף (אוטומציות/AI/קנה-מידה). בשלב זה רק **מציגים** מה יהיה פרו (כתר), בלי לחסום — כדי לא לנעול משתמשים לפני שיהיה מנגנון תשלום.
- **קבצים:** חדשים — `services/plan.js`(+טסט), `components/ProBadge.js`(+טסט), `styles/pro.css`, `pages/UpgradePage.js`(+טסט). שונו — `components/Icon.js` (crown), `App.js` (route /upgrade), `components/SideMenu.js` (כניסת שדרוג + כתר), ו-5 מסכים שקיבלו כתר (`AiAssistantPage`, `BulkReminderButton`, `BulkPaymentRequestButton`, `TeamManager`, `files/ReceiptsCard`).
- **החלטות:** (1) בלי אכיפה בשלב א' — `PRO_ENFORCED=false`; להפעלה עתידית מדליקים את המתג ו-`isFeatureLocked` יתחיל לחסום. (2) `ProBadge` דקורטיבי (`aria-hidden`) כדי לא ללכלך שמות נגישים. (3) הבחנת פרו לפי שדה `plan` על המשתמש (ברירת מחדל: free). ⚠️ **ריצה מקבילה:** במהלך העבודה רץ סוכן שני (LandingPage) על אותו worktree/ריפו — קרתה תאונת index משותף והקבצים נכנסו ל-commit שלו; ההשלמה (route+תפריט) בוצעה לאחר שהסוכן נעצר.
- **אימות:** `npm test` — 211/211 עוברים (50 suites); `npm run build` — Compiled successfully. נדחף ל-main (`a85c32e`).
- **הצעד הבא:** כשתהיה החלטה + מנגנון תשלום/הענקת פרו — להדליק `PRO_ENFORCED`, לחווט את שדה `plan` מהשרת (כרגע לקוח בלבד), ולהוסיף אכיפת פרו גם בשרת. אופציונלי: העברת "תזכורת גורפת"/"עוזרת תקציב" לחינם אם רוצים יותר ויראליות.

## 21.07.2026 — אבטחה: fail-closed על מפתח ה-JWT בייצור [Claude Opus]

- **מה נעשה:** מפתח-הפיתוח החלש הפך לקבוע נקוב `JwtSettings.DevKey`, ונוסף ב-`Program.cs` שער **fail-closed**: כשהסביבה אינה Development והמפתח `Jwt:Key` חסר **או** שווה למפתח-הפיתוח הציבורי — השרת **זורק חריגה ולא עולה**. כך אי אפשר לפרוס בטעות ייצור עם מפתח שאפשר לזייף איתו טוקנים (המפתח הציבורי כתוב בריפו).
- **למה:** ביקורת אבטחה (בקשת בעלת המוצר). בעלת המוצר אימתה שב-Railway מוגדר `Jwt__Key` אקראי חזק (לא מפתח-הפיתוח), ולכן השער בטוח ולא מפיל את הייצור הקיים.
- **קבצים:** `backend/Services/JwtTokenService.cs`, `backend/Program.cs`.
- **אימות:** `dotnet build` 0/0; סמוק-טסט — הרצה בייצור **בלי** `Jwt__Key` זרקה `InvalidOperationException` ולא הגיעה ל-"Now listening" (בפיתוח ועם מפתח — עולה כרגיל).
- **הצעד הבא:** מעבר SQLite→PostgreSQL (שלב 11) — כולל הקמת שירות Postgres ב-Railway והעברת הנתונים הקיימים; לתכנן בזהירות.

## 21.07.2026 — אבטחה: הגבלת ניסיונות לקוד איפוס הסיסמה (anti brute-force) [Claude Opus]

- **מה נעשה:** נוסף `User.ResetCodeAttempts` (+ מיגרציה `AddResetCodeAttempts`). ב-`AuthService.ResetPasswordAsync` — כל קוד שגוי סופר ניסיון, ואחרי **5 ניסיונות שגויים הקוד מבוטל** (נמחק, צריך לבקש חדש). הפקת קוד חדש ואיפוס מוצלח מאפסים את המונה. כך ניחוש גס של הקוד בן 6 הספרות מוגבל ל-5 ניסיונות = 5 למיליון (במקום מיליון אפשרויות פתוחות).
- **למה:** ביקורת אבטחה (בקשת בעלת המוצר) — הקוד היה חשוף לניחוש גס בלי תקרת ניסיונות.
- **קבצים:** `backend/Models/User.cs`, `backend/Services/AuthService.cs`, `backend/Migrations/*AddResetCodeAttempts.*`.
- **אימות:** `dotnet build` 0/0, המיגרציה נקייה (עמודת int); סמוק-טסט: 6 ניסיונות שגויים → 400 בלי קריסה, והלוג הראה `Reset code invalidated after too many attempts` אחרי ה-5.
- **⚠️ הצעד הבא (קריטי לבעלת המוצר):** לוודא ש-`Jwt__Key` מוגדר ב-Railway (שירות `soothing-clarity`) כמחרוזת אקראית ארוכה — אחרת `JwtSettings.GetKey` נופל למפתח-פיתוח **ציבורי** שבריפו, וכל מי שרואה את הריפו יכול לזייף טוקנים. הבא בתור: מעבר ל-SQL Server (שלב 11).

## 21.07.2026 — מייל RTL + תיקון: תשלומי אשראי לא נספרו בבקשת התשלום [Claude Opus]

- **מה נעשה:** (1) **מייל האיפוס מימין-לשמאל** — `ResendEmailSender` שולח עכשיו גם `html` עטוף ב-`<div dir="rtl">` (לא רק טקסט פשוט), כך שהעברית מיושרת נכון בכל תוכנת מייל. (2) **תיקון באג חישוב:** תשלום ב**אשראי** (`CardAmount`, נשמר ע"י `CardPaymentService`) **לא נספר** בבקשת התשלום — `PaymentResponseDto` לא החזיר אותו ו-`amountPaidSoFar` בפרונט לא כלל אותו. התוצאה: הורה ששילם באשראי נראה כמי שלא שילם, והחוב חושב שגוי. נוסף `CardAmount` ל-DTO ול-`PaymentService` (ToResponse+Default), ונכלל ב-`amountPaidSoFar` וב-`total` של `PaymentRow`.
- **למה:** בקשות בעלת המוצר — כיוון עברי במייל, ותלונה שבקשת התשלום "לא מחשבת נכון את הכסף".
- **קבצים:** backend: `DTOs/PaymentResponseDto.cs`, `Services/PaymentService.cs`, `Services/ResendEmailSender.cs`. frontend: `services/paymentsService.js`, `pages/payments/PaymentRow.js`.
- **אימות:** `dotnet build` 0/0, `npm run build` ירוק, 25 טסטי תשלומים עוברים. (Dashboard/StudentService כבר סופרים אשראי — רק ה-DTO והפרונט פספסו.)
- **הצעד הבא:** לוודא מול בעלת המוצר שהתלונה נפתרה; אם ה"שולם" השגוי נמשך בתרחיש שאינו אשראי — לבדוק את סכום היעד של הקטגוריה (amount).

## 21.07.2026 — שליחת מייל האיפוס: מעבר מ-Brevo ל-Resend [Claude Opus]

- **מה נעשה:** החלפת שולח המייל מ-Brevo ל-**Resend** (HTTP API, `https://api.resend.com/emails`, Bearer auth). נוסף `ResendEmailSender` (typed `HttpClient`), הרישום ב-`Program.cs` הוחלף, `appsettings.json` — סקשן `Brevo` הוחלף ב-`Resend` (`ApiKey`, `Sender`), ו-`BrevoEmailSender.cs` נמחק. ברירת מחדל לשולח: `onboarding@resend.dev` (עובד בלי דומיין).
- **למה:** חשבון Brevo החדש של בעלת המוצר היה "תחת בדיקה" וחסם יצירת מפתח API. Resend מאפשר יצירת מפתח מיד. (SMTP רגיל לא אופציה — Railway חוסמת אותו.)
- **קבצים:** backend: `Services/ResendEmailSender.cs` (חדש), `Services/BrevoEmailSender.cs` (נמחק), `Program.cs`, `appsettings.json`.
- **אימות:** `dotnet build` 0/0; סמוק-טסט (רישום → `forgot-password`) חזר HTTP 200 ב-0.9 שניות, והלוג הראה POST ל-`api.resend.com/emails` שקיבל 401 עם מפתח מזויף (נתיב ה-HTTPS מוכח).
- **⚠️ הצעד הבא (דרוש מבעלת המוצר):** ב-Railway → **soothing-clarity** → Variables: `Resend__ApiKey` = מפתח ה-API מ-resend.com. **מגבלה:** בלי דומיין מאומת, Resend שולח רק לכתובת בעל החשבון (`avivitm91@gmail.com`) — מספיק לאיפוס של בעלת המוצר עצמה. לשליחה לחברי ועד אחרים: לאמת דומיין ב-Resend ולעדכן `Resend__Sender`.

## 20.07.2026 — שליחת מייל האיפוס: מעבר מ-SMTP ל-Brevo (HTTPS) [Claude Opus]

- **מה נעשה:** החלפת מנגנון שליחת המייל מ-SMTP (Gmail) ל-**HTTP API של Brevo** (`https://api.brevo.com/v3/smtp/email`). נוסף `BrevoEmailSender` (typed `HttpClient`), הרישום ב-`Program.cs` הוחלף (`AddHttpClient<IEmailSender, BrevoEmailSender>`), `appsettings.json` — סקשן `Smtp` הוחלף ב-`Brevo` (`ApiKey`, `Sender`), ו-`SmtpEmailSender.cs` נמחק.
- **למה:** אצל בעלת המוצר המייל לא נשלח וה-UI נתקע על "רק רגע". האבחון: **Railway חוסמת שליחת SMTP** (פורט 587), ולכן `SmtpClient` נתלה עד timeout (~100 שניות). Brevo שולח דרך HTTPS (443) ש-Railway לא חוסמת.
- **קבצים:** backend: `Services/BrevoEmailSender.cs` (חדש), `Services/SmtpEmailSender.cs` (נמחק), `Program.cs`, `appsettings.json`.
- **אימות:** `dotnet build` 0/0; סמוק-טסט מלא (רישום משתמש → `forgot-password`) — חזר **HTTP 200 ב-0.82 שניות** (במקום להיתקע), והלוג הראה שהקריאה הגיעה בפועל ל-Brevo וקיבלה 401 עם מפתח מזויף (נתיב ה-HTTPS מוכח). עם מפתח אמיתי + Sender מאומת → 201 ומייל נשלח.
- **⚠️ הצעד הבא (דרוש מבעלת המוצר):** ב-Railway → שירות **soothing-clarity** (ה-backend!) → Variables: `Brevo__ApiKey` = מפתח ה-API של Brevo, `Brevo__Sender` = `avivitm91@gmail.com` (כתובת השולח המאומתת ב-Brevo). אפשר למחוק את משתני `Smtp__*` הישנים.

## 20.07.2026 — איפוס סיסמה עצמאי בקוד למייל (frontend + backend) [Claude Opus]

- **מה נעשה:** זרימת איפוס סיסמה מלאה בשני שלבים. **Frontend:** `ForgotPasswordPage` (שלב 1 — בקשת קוד למייל; שלב 2 — הזנת קוד + סיסמה חדשה), `authService.requestPasswordReset`/`resetPassword`, נתיב `/forgot-password` (ציבורי), והקישור "שכחת סיסמה?" במסך הכניסה הוחלף מפניית וואטסאפ ידנית למסך החדש. **Backend:** שדות `ResetCodeHash`+`ResetCodeExpiresAt` ב-`User` + מיגרציה `AddPasswordResetCode`; שירות `IEmailSender`/`SmtpEmailSender` (SMTP/Gmail); `AuthService.RequestPasswordResetAsync`/`ResetPasswordAsync` (קוד 6 ספרות קריפטוגרפי, מגובב PBKDF2, תוקף 15 דק', חד-פעמי); ונקודות קצה `POST /api/auth/forgot-password` + `/api/auth/reset-password`.
- **למה:** בקשת בעלת המוצר לאיפוס אוטומטי (במקום וואטסאפ ידני לתמיכה). בחירתה: שליחה במייל דרך Gmail SMTP.
- **קבצים:** frontend: `src/pages/ForgotPasswordPage.js`(+טסט), `src/services/authService.js`, `src/App.js`, `src/pages/LoginPage.js`. backend: `Models/User.cs`, `DTOs/PasswordResetDtos.cs`, `Services/{IEmailSender,SmtpEmailSender,IAuthService,AuthService}.cs`, `Controllers/AuthController.cs`, `Program.cs`, `appsettings.json`, `Migrations/20260720125513_AddPasswordResetCode.*`.
- **החלטות/אבטחה:** לא חושפים אם המייל קיים (`forgot-password` תמיד 200); הקוד נשמר **מגובב בלבד**, חד-פעמי, נמחק אחרי שימוש מוצלח; אם SMTP לא מוגדר — לא שולחים אבל לא קורסים (נרשם ללוג). נעילת backend נתפסה ושוחררה לפי הפרוטוקול. אימות: `dotnet build` 0/0, המיגרציה הוחלה, סמוק-טסט של ה-endpoints עבר (200/400), ‏`npm run build` + טסטים ירוקים.
- **⚠️ הצעד הבא (דרוש מבעלת המוצר):** להגדיר ב-Railway (שירות ה-backend) משתני סביבה: `Smtp__Host=smtp.gmail.com`, `Smtp__Port=587`, `Smtp__User=<כתובת Gmail>`, `Smtp__Password=<App Password של Gmail>`, `Smtp__From=<כתובת Gmail>`. עד אז הזרימה עובדת אך המייל לא נשלח בפועל.

## 19.07.2026 — UX: קיצור אשף ההרשמה, פופאפ פתיחה, ואישור לכל מחיקה [Claude Opus]

- **מה נעשה:** (1) **אשף ההרשמה קוצר מ-5 ל-4 צעדים** — שאלת "כמה ועדים" הועברה למסך ניהול ההרשאות (`TeamSetupPage`), שם היא נשאלת רק בהקמה הראשונה ויוצרת מוסדות נוספים דרך `addInstitution`. `CommitteesStep.js` נמחק. (2) בצעד הגבייה נוסף כפתור עדין **"אכניס את הפרטים מאוחר יותר"** + טקסט מרגיע. (3) **מסך הפתיחה נוקה** ללוגו + 2 כפתורים; הטקסט הרגשי ("אנחנו יודעים כמה שעות...") עבר **לפופאפ הכניסה** (`WelcomePopup`). (4) במסך ההרשאות נוסף כפתור **"אעשה זאת מאוחר יותר"** לצד "כניסה לאפליקציה". (5) **אישור מחיקה (`ConfirmDialog`) נוסף ב-4 מקומות שחסרו:** הסרת חבר צוות, הסרת קטגוריית גבייה, ומחיקת הוצאה (`ExpensesList` + `ExpenseModal`).
- **למה:** בקשות ישירות של בעלת המוצר — שהכניסה לא "תחפור" בשאלות, ושכל מחיקה תבקש אישור.
- **קבצים:** `src/pages/onboarding/{OnboardingWizard,CollectionStep}.js` (+מחיקת `CommitteesStep.js`), `src/pages/TeamSetupPage.js`, `src/pages/WelcomePage.js`, `src/components/WelcomePopup.js`, `src/pages/CollectionSettingsPage.js`, `src/pages/home/{ExpensesList,ExpenseModal}.js`, `src/styles/{onboarding,team}.css`, וטסטים: `src/pages/onboarding/Onboarding.test.js`, `src/App.test.js`.
- **החלטות:** הטקסט הרגשי ירד ממסך הפתיחה לפי בחירת בעלת המוצר (רק בפופאפ בכניסה). הסרת שורת מוצר/רשת בטופס ספק נשארה בלי אישור (עריכת טופס זמנית שאינה נשמרת עד שמירה). הטסטים עודכנו (5→4 צעדים; מסך פתיחה בלי הטקסט). `npm run build` + הטסטים הרלוונטיים ירוקים.
- **הצעד הבא:** מערכת הודעות "נשמר ✓ / שגיאה" בכל שמירה, מסך "המערכת חושבת... הנתונים שמורים בבטחה", כפתור AI ליד הפעמון + הגדלת וואטסאפ, ועדכוני עיצוב (כפתורים משניים, כותרות שחורות, קונטרסט). איפוס סיסמה אוטומטי בסוף (דורש נעילת backend).

## 13.07.2026 — ⚠️ החלטת עיצוב סופית של בעלת המוצר: פלטת ורוד (לא חום, לא סגול) [Claude Fable]

- **מה נעשה:** בעלת המוצר הנחתה ישירות (בשיחה): הפלטה היא **ורוד חי ונעים ומואר** — **בלי חום ובלי סגול**. הוסר הלב הסגול 💜 מהמותג בכל המקומות (BrandName). הפלטה: primary `#e05088`, primary-light = **`#f9dae9`** (הגוון שבעלת המוצר ביקשה במפורש, לרקעים/כותרת/תגים), רקע `#fff6fa`, טקסט `#3b2b33`. הצבעים הייעודיים (ביט/פייבוקס/מזומן/סטטוסים/אקסנט) נשמרו. הוסף כלל מפורש בראש `theme.css`.
- **⚠️ חשוב לכל הסוכנים:** קומיט `5e5bdfa "restore the warm brown/cream palette"` **החזיר את החום בטעות** — זה סתר את הנחיית בעלת המוצר. **הפלטה הנכונה היא ורוד. אין להחזיר לחום/סגול בלי אישור מפורש ובכתב של בעלת המוצר.**
- **קבצים:** src/styles/theme.css (+ index.html/manifest שכבר היו ורודים; BrandName ללא לב).
- **הצעד המומלץ הבא:** להחיל את צבעי אמצעי התשלום (ביט טורקיז / פייבוקס כחול / מזומן) על תגי/כפתורי התשלום — הצבעים כבר מוגדרים כמשתנים.

## 12.07.2026 — לוח שנה: 4 שיפורים לפי בעלת המוצר [Claude Fable]

- **מה נעשה:** (1) **חצי הניווט** תוקנו — היו `‹`/`›` (תווים שמתהפכים ב-RTL וגרמו לחצים להצביע הפוך); הוחלפו ל-`←`/`→` (לא מתהפכים): הקודם מצביע ימינה, הבא שמאלה. (2) **נגישות בנייד** — כל תא-יום בלוח הוא כעת כפתור; לחיצה על יום פותחת הוספת אירוע עם התאריך מוכן (במקום לבחור ידנית ב-date picker של אנדרואיד). (3) **עריכת אירועים** — נוסף `updateEvent` ל-eventsService, ‏EventForm תומך במצב עריכה (initialEvent, כפתור "עדכון האירוע"), וכפתור עריכה ✏️ ליד כל אירוע ברשימה. (4) **תאריכים עבריים** — מוצג התאריך העברי (גימטריה, מ-Intl) לצד כל יום בלוח וברשימת האירועים. 12 טסטי לוח שנה עוברים (2 חדשים: לחיצה-על-יום, עריכה), סה"כ 94, build ירוק.
- **למה:** בקשות ישירות של בעלת המוצר לשיפור נראות ושימושיות לוח השנה בנייד.
- **קבצים:** src/pages/CalendarPage.js, src/pages/calendar/{MonthGrid,EventForm,CalendarPage.test}.js, src/services/eventsService.js, src/styles/calendar.css.
- **החלטות:** לחיצה על יום = הוספת אירוע (עריכה נעשית מהרשימה, לא מהלוח, כדי לא לקנן כפתורים). התאריך העברי מחושב מהלוח העברי המובנה בדפדפן (Intl) — לא מומצא. `updateEvent` עם fallback ל-localStorage כמו שאר הפעולות (עובד גם בלי שרת).
- **הצעד המומלץ הבא:** ⚠️ עדיין פתוח בעדיפות עליונה: תיקון בידוד הנתונים (SECURITY_REMEDIATION.md) — ממתין לשחרור נעילת ה-backend (סוכן אחר מתקן דליפה כרגע).

## 12.07.2026 — תפריט צד: "הוסף מוסד" + תיקון הסתרת התנתקות [Claude Fable]

- **מה נעשה:** (1) נוסף כפתור **"➕ הוסף מוסד"** בתפריט הצד (מתחת לרשימת המוסדות) — פותח מודאל לשם המוסד, יוצר מוסד חדש לא-מופעל (`institutionsService.addInstitution`), וממשיך למסך ההפעלה/רכישה שלו (זרימת ההפעלה הקיימת). (2) תוקן העיצוב: "צור קשר" וההתנתקות היו נדחפים לתחתית ומוסתרים מאחורי הניווט התחתון — הועלו למעלה (`margin-top:auto`→`16px`) והתפריט הועלה מעל הניווט (z-index 60). טסט חדש ל-`addInstitution`; 7 טסטי מוסדות/תפריט עוברים, build ירוק.
- **למה:** בעלת המוצר ביקשה אפשרות להוסיף עוד מוסד מהתפריט, ושכפתור ההתנתקות לא יהיה מוסתר.
- **קבצים:** `src/services/institutionsService.js` (+`addInstitution`) + טסט, `src/components/SideMenu.js`, `src/styles/sidemenu.css`.
- **החלטות:** מוסד חדש נוצר במצב לא-מופעל ("🔒 להפעלה") ומשתלב בזרימת ההפעלה הקיימת של הסוכן השני (multi-institution) — לא שיניתי את מנגנון ההפעלה, רק הוספתי כניסה אליו. הפרדת הנתונים פר-מוסד בשרת ממשיכה אצל הסוכן השני.
- **הצעד המומלץ הבא:** כשעבודת ה-multi-institution תושלם, לוודא שהוספת מוסד + הפעלה יוצרת Group נפרד בשרת ומסננת נתונים כראוי.

## 12.07.2026 — כניסת Google = חשבונות קיימים בלבד (החלטת בעלת המוצר) [Claude Fable]

- **מה נעשה:** לפי בקשת בעלת המוצר — כניסת Google **לא יוצרת יותר חשבון חדש**, אלא רק מתחברת לחשבון קיים (שנרכש ונרשם בשם משתמש+סיסמה). `AuthService.LoginWithGoogleAsync` שונה: מאתר לפי GoogleId/מייל; אם אין → מחזיר שגיאה ידידותית "לא נמצא חשבון... יש להירשם תחילה או להתחבר עם שם משתמש וסיסמה". חשבון קיים עם אותו מייל עדיין מקושר ל-GoogleId בכניסה ראשונה. הוסרה `GenerateUniqueUsernameAsync` (לא בשימוש). בלקוח (בקומיט קודם): כפתור Google הוסר ממסך ההרשמה, נשאר רק בכניסה. אומת: token מזויף→401 ידידותי; השרת רץ ותואם למיגרציה `ScopeEntitiesToGroup` של הסוכן השני.
- **למה:** מודל עסקי — המערכת נמכרת בתשלום עם הרשמה; Google הוא רק אמצעי כניסה נוח לקיימים, לא דלת ליצירת חשבונות חינם.
- **קבצים:** `backend/Services/AuthService.cs` (+`ROADMAP.md`, `PROJECT_LOG.md`). (הלקוח — `RegisterPage.js` — בקומיט הקודם.)
- **החלטות:** בעלת המוצר אישרה שהמייל של חשבונה (avivitm91@gmail.com) זהה למייל ה-Google שלה — לכן כניסת Google תזהה את חשבונה לפי המייל ותיכנס אליו.
- **הצעד המומלץ הבא:** נושא נפרד שעלה — כניסה במכשיר חדש (מחשב) מציגה את אשף ההרשמה כי מצב ה-onboarding נשמר ב-localStorage פר-מכשיר, לא פר-חשבון בשרת. זה **בדיוק** מה שעבודת ה-multi-institution (Stage 2, סוכן אחר) פותרת — הפרדת נתונים פר-מוסד/חשבון בשרת (מיגרציית `ScopeEntitiesToGroup` כבר הוסיפה `GroupId` לישויות). לא לתקן במקביל כדי לא להתנגש. עד אז: להשתמש במכשיר שבו הוגדר הגן (הטלפון), ולא להשלים אשף שוב במכשיר אחר (למניעת גן כפול).


- **מה נעשה:** נבנתה כניסה/הרשמה עם Google מקצה לקצה. **שרת** (תחת נעילת backend): `POST /api/auth/google` — מאמת את ה-ID token מול גוגל (`Google.Apis.Auth`, בדיקת audience=Client ID, חתימה, תוקף) ומאתר/יוצר משתמש: משתמש חדש מקבל שם משתמש ייחודי נגזר מהמייל (בלי סיסמה) + תוקף מנוי לפי `SubscriptionPolicy`; חשבון מייל+סיסמה קיים עם אותו מייל — מקושר למזהה גוגל. `GoogleSettings` מחזיק את ה-Client ID (ברירת מחדל בקוד — ציבורי; ניתן לדריסה ב-`Google__ClientId`). **לקוח:** `GoogleSignInButton` (טוען Google Identity Services, מציג את הכפתור הרשמי, מעביר credential), `authService.loginWithGoogle`, שולב במסכי הכניסה וההרשמה עם מפריד "או". אומת מול שרת רץ: token מזויף→401 ידידותי בעברית, credential חסר→400. 83 טסטים עוברים (4 חדשים), build ירוק.
- **למה:** להשלים את שלב 10 (UI_SPEC ס' 2) — כניסה נוחה עם חשבון גוגל, לצד שם משתמש/סיסמה.
- **קבצים:** שרת: `backend/DTOs/GoogleLoginDto.cs`, `backend/Services/GoogleSettings.cs` (חדשים), `backend/Services/AuthService.cs` (+`LoginWithGoogleAsync`), `IAuthService.cs`, `backend/Controllers/AuthController.cs`, `backend/appsettings.json`, `.csproj` (חבילת Google.Apis.Auth). לקוח: `src/config/google.js`, `src/components/GoogleSignInButton.js` (חדשים), `src/services/authService.js`, `src/pages/LoginPage.js`, `src/pages/RegisterPage.js`, `src/styles/onboarding.css`, `src/pages/Auth.test.js`.
- **החלטות:** אימות ה-token בשרת דרך `GoogleJsonWebSignature.ValidateAsync` (בודק audience=Client ID) — אין צורך ב-Client Secret. ה-Client ID ציבורי, לכן ברירת מחדל בקוד (בלי צורך במשתנה סביבה נוסף אצל בעלת המוצר). כניסת Google **יוצרת חשבון אם אין** (find-or-create), כך שגם "כניסה עם Google" בהרשמה עובדת. **בעלת המוצר יצרה את ה-Client ID** ב-Google Cloud (פרויקט VaadyGo, Authorized JavaScript origin = כתובת הפרונט + localhost:3000).
- **הצעד המומלץ הבא:** בעלת המוצר תבדוק כניסת Google באתר החי אחרי הפריסה (כמה דקות). נותר בשלב 10: אכיפת תפקיד `SuperAdmin` על ניהול ספקים (שלב 7). שאר המערכת חיה ומאובטחת.

## 11.07.2026 — תקציבי החגים עברו לשרת (משותפים לכל חברות הוועד) [Claude Code B]

- **מה נעשה:** תקציבי החגים (שנקבעים בלוח השנה, שלב 6) הועברו מ-localStorage מקומי ל**שרת** — כמו קישורי התשלום — כדי שכל 2-3 חברות הוועד יראו את אותם תקציבים, וכדי שהעוזרת התקציבית במסך המתנות תשקף אותם. **שרת** (תחת BACKEND_LOCK): נוסף שדה `HolidayBudgetsJson` למודל `Group` (מילון "שם חג|שנה עברית → סכום" כ-JSON) + Migration‏ `AddGroupHolidayBudgets`, נחשף כ-`HolidayBudgets` (Dictionary) ב-`GroupResponseDto`, ונוסף `PUT /api/groups/{id}/holiday-budgets` (‏`GroupsController` → `IGroupService.UpdateHolidayBudgetsAsync`). אומת מקצה-לקצה מול שרת רץ (יצירת גן=ריק, PUT עם חנוכה/פורים, GET מחזיר את הסכומים). **לקוח:** `holidayBudgetsService` הפך לאסינכרוני מול השרת (לפי `groupId` מ-onboarding) עם **נפילה חיננית ל-localStorage**; הקומפוננטות (`CalendarPage`, `HolidayBudgetDialog`, `GiftsPage`/`BudgetAssistant`) כבר אסינכרוניות אז השינוי שקוף. 15 טסטים (לוח שנה+מתנות) עוברים + build ירוק.
- **למה:** לפי ה-README יש 2-3 חברות ועד לגן; תקציב שנשמר רק בדפדפן של אחת לא נראה לאחרות. שמירה ברמת הגן פותרת זאת ומחברת בין לוח השנה למסך המתנות בנתוני אמת.
- **קבצים:** שרת: `Models/Group.cs`, `DTOs/GroupResponseDto.cs`, `Services/{IGroupService,GroupService}.cs`, `Controllers/GroupsController.cs`, `Migrations/*AddGroupHolidayBudgets*` + snapshot. לקוח: `src/services/holidayBudgetsService.js`.
- **החלטות:** (1) עבודת השרת תחת **BACKEND_LOCK** (נתפס ב-80b1e7f, ישוחרר אחרי CI ירוק). (2) אחסון כ-JSON על ה-Group (ולא טבלה חדשה) — פשוט, additive, ומקביל לקישורי התשלום; המפתחות זהים ל-`holidayBudgetKey` בלקוח. (3) `Budget` הקיים הוא מודל תנועות כללי — לא מתאים, לכן לא נעשה בו שימוש. (4) נפילה מקומית נשמרת כמטמון.
- **הצעד המומלץ הבא:** לאחד את קריאות ה-Group בלקוח ל-`groupsService` יחיד (payment links + holiday budgets + categories שולפים את אותו גן) לצמצום קריאות; ובריבוי-מוסדות — לוודא ש-`groupId` הוא של המוסד הפעיל.

## 11.07.2026 — ריבוי מוסדות Stage 2: הפרדת תלמידים לפי מוסד (שרת + לקוח)

- **מה נעשה:** תחת נעילת backend, מומשה הפרדת הנתונים האמיתית — התלמידים מסוננים לפי המוסד הפעיל. **שרת:** `StudentService.GetAllAsync(int? groupId)` מסנן תלמידים לפי ה-Group; `CreateAsync(dto, groupId)` משייך תלמיד חדש למוסד; `StudentsController` קורא את המוסד הפעיל מכותרת **`X-Institution`** (מזהה ה-Group). Migration‏ `BackfillStudentGroup` משייכת תלמידים קיימים ללא שיוך למוסד הראשון. **לקוח:** `api.js` מצרף `X-Institution` לכל בקשה (מזהה ה-Group של המוסד הפעיל, מ-`institutionsService.getActiveServerGroupId`); `onboardingService.saveOnboarding` מחזיר את מזהה ה-Group, ו-`OnboardingWizard` שומר אותו כ-`serverGroupId` של המוסד. **אומת מקצה-לקצה מול שרת רץ:** תלמיד במוסד G1 + תלמיד ישן ללא שיוך שניהם מוצגים כש-G1 פעיל; במוסד אחר מוצג רק הישן (התלמיד של G1 מוסתר) — הפרדה עובדת ונתונים קיימים לא נעלמים. build (שרת+לקוח) ירוק; טסטי לקוח עוברים בבידוד.
- **למה:** זה מה שהופך את "מעבר בין מוסדות" (Stage 1) לאמיתי — כל מוסד רואה רק את התלמידים שלו.
- **קבצים:** שרת: `backend/Services/{IStudentService,StudentService}.cs`, `backend/Controllers/StudentsController.cs`, Migration `BackfillStudentGroup`. לקוח: `src/services/{api,institutionsService,onboardingService}.js`, `src/pages/onboarding/OnboardingWizard.js`.
- **החלטות/לקחים:** (1) **בטיחות נתונים חיים:** תלמידים ללא שיוך (GroupId==null) תמיד מוצגים, כדי שלא ייעלמו; מיגרציה משייכת אותם למוסד הראשון. משתמש עם מוסד אחד — התנהגות זהה להיום. (2) **תקלה חמורה שקרתה:** למרות שהחזקתי את נעילת ה-backend, סוכן אחר הריץ `git reset --hard` בתיקייה המשותפת ו**מחק את כל העבודה הלא-שמורה שלי** (שרת+לקוח). שוחזר מהזיכרון ונדחף מיד. **לקח: לדחוף (push) עבודת backend מוקדם ותכופות — הנעילה מגינה מפני עריכה, אך לא מפני reset הרסני.** (3) שיוך User→Group ואכיפת הרשאות פר-משתמש, והפרדת שאר הישויות (תשלומים/לוח שנה/מתנות/קבצים) — Stage 3.
- **הצעד הבא — Stage 3:** להרחיב את ההפרדה לשאר הישויות, ולשייך Group→User עם אכיפת הרשאה (שמשתמש יראה רק מוסדות שלו). דורש נעילת backend.

---

## 11.07.2026 — תפריט הצד: נשלף מימין מתחת לכותרת + "צור קשר" ו"התנתק" [Claude Code B]

- **מה נעשה (בקשת בעלת המוצר, צד-לקוח בלבד):** (1) **מיקום התפריט** — התפריט הנשלף (`SideMenu`) כיסה את הכותרת/לוגו; עכשיו נשלף מצד ימין **מתחת לכותרת** (הלוגו נשאר גלוי ובהיר), עם אנימציית כניסה מימין. כדי למקם מדויק, סודרה הכותרת (`.app-header`) לשורה אחת בגובה ידוע (flex, min-height 56px, הלוגו במרכז). (2) **"צור קשר"** — פריט חדש שפותח וואטסאפ למספר התמיכה 054-4579179 (מספר ציבורי, לא סוד) עם הודעת פתיחה מוכנה. (3) **"התנתק"** — נותב למסך הכניסה (`/login`) במקום `/welcome`, ושמו הובהר ל"התנתק". 2 טסטים חדשים + build ירוק.
- **למה:** בקשות ישירות של בעלת המוצר לשיפור הניווט והנגישות לתמיכה/יציאה.
- **קבצים:** `src/components/SideMenu.js` + טסט, `src/styles/sidemenu.css`, `src/styles/theme.css` (כותרת בשורה אחת). כולם צד-לקוח.
- **החלטות:** (1) **לא נגעתי ב-backend** — ה-BACKEND_LOCK תפוס ע"י סוכן אחר; כל השינויים ב-`src/`. (2) מספר התמיכה נשמר כקבוע בקוד (מספר ציבורי) לפי בחירת בעלת המוצר. (3) הכותרת סודרה ל-flex כדי לאפשר מיקום קבוע לתפריט מתחתיה. (4) שמרתי רק את 4 הקבצים שלי; לא נגעתי בעבודה לא-שמורה של אחרים.
- **הצעד המומלץ הבא:** אם ירצו — מספר תמיכה בהגדרה מרכזית, וקיצורים נוספים בתפריט.

## 11.07.2026 — מסך "תוקף המנוי הסתיים" (שלב 10, צד לקוח)

- **מה נעשה:** נוסף `SubscriptionExpiredPage` (נתיב ציבורי `/subscription-expired`) — מסך ידידותי כשמנוי פג. `LoginPage` מזהה את הודעת "המנוי פג" מהשרת ומנתב אליו במקום הודעת שגיאה גולמית. המסך מסביר שהמנוי שנתי (עד 30.8), שהנתונים שמורים, ומזמין לחדש מול VaadyGo (בלי סליקה — רק מסר). טסט חדש שמאמת את הניתוב. 79 טסטים עוברים, build ירוק. נעשה כולו בצד הלקוח — לא נגעתי בשרת (שהיה נעול לעריכה על ידי סוכן אחר).
- **למה:** להשלים פריט צד-לקוח פתוח בשלב 10, ולתת חוויה נעימה במקום שגיאה מבלבלת כשמנוי מסתיים.
- **קבצים:** `src/pages/SubscriptionExpiredPage.js` (חדש), `src/pages/LoginPage.js`, `src/App.js`, `src/pages/Auth.test.js`, ROADMAP.md.
- **החלטות:** זיהוי המנוי-פג לפי הכללת המחרוזת "המנוי פג" בהודעת השרת (בלי לשנות את השרת). לא הומצא מנגנון חידוש/סליקה — המסך אינפורמטיבי בלבד. "צרו קשר" נשאר גנרי (אין עדיין ערוץ תמיכה מוגדר — ❓ ב-UI_SPEC).
- **הצעד המומלץ הבא:** בשלב 10 נותר רק "כניסה עם Google" (OAuth) — דורש פרטי חיבור מ-Google Cloud של בעלת המוצר. שאר המערכת חיה ומאובטחת ב-Railway.

## 10.07.2026 — ימי הולדת הצוות: תקציב מומלץ למתנה = 3% מהתקציב [Claude Code]

- **מה נעשה:** במסך הבית, ליד כל יום הולדת של הצוות, **המערכת ממליצה** על תקציב למתנה — מוצג "🎁 מומלץ למתנה: X ₪", כאשר X = **3% מסך התקציב הכולל** (יעד הגבייה). התצוגה אוטומטית, בלי הזנה של המשתמשת.
- **למה:** בקשת בעלת המוצר — לעזור לוועד לדעת כמה מומלץ להשקיע על מתנה, לפי כלל אחיד יחסי לתקציב.
- **קבצים:** `src/pages/home/StaffBirthdays.js` (מקבל `totalBudget` כ-prop ומחשב 3%), `src/pages/HomePage.js` (מעביר `totalBudget={dashboard.collectionTarget}`), `src/styles/home.css`, `src/pages/home/StaffBirthdays.test.js`. **פרונט בלבד — לא נגעתי ב-backend.**
- **החלטות:** (1) **המערכת מחשבת ולא המשתמשת מזינה** — 3% מיעד הגבייה (`collectionTarget` מהדשבורד). זהו שינוי מהגרסה הראשונה של הפיצ'ר (שם המשתמשת הזינה סכום ידני ל-localStorage) לפי הבהרת בעלת המוצר. הקבצים `staffGiftBudgetsService.js` ו-`StaffGiftBudgetForm.js` נמחקו. (2) כשאין תקציב (0) — לא מוצגת המלצה. (3) האחוז מרוכז בקבוע `GIFT_BUDGET_RATE` לשינוי קל בעתיד.
- **הצעד הבא:** אם בעלת המוצר תרצה אחוז אחר או בסיס אחר (למשל יתרת הקופה במקום היעד) — שינוי של קבוע/שדה אחד.

## 10.07.2026 — קישורי התשלום של הוועד עברו לשרת (משותפים לכל החברות) [Claude Code B]

- **מה נעשה:** קישורי הביט/פייבוקס של הוועד (ששימשו את "בקשת תשלום") הועברו מ-localStorage מקומי ל**שרת**, כדי שכל 2-3 חברות הוועד יראו את אותם קישורים. **שרת** (תחת BACKEND_LOCK): נוספו `BitLink`/`PayboxLink` למודל `Group` + Migration‏ `AddGroupPaymentLinks` (עמודות additive), מופו ב-`GroupResponseDto`, ונוסף `PUT /api/groups/{id}/payment-links` (‏`GroupsController` → `IGroupService.UpdatePaymentLinksAsync`) עם ולידציה שהקישור מתחיל ב-http/https. אומת מקצה-לקצה מול שרת רץ (הרשמה→token→יצירת גן→PUT→GET מחזיר את הקישורים→400 על קישור לא תקין). **לקוח:** `paymentSettingsService` הפך לאסינכרוני — קורא/כותב לשרת לפי `groupId` (מ-onboarding) עם **נפילה חיננית ל-localStorage** כשאין גן/שרת; `PaymentRequestButton` טוען את הקישורים ב-`useEffect`. 3 טסטי לקוח עוברים + build ירוק.
- **למה:** לפי ה-README יש 2-3 חברות ועד לגן — קישור שנשמר רק בדפדפן של אחת לא נראה לאחרות. שמירה ברמת הגן פותרת זאת, ותואמת-קדימה לריבוי-מוסדות (כל מוסד — קישורים משלו).
- **קבצים:** שרת: `Models/Group.cs`, `DTOs/GroupResponseDto.cs` (+`GroupPaymentLinksDto`), `Services/{IGroupService,GroupService}.cs`, `Controllers/GroupsController.cs`, `Migrations/*AddGroupPaymentLinks*` + snapshot. לקוח: `src/services/paymentSettingsService.js`, `src/components/PaymentRequestButton.js`.
- **החלטות:** (1) עבודת השרת בוצעה תחת **BACKEND_LOCK** (נתפס ב-ee435c4, ישוחרר אחרי CI ירוק) — אף סוכן אחר לא נגע ב-backend במקביל. (2) endpoint ייעודי `/payment-links` (ולא PUT גורף לגן) — עדכון ממוקד שלא נוגע בקטגוריות/יעד. (3) נפילה מקומית נשמרת כמטמון — התכונה עובדת גם בלי גן מסונכרן. (4) לא נגעתי בעבודת ה-frontend הזרה (תקציבי מתנות לצוות) שהייתה לא-שמורה בעץ.
- **הצעד המומלץ הבא:** בריבוי-מוסדות — לוודא ש-`groupId` הוא של המוסד הנבחר; אפשר גם לחבר את תקציבי החגים (לוח שנה) לשרת באותה דרך.

## 09.07.2026 — מסך תשלום (דמו) — placeholder עד לחיבור סליקה [Claude Code]

- **מה נעשה:** נבנה `CheckoutPage` בנתיב `/pay` — מסך תשלום שנראה אמיתי (פס "מסך הדגמה", סיכום סכום+תיאור מפרמטרים בכתובת `?amount=&for=`, טופס כרטיס אשראי עם ולידציה בעברית, כפתור "שלם X ₪") שמדמה תשלום מוצלח ועובר למסך "התשלום התקבל בהצלחה". **פרונט בלבד — לא נגעתי ב-backend.**
- **למה:** בעלת המוצר ביקשה להתחיל לבנות את מסך התשלום כ-placeholder לא-אמיתי עד שתחובר סליקה. (סליקה בתוך המערכת מסומנת ב-ROADMAP כרצון עתידי מחוץ לתחום — זהו ה-UI המקדים בלבד.)
- **קבצים:** `src/pages/CheckoutPage.js` + טסט, `src/styles/checkout.css`, `src/App.js` (נתיב `/pay`). שימוש חוזר ב-`useForm`/`Input`/`Button`/`formatShekels` (DRY).
- **החלטות:** (1) **אין חיוב ואין שליחה/שמירה של פרטי הכרטיס** — הכל נשאר בצד הלקוח ונזרק; פס הדגמה בולט כדי שלא יטעו לחשוב שזה חי. (2) הסכום/תיאור מגיעים מפרמטרים בכתובת — כך שקישור תשלום עתידי (למשל מתזכורת) יעביר אותם, וכשתחובר סליקה מחליפים רק את פונקציית `pay()`. (3) הנתיב מאחורי הזדהות כרגע; אם בעתיד הורים ישלמו דרך קישור ללא חשבון — יהפוך לציבורי.
- **הצעד הבא:** נקודת כניסה למסך (כפתור "שלם עכשיו" בתזכורת/מסך תשלומים), ובהמשך חיבור ספק סליקה אמיתי במקום פונקציית ה-`pay()` המדומה.

## 09.07.2026 — ריבוי מוסדות תחת משתמש אחד — Stage 1 (צד לקוח)

- **מה נעשה:** לפי בקשת בעלת המוצר — משתמשת אחת יכולה לנהל כמה ועדים/מוסדות. **החלטות בעלת המוצר:** (א) מוסד נוסף = "רכישה" נפרדת, בשלב זה **מסך רכישה זמני** בלי סליקת אשראי אמיתית; (ב) כל מוסד = **סביבה נפרדת לחלוטין**; (ג) לבנות **בשלבים — קודם הנראה**. **Stage 1 (צד לקוח בלבד):** `institutionsService` מנהל רשימת מוסדות + מוסד פעיל ב-localStorage; בסיום ההרשמה נוצר המוסד הראשי (מופעל) + מוסדות נוספים מהשמות שהוזנו בצעד 3 (לא-מופעלים). נוסף **תפריט צד** (`SideMenu`, נפתח מכפתור ☰ בכותרת) עם **מחליף מוסדות** (`InstitutionSwitcher`) ויציאה; שם המוסד הפעיל מוצג בכותרת. מעבר למוסד מופעל טוען את נתוני ההרשמה שלו (שם/גבייה/קטגוריות מתחלפים); מוסד "🔒 להפעלה" מוביל ל-`PurchasePage` (רכישה זמנית → הרשמה → מופעל ופעיל). 4 טסטים חדשים (סה"כ 73 עוברים) + build ירוק.
- **למה:** ועד רבים מנוהל ע"י אותה אחראית; היא רוצה חשבון אחד ולעבור בין המוסדות בלי להתחבר מחדש.
- **קבצים:** `src/services/institutionsService.js` (+טסט), `src/components/{SideMenu,InstitutionSwitcher}.js`, `src/pages/PurchasePage.js`, `src/styles/sidemenu.css`, `src/App.js` (כפתור ☰ + תפריט צד + נתיב רכישה + שם מוסד בכותרת), `src/pages/onboarding/OnboardingWizard.js` (רישום המוסדות בסיום).
- **החלטות:** (1) Stage 1 הוא **צד לקוח בלבד** — כל מוסד שומר את נתוני ההרשמה שלו, וחילוף מוסד טוען אותם למפתח ה-onboarding המשותף כך שהמסכים שנשענים עליו מתחלפים. (2) **הפרדת התלמידים/התשלומים בפועל = Stage 2 (שרת)** — כרגע נתוני ה-API עדיין משותפים; זה הצעד הבא. (3) מסך הרכישה מדומה (סליקה אמיתית = הרחבה עתידית עם ספק סליקה). (4) לא נגעתי ב-ROADMAP (היו בו שינויים לא-שמורים של סוכן אחר) — התיעוד כאן בלבד.
- **הצעד הבא — Stage 2 (שרת, דורש נעילת backend):** לשייך `Group`/מוסד ל-`User`, ולסנן את כל הנתונים (תלמידים, תשלומים, לוח שנה, מתנות, קבצים) לפי המוסד הפעיל. אחרי זה החלפת מוסד תחליף גם את הנתונים האמיתיים.

---

## 09.07.2026 — תזכורת גורפת: סינון לפי כיתה/קבוצה [Claude Code B]

- **מה נעשה:** ל-`BulkReminderButton` נוסף סינון לפי כיתה/קבוצה בתוך החלון (רשימה נפתחת שמופיעה רק כשיש יותר מכיתה אחת בין החייבים). הרשימה והמונה מתעדכנים לכיתה הנבחרת — כך אפשר לתזכר רק הורי כיתה מסוימת. טסט חמישי. **צד-לקוח בלבד** (BACKEND_LOCK נבדק = FREE, לא נגעתי בשרת).
- **קבצים:** `src/components/BulkReminderButton.js` + טסט.
- **הצעד המומלץ הבא:** שמירת סטטוס התזכורות בשרת (מעקב לאורך זמן) — כשיתפנה ה-backend.

## 09.07.2026 — תזכורת גורפת: מעקב "נשלח" + מונה התקדמות [Claude Code B]

- **מה נעשה:** שיפור ל-`BulkReminderButton` (תזכורת לחייבים): לחיצה על "שליחה" ליד הורה מסמנת אותו כ**נשלח** (רקע ירוק + ✅, הכפתור הופך ל"שליחה חוזרת"), ומונה "נשלחו X מתוך Y" מתעדכן. כל פתיחת חלון מתחילה קמפיין נקי. טסט רביעי נוסף.
- **למה:** בעלת המוצר עוברת על רשימה ארוכה של הורים; בלי סימון קל לאבד את המקום ולתזכר פעמיים. הסימון האוטומטי בלחיצה (פתיחת וואטסאפ = נשלח) נותן מעקב בלי עבודה נוספת.
- **קבצים:** `src/components/BulkReminderButton.js` + טסט, `src/styles/payments.css`. **צד-לקוח בלבד** — לא נגעתי ב-backend (BACKEND_LOCK נבדק = FREE) ולא ב-StudentsPage.
- **החלטות:** הסימון בזיכרון החלון (state) ומתאפס בפתיחה מחדש — מתאים למעקב "בישיבה אחת" ולא שומר סימונים ישנים מקמפיין קודם.
- **הצעד המומלץ הבא:** אם ירצו — סינון התזכורת לפי כיתה/קבוצה, או שמירת הסטטוס בשרת למעקב לאורך זמן.

## 09.07.2026 — הרשמה: השלמה אוטומטית לשם הגן מהמאגר הרשמי (משרד החינוך) [Claude Code]

- **מה נעשה:** באשף ההרשמה, שדה "שם הגן" קיבל השלמה אוטומטית מהמאגר הרשמי של משרד החינוך (data.gov.il, מאגר "מאפייני מוסדות חינוך", resource_id `5548fd63-5868-4053-ad81-98caddc5e232`), מסונן לסוג "גן ילדים" ולפי העיר שנבחרה. נוצר `kindergartensService` שקורא ל-`datastore_search` (filters לסוג + q לעיר/שם, dedupe על כפילויות-שנים), רכיב `Autocomplete` הורחב ב-prop‏ `filterLocally` (להצגת תוצאות שרת בלי סינון מקומי כפול), ו-`GanDetailsStep` מושך הצעות ב-debounce לפי עיר+שם. **הקלדה חופשית תמיד עובדת** (כבקשת בעלת המוצר), וכל כשל בשליפה נבלע בחן.
- **למה:** בעלת המוצר ביקשה שיפתח מאגר גנים אמיתי בבחירה, עם אופציה להקלדה ידנית.
- **קבצים:** `src/services/kindergartensService.js` (חדש), `src/components/Autocomplete.js`, `src/pages/onboarding/GanDetailsStep.js` + טסט חדש. **פרונט בלבד — לא נגעתי ב-backend.**
- **החלטות:** (1) **קריאה ישירה מהדפדפן ל-data.gov.il** — אומת ש-CORS מותר (`Access-Control-Allow-Origin: *`), ולכן **אין צורך בשרת/מתווך ואין צורך בנעילת BACKEND_LOCK**. (2) בחירת מקור: המאגר הרשמי (החלטת בעלת המוצר) — לא רשימה סטטית (גנים = עשרות אלפים, לא ניתן להטמיע) ולא הגנים שבמערכת. (3) שאילתה רק כשיש עיר + ≥2 תווים בשם, כדי לא להעמיס על ה-API.
- **הצעד הבא:** אם ה-API של data.gov.il אי-פעם לא זמין — ההתנהגות מתדרדרת בחן להקלדה חופשית; שקילת cache קליינט-סייד אם יתגלה עומס.

## 09.07.2026 — תזכורת גורפת לכל החייבים (וואטסאפ, בקשת בעלת המוצר)

- **מה נעשה:** נוסף כפתור **"📣 תזכורת לחייבים"** ליד כותרת מסך התלמידים. בלחיצה נפתח חלון עם הודעה גנרית **ניתנת לעריכה** (ברירת מחדל = הנוסח שבעלת המוצר נתנה: "שלום לך :) אנחנו עושים מעבר על התשלומים... נא לטפל בהקדם, תודה!") ורשימת כל ההורים שטרם שילמו, עם כפתור וואטסאפ אישי ליד כל שם. עריכת ההודעה משתקפת מיד בכל הקישורים. 3 טסטים חדשים + build ירוק.
- **למה:** בעלת המוצר ביקשה דרך לשלוח בבת אחת תזכורת לכל מי שלא שילם. וואטסאפ לא תומך בשליחה לכמה נמענים בקישור אחד — לכן פותחים שיחה נפרדת לכל הורה (לחיצה אחת לכל אחד).
- **קבצים:** `src/components/BulkReminderButton.js` + טסט (חדשים), `src/pages/StudentsPage.js` (הכפתור בכותרת — 2 הוספות בלבד), `src/styles/payments.css`.
- **החלטות:** (1) הרכיב עצמאי ונשען על סיכומי התשלום (`hasUnpaid`) ש-StudentsPage כבר טוען ברקע — בלי קריאות שרת נוספות. (2) עריכה של StudentsPage צומצמה ל-2 שורות (import + הכפתור) כדי למזער התנגשות עם הסוכן שהוסיף "ייבוא תלמידים" באותו קובץ. (3) ההודעה ניתנת לעריכה לפני שליחה — גמישות לקמפיינים שונים. (4) מחזור שימוש ב-`buildWhatsappReminderUrl` הקיים (DRY).
- **הצעד המומלץ הבא:** אם ירצו — "סמן כנשלח" לכל הורה כדי לעקוב את מי כבר תזכרו; או ריכוז קישורי הוועד (ביט/פייבוקס) והתזכורות במודל Group בשרת.

## 09.07.2026 — מסך הרשמה: הצעת סיסמה חזקה + מד חוזק (בקשת בעלת המוצר)

- **מה נעשה:** בעלת המוצר קיבלה התראת דפדפן ש-`123456` היא סיסמה שנפרצה, וביקשה עזרה בבחירת סיסמה. נוסף למסך ההרשמה: (1) כפתור **"✨ הצע לי סיסמה חזקה"** שמייצר סיסמה אקראית חזקה (14 תווים, 4 סוגי תווים, `crypto.getRandomValues`, בלי תווים מבלבלים) וממלא אותה גלויה; (2) **מד חוזק סיסמה** חי (4 מקטעים + תווית חלשה/בינונית/חזקה/חזקה מאוד) לפי אורך ומגוון תווים — סיסמה מסוג-תווים-אחד (כמו 123456) תמיד "חלשה"; (3) כפתור עין להצגה/הסתרה. נבנה כרכיב גנרי `PasswordField` לשימוש חוזר. 3 טסטים חדשים, סה"כ 64 עוברים, build ירוק.
- **למה:** הגנה על המשתמשת מבחירת סיסמה חלשה למערכת שמכילה פרטי ילדים — חוויה מודרכת במקום התראה מבלבלת.
- **קבצים:** `src/utils/passwordStrength.js` (חדש), `src/components/PasswordField.js` (חדש) + טסט, `src/pages/RegisterPage.js`, `src/styles/onboarding.css`.
- **החלטות:** מד החוזק מודד הרכב (אורך+מגוון), לא רשימות פריצה (על זה הדפדפן מתריע). הרכיב גנרי כדי שישמש גם במסך "שינוי סיסמה" עתידי. אורך מינימום בשרת נשאר 6 (RegisterDto) — המד רק ממליץ, לא חוסם.
- **הצעד המומלץ הבא:** המערכת עלתה בהצלחה ל-Railway ובעלת המוצר נכנסה מהטלפון ✅. מומלץ: הוספת `Jwt__Key` כמשתנה סביבה בשרת (soothing-clarity) לאבטחת ה-token בייצור (כרגע רץ עם מפתח ברירת-מחדל). בנוסף: ניקוי פרויקטי Railway כפולים ישנים.

## 09.07.2026 — בקשת תשלום מכרטיס התלמיד (ביט/פייבוקס/מזומן דרך וואטסאפ)

- **מה נעשה:** לפי בקשת בעלת המוצר — ליד כל תלמיד נוסף כפתור **"בקשת תשלום 💸"**. בלחיצה נפתח חלון עם החוב הפתוח (הקטגוריות שטרם שולמו + סה"כ) ושלושה אמצעים: **ביט / פייבוקס / מזומן**. כל אמצעי פותח **וואטסאפ להורה** עם הודעה מוכנה — לביט/פייבוקס מצורף קישור התשלום הקבוע של הוועד + הסכום; למזומן תזכורת בלבד ("נא להסדיר את התשלום בהקדם"). הרכיב `PaymentRequestButton` עצמאי לחלוטין (מנהל טעינה, קישורים וחלון בעצמו) ונכנס ל-`StudentCard` — **בלי לגעת ב-StudentsPage**, כדי לא להתנגש בסוכן מקביל שערך אותו באותו רגע (ייבוא תלמידים). 3 טסטים חדשים + build ירוק.
- **למה:** בעלת המוצר ביקשה "העברת כספים דרך האפליקציה" — כאן ממומש כ**קישור לאמצעי תשלום חיצוני** (לא סליקה פנימית): הוועד שולח להורה הודעת וואטסאפ עם הסכום והקישור, וההורה משלם באפליקציה שלו. תואם את הגבול "בלי סליקה בתוך המערכת".
- **קבצים:** `src/components/PaymentRequestButton.js` + טסט (חדשים), `src/services/paymentSettingsService.js` (חדש — קישורי הוועד ב-localStorage), `src/services/paymentsService.js` (‏`buildPaymentRequestMessage` + הוצאת `reminderHead` משותף — DRY), `src/components/StudentCard.js` (הכפתור), `src/styles/payments.css`.
- **החלטות:** (1) **לא ממציאים קישורי תשלום** — המשתמשת מדביקה את קישור הביט וקבוצת הפייבוקס של הוועד פעם אחת (בתוך חלון הבקשה, ⚙️); בלי קישור, אותו אמצעי מציע להוסיף אותו. (2) שאלתי את בעלת המוצר על המנגנון (יש לה קישורים לשניהם; העדפה: וואטסאפ להורה עם הסכום+קישור). (3) הקישורים נשמרים בדפדפן (localStorage) — לא בשרת — כדי לא להוסיף Migration ל-backend החם (יעברו ל-Group בעתיד). (4) הרכיב עצמאי כדי להימנע מהתנגשות ב-StudentsPage.
- **הצעד המומלץ הבא:** כשמודל Group יקבל שדות — להעביר לשם את קישורי הוועד (משותף לכל החברות). אפשר גם לצרף את הסכום לכל אמצעי אם ביט/פייבוקס יתמכו בקישור עם סכום מוגדר מראש.

## 09.07.2026 — ייבוא תלמידים מקובץ + תיקון טסט תשלומים שחסם CI

- **מה נעשה:** לפי בקשת בעלת המוצר — ייבוא תלמידים מקובץ במקום הקלדה ידנית. במסך התלמידים נוסף כפתור "📄 ייבוא מקובץ" שפותח מודאל: מורידים תבנית CSV (שם הילד · שם ההורה · טלפון), ממלאים, מעלים — והמערכת יוצרת את כולם, עם סיכום כמה נוספו וכמה נכשלו (לתיקון ידני). שם הילד מפוצל אוטומטית לפרטי+משפחה; מזוהה מפריד פסיק/נקודה-פסיק (אקסל עברי); שורת כותרת מדולגת. נוסף שדה "שם הורה" לטופס התלמיד ולמודל בשרת (`Student.ParentName`), וּוֹלידציית השרת רוככה (רק שם פרטי חובה) כדי לאפשר ייבוא שמות-בלבד. **בנוסף — תוקן טסט שבור של התשלומים:** הסוכן שפיצל תשלומים לביט/פייבוקס/מזומן שינה את `PaymentRow` אך לא עדכן את `StudentPaymentsPage.test`, וה-CI היה אדום (חוסם פריסה לכולם, כולל מסך הקבצים). הטסט עודכן ל-UI החדש (שדה סכום לכל אמצעי במקום בורר). כל 58 הטסטים עוברים + build ירוק.
- **למה:** ועד עם 30 ילדים לא מקליד תלמיד-תלמיד; מעלה רשימה וגמרנו. תיקון ה-CI חיוני — בלעדיו שום דבר לא נפרס.
- **קבצים:** לקוח: `src/pages/students/StudentsImport.js`, `src/services/studentsImport.js` (+טסט), `src/pages/StudentsPage.js` (כפתור+מודאל), `src/components/StudentForm.js` (שדה שם הורה — כבר נכנס עם commit התשלומים), `src/styles/students.css`, `src/pages/StudentPaymentsPage.test.js` (תיקון). שרת (כבר על main מ-commit קודם): `Student.ParentName` + ריכוך `StudentWriteDto` + Migration‏ `AddStudentParentName`.
- **החלטות:** (1) הייבוא משתמש ב-endpoint הקיים של יצירת תלמיד (לולאה) — בלי endpoint חדש. (2) ולידציית השרת רוככה (רק שם פרטי חובה) כדי לאפשר שמות-בלבד; הטופס בלקוח עדיין מחייב שדות בהוספה ידנית. (3) תיקנתי טסט של סוכן אחר כי הוא חסם פריסה לכולם והסוכן כבר סיים (קבצים committed/נקיים) — לא התנגשות עם עבודה פעילה. (4) ה-backend של ParentName נסחף לתוך commit התשלומים בתיקייה המשותפת — המצב על main עקבי (מודל+מיגרציה+snapshot).
- **הצעד הבא:** כשהאתר יפרס (עכשיו ש-CI ירוק) — לבדוק ייבוא מקובץ במסך התלמידים ואת מסך הקבצים. בהמשך: כניסת Google (שלב 10).

## 09.07.2026 — תשלומים: פיצול לפי אמצעי (ביט/פייבוקס/מזומן) עם סכום לכל אחד [Claude Fable 5]

- **מה נעשה:** במסך תשלומי התלמיד, כל קטגוריה מציגה עכשיו **שדה סכום נפרד לכל אמצעי** — ביט / פייבוקס / מזומן — כך שאפשר גם לפצל תשלום בין אמצעים (בקשת בעלת המוצר). **שרת:** מודל `Payment` שומר `BitAmount`/`PayBoxAmount`/`CashAmount` במקום `Amount`+`Method` יחידים (הסך ששולם = סכומם); Migration‏ `SplitPaymentByMethod` משנה את הטבלה **ושומר נתונים קיימים** (הסכום הישן עובר לעמודת האמצעי שנבחר בו, לפני מחיקת העמודות הישנות). DTOs, `PaymentService`, `DashboardService` (פירוק אמצעים + נגבה) ו-`StudentService.TotalPaid` עודכנו למבנה החדש. **לקוח:** `PaymentRow` מציג שדה סכום לכל אמצעי עם סה"כ רץ; שורה ששולמה מציגה את הפירוק. commit `ff82b2c`.
- **למה:** בעלת המוצר ביקשה לרשום כמה שולם בכל אמצעי, עם אפשרות פיצול.
- **קבצים:** `backend/Models/Payment.cs`, `backend/DTOs/{PaymentUpsertDto,PaymentResponseDto}.cs`, `backend/Services/{PaymentService,DashboardService,StudentService}.cs`, Migration `SplitPaymentByMethod`; `src/pages/payments/PaymentRow.js`.
- **החלטות:** (1) המיגרציה שומרת נתונים — לא מאבדים תשלומים קיימים. (2) "שולם" נשאר סימון ידני; ולידציה: אי אפשר לסמן ששולם בלי סכום באחד האמצעים. (3) `Amount` בתשובה = יעד הקטגוריה (לתצוגה/תזכורת), הסכומים בפועל בשלושת השדות. (4) המיגרציה נערמה על `AddStudentParentName` של סוכן מקביל ונדחפו יחד (ה-snapshot מצמיד אותן); פיצ'ר ייבוא-התלמידים שלו נשאר WIP שלו ולא נכלל בקומיט.
- **אימות:** מקצה-לקצה מול שרת רץ — PUT פיצול (ביט 700 + מזומן 500) נשמר, `TotalPaid`=1,200, פירוק דשבורד ביט 700/מזומן 500, וסימון "שולם" עם אפסים → 400. ‏`npm run build` ירוק.
- **הצעד המומלץ הבא:** לאפשר עריכת הסכומים גם אחרי סימון "שולם" (כרגע ביטול-הסימון פותח לעריכה מחדש).

## 09.07.2026 — שלב 8 הושלם: מסך קבצים — קישורי תיקיות Google Drive (ידני)

- **מה נעשה:** מומש מסך הקבצים לפי הגישה המעודכנת של בעלת המוצר (קישורים ידניים במקום OAuth). **שרת:** מודל `DriveFolder` (שם + קישור) + Migration‏ `AddDriveFolders` + שכבות מלאות — `FoldersController` (`api/folders`) דק → `IDriveFolderService`/`DriveFolderService` → `IRepository<DriveFolder>` גנרי, עם DTOs וולידציה בעברית (קישור חייב http/https). **לקוח:** `FilesPage` — כל תיקייה מוצגת כקישור שנפתח בדרייב (טאב חדש), עם הוספה/עריכה (`FolderForm`) ומחיקה באישור, מצבי טעינה/שגיאה/ריק, ו-fallback ל-localStorage. 3 טסטי לקוח (סה"כ 53 עוברים) + build ירוק.
- **למה:** בעלת המוצר רצתה דרך פשוטה לרכז קישורים לתיקיות הדרייב של הוועד בלי לחפש ביניהן — בלי המורכבות (והתלות בשלב 10) של OAuth אמיתי.
- **קבצים:** שרת: `backend/Models/DriveFolder.cs`, `backend/DTOs/DriveFolderDtos.cs`, `backend/Services/{IDriveFolderService,DriveFolderService}.cs`, `backend/Controllers/FoldersController.cs`, `backend/AppDbContext.cs`, `backend/Program.cs` (DI), Migration `AddDriveFolders`. לקוח: `src/pages/FilesPage.js`, `src/pages/files/{FolderForm,FilesPage.test}.js`, `src/services/filesService.js`, `src/styles/files.css`. תיעוד: UI_SPEC ס' 13 עודכן לגישת הקישורים הידניים.
- **החלטות:** (1) **בלי OAuth בשלב זה** — קישורים ידניים נותנים את כל הערך (ריכוז התיקיות) בלי תלות בכניסת Google; OAuth אמיתי + העלאת קבצים = הרחבה עתידית עם שלב 10. (2) המשתמשת אחראית להרשאות השיתוף בדרייב — המערכת רק שומרת קישור. (3) **אימות e2e נדחה לפריסה** — השרת המקומי היה תפוס ע"י סוכן אחר (פורט 5195), ולא עצרתי אותו כדי לא להפריע; ה-build עובר וה-CRUD זהה בדיוק ל-Vendor/Gift שמאומתים בייצור.
- **הצעד הבא:** לפי בקשת בעלת המוצר — ייבוא תלמידים מרשימה (הדבקה/CSV) במקום הקלדה ידנית (ממתין לאישור הגישה: שמות-בלבד עם טלפון אופציונלי). בהמשך: כניסת Google (שלב 10) שתפתח גם OAuth ל-Drive.

## 09.07.2026 — עוזרת ה-AI: מעבר מ-Claude ל-Google Gemini (מסלול חינמי) [Claude Code]

- **מה נעשה:** לפי החלטת בעלת המוצר, ספק ה-AI הוחלף מ-Anthropic Claude ל-**Google Gemini** במסלול החינמי. שוכתב `AiService` לקרוא ל-Gemini `generateContent` (Generative Language API) דרך אותו HttpClient: כתובת `…/v1beta/models/{model}:generateContent`, מפתח בכותרת `x-goog-api-key`, גוף עם `systemInstruction`/`contents`/`generationConfig`, וחילוץ מ-`candidates[0].content.parts[].text`. טיפול בחסימות בטיחות (‏`promptFeedback.blockReason` וגם `finishReason=SAFETY`). דגם ברירת מחדל `gemini-2.5-flash` (זמין במסלול החינמי; ניתן להחליף ל-`gemini-3.5-flash` דרך `Gemini__Model` בלי שינוי קוד).
- **למה:** בעלת המוצר בחרה להתחיל במסלול חינמי ולעבור לתשלום רק אם יהיה ביקוש. Gemini מציע מדרגה חינמית מוגבלת; Claude/Anthropic הוא בתשלום מהרגע הראשון.
- **קבצים:** `backend/Services/AiService.cs` (שוכתב), `backend/appsettings.json` (סקשן `Anthropic`→`Gemini`, מפתח ריק — בלי סוד), `backend/Controllers/AiController.cs` (הודעת 503 מעודכנת). **הממשק `IAiService` לא השתנה** — הקונטרולר ומסך העוזרת בלקוח לא נגעו. `Program.cs` לא שונה (רישום ה-HttpClient הגנרי מתאים לכל ספק) — נמנעה התנגשות עם סוכן שלב 8 שעורך אותו כרגע.
- **החלטות:** (1) שימוש ב-`generateContent` הקלאסי והיציב (ולא ב-Interactions API החדש) — מבנה JSON מתועד ופשוט לניתוח ידני ב-C#. (2) המפתח רק ממשתני סביבה (`Gemini:ApiKey`), לעולם לא בקוד. (3) הפרטיות נשמרה: ה-persona לא שולח שמות/טלפונים — **שימי לב שהמסלול החינמי של Google עשוי להשתמש בנתונים לשיפור המוצר** (בשונה ממסלול בתשלום), ולכן חשוב שלא יישלחו פרטים מזהים.
- **אימות:** build נקי (0 אזהרות) בעותק מבודד; מקצה-לקצה: הרשמה→טוקן, ואז `POST /api/ai/ask` עם הטוקן החזיר **503** ("לא הופעלה") — מוכיח שהחיווט תקין וממתין רק למפתח. הנתיב המלא ייבדק עם המפתח האמיתי של בעלת המוצר.
- **הצעד הבא:** בעלת המוצר יוצרת מפתח חינמי ב-Google AI Studio (aistudio.google.com) ומוסיפה משתנה `Gemini__ApiKey` ל-Railway Variables בשירות הבקאנד; אחר כך בדיקה מהאפליקציה החיה.

## 09.07.2026 — הרשמה: השלמה אוטומטית לעיר + רכיב Autocomplete גנרי [Claude Fable 5]

- **מה נעשה:** שדה "עיר" באשף ההרשמה קיבל **השלמה אוטומטית**. נבנה רכיב גנרי `Autocomplete` (מציג הצעות מסוננות תוך כדי הקלדה, **ומאפשר גם ערך חופשי** שאינו ברשימה), קובץ נתונים `src/data/israeliCities.js` (~110 היישובים הגדולים/מוכרים), וחיבור לשדה העיר ב-`GanDetailsStep`. פרונט-בלבד, לא תלוי בשרת. קומפילציה נקייה (תוקנה אזהרת נגישות: `combobox` דורש `aria-controls`). commit `5e018a0`.
- **למה:** בקשת בעלת המוצר — השלמה אוטומטית לעיר, עם נפילה להקלדה חופשית ליישוב שלא ברשימה.
- **קבצים:** `src/components/Autocomplete.js` (חדש), `src/data/israeliCities.js` (חדש), `src/styles/autocomplete.css` (חדש), `src/pages/onboarding/GanDetailsStep.js`.
- **החלטות:** הרשימה חלקית (הערים הגדולות) + הקלדה חופשית — לא חוסמת יישוב חסר; להחלפה ברשימת היישובים הרשמית המלאה (הלמ"ס) די להחליף קובץ אחד. הרכיב גנרי — ישרת גם את רשימת הגנים-לפי-עיר בהמשך.
- **הצעד המומלץ הבא:** רשימת גנים/בתי ספר **לפי העיר שנבחרה** (מוסדות חינוך של משרד החינוך) — הבקשה הפתוחה הנותרת של בעלת המוצר; יכולה להשתמש באותו רכיב `Autocomplete`.

## 09.07.2026 — מסך תלמידים: תאריך לידה + סכום ששולם לכל תלמיד [Claude Fable 5]

- **מה נעשה:** לכל תלמיד נוסף **תאריך לידה** ותצוגת **כמה שילם** בכרטיס. שרת: שדה `BirthDate` (DateOnly, אופציונלי) ב-`Student` וב-DTOs, `StudentService` מזריק את מאגר התשלומים ומחזיר `TotalPaid` (סכום התשלומים ששולמו) לכל תלמיד, ו-Migration‏ `AddStudentBirthDate` (עמודה additive). לקוח: שדה תאריך לידה בטופס (ריק→null), וכרטיס התלמיד מציג יום הולדת בקטן (🎂) ו"שולם עד כה: X ₪". אומת מקצה-לקצה (POST 201 עם תאריך, GET מחזיר `birthDate`+`totalPaid`, קומפילציה נקייה). commit `57babd5`.
- **למה:** בקשת בעלת המוצר — ליד כל תלמיד יום הולדת בקטן וכמה כסף העביר.
- **קבצים:** `backend/Models/Student.cs`, `backend/DTOs/{StudentWriteDto,StudentResponseDto}.cs`, `backend/Services/StudentService.cs`, Migration `AddStudentBirthDate`; `src/components/{StudentCard,StudentForm}.js`, `src/pages/StudentsPage.js`, `src/services/format.js`.
- **החלטות:** `BirthDate` אופציונלי (לא שובר תלמידים קיימים); `TotalPaid` מחושב בשרת (לא נשמר במסד) מסכום התשלומים ששולמו. בוצע על **בסיס נקי** — ה-WIP המקביל (ספקים/אבטחה) הוחנה בצד (stash) ואז הוחזר, כדי שהמיגרציה שלי לא תתערבב בשלהם.
- **הצעד המומלץ הבא:** רישום תשלום מהיר ישירות מכרטיס התלמיד (כיום דרך מסך תשלומים נפרד).

## 09.07.2026 — 🚀 שלב 0 הושלם: המערכת עלתה לאוויר מקצה-לקצה (Railway)

- **מה נעשה:** בעלת המוצר פרסה את המערכת המלאה ל-Railway בליווי צעד-אחר-צעד. פרויקט Railway אחד (`superb-hope`) עם **שני שירותים**: הפרונט (`vaddygo`, `https://vaddygo-production.up.railway.app`) והבקאנד החדש (`soothing-clarity`, `https://soothing-clarity-production.up.railway.app`). הבקאנד: Root Directory=`backend`, Builder=**Dockerfile** (זוהה אוטומטית), Volume ב-`/data`, ומשתני סביבה — `ConnectionStrings__Default=Data Source=/data/vaadygo.db`, `Cors__AllowedOrigins__0`=כתובת הפרונט, `Jwt__Key`=מפתח אקראי. הפרונט חובר עם `REACT_APP_API_URL`=כתובת הבקאנד (נבנה מחדש כדי לצרוב אותה). **אימות:** `GET /api/health` החזיר `{"status":"ok","database":"ok"}`; הרשמת משתמש, הגדרת גן והוספת תלמיד נשמרו ושׂרדו יציאה-וחזרה — כלומר נתונים אמיתיים בשרת, לא ב-localStorage.
- **למה:** זה ההבדל בין "אתר הדגמה" ל"מוצר חי" — מכאן חברות ועד יכולות להירשם ולעבוד באמת, והנתונים נשמרים.
- **קבצים:** ללא שינוי קוד — פעולות דשבורד ב-Railway. עודכנו `ROADMAP.md` (שלב 0 → ✅) ו-`PROJECT_LOG.md`. `DEPLOYMENT.md` הושלם קודם עם משתנה `Jwt__Key`.
- **החלטות/לקחים:** (1) **נמחקו 5 פרויקטי Railway כפולים** (`generous-respect`, `innovative-nature`, `skillful-learning`, `precious-gentleness`, `humorous-reflection`) שנוצרו בטעות מ"New Project" חוזר — צריך פרויקט אחד עם שני שירותים, לא פרויקט לכל שירות. (2) Railway זיהה את ה-Dockerfile אוטומטית ברגע ש-Root Directory הוגדר ל-`backend` — לא היה צריך לבחור Builder ידנית. (3) `REACT_APP_API_URL` נצרב בזמן build של הפרונט, ולכן חובה Deploy-מחדש של הפרונט אחרי הגדרתו.
- **הצעד הבא:** להשלים את שלב 10 (כניסת Google + אכיפת SuperAdmin על ניהול ספקים) ואת שלב 8 (Google Drive), ולסיים את שלב 11 (הקשחת ייצור — כבר יש Health Check).

## 09.07.2026 — שלב 11 (חלקי): Health Check endpoint + סריקת שם סופית

- **מה נעשה:** (1) **Health Check** — נוסף `HealthController` עם `GET /api/health` (‏`[AllowAnonymous]`) שמחזיר 200 כשהשרת והמסד זמינים (בודק `Database.CanConnectAsync`) ו-503 כשאין גישה למסד. קריטי אחרי שלב 10: ה-`FallbackPolicy` אוכף token על כל endpoint, כך שבלי `[AllowAnonymous]` בודק הבריאות של Railway היה מקבל 401. (2) **סריקת שם סופית** — אחרי ששלבים 7/9/10 נדחפו, הוחלפו 8 המופעים האחרונים של "VaadyGo"→"VaddyGo" בקוד שלהם (פרומפט העוזרת שמוצג למשתמשת, קבועי Issuer/Audience של ה-JWT, והערות). אין יותר אף "VaadyGo" בריפו.
- **למה:** בלי endpoint בריאות פתוח, פלטפורמת הפריסה לא יכולה לבדוק אם השרת חי (כל שאר ה-endpoints דורשים token). זה מסיר חסם לפני העלייה ל-Railway.
- **קבצים:** `backend/Controllers/HealthController.cs` (חדש); סריקת שם: `backend/Services/AiService.cs`, `backend/Services/JwtTokenService.cs`, `backend/Models/User.cs`, `backend/Models/Vendor.cs`, `src/pages/gifts/VendorForm.js`, `PROJECT_LOG.md`. ROADMAP עודכן.
- **החלטות:** ‏(1) קונטרולר עם `[AllowAnonymous]` ולא `MapHealthChecks` — לא דורש שינוי ב-Program.cs (הימנעות מהתנגשות עם שלב 10 שעדיין 🔄), ומחזיר גם סטטוס מסד. (2) קבועי ה-JWT הם מקור-אמת יחיד (`JwtSettings`) שגם ההפקה וגם האימות מפנים אליו — שינוי הערך בטוח, ואין עדיין טוקנים שהונפקו. (3) שאר הקשחת שלב 11 (SQL Server, גיבויים, CORS לדומיין) נשארת פתוחה — תלויה בפריסה בפועל; שחררתי את התפיסה.
- **אימות:** אומת מקצה-לקצה בעותק מבודד (השרת של אביבית רץ במקביל, לא הופרע): `/api/health` בלי token → 200 `{status:ok,database:ok}`; `/api/students` בלי token → 401. build נקי (0 אזהרות).
- **הצעד המומלץ הבא:** פריסת הבקאנד ל-Railway (שלב 0) עם `/api/health` כ-Health Check Path ו-`Jwt__Key` במשתני סביבה; אחר כך Google OAuth (סיום שלב 10) שיפתח את שלב 8 (Drive).

## 09.07.2026 — שלב 10: אבטחה וכניסה — בסיס מלא (שרת + לקוח), נותר Google OAuth

- **מה נעשה:** אינטגרציה והשלמה של שלב האבטחה. **שרת (אונטגר מסשן phase-10 מקביל שכבר לא רץ, אומת ונדחף):** מודל `User` (שם משתמש, מייל, `PasswordHash`, `GoogleId`, `Role`, `SubscriptionValidUntil`), `AuthService` (הרשמה/כניסה, גיבוב PBKDF2 ב-`PasswordHasher`), `SubscriptionPolicy` שמעניק תוקף עד 30.8 של השנה שאחרי הרכישה, `JwtTokenService` (תוקף ה-token = תוקף המנוי, מפתח מ-env בלבד), ו-`FallbackPolicy` שדורש token בכל endpoint (`AuthController` הוא `[AllowAnonymous]`). Migration‏ `AddUsers`. **לקוח (נכתב עכשיו):** `authService` (login/register/logout/getToken/isSuperAdmin), `api.js` מצרף `Authorization: Bearer <token>` לכל בקשה (קריאת המפתח ישירות מ-localStorage כדי למנוע תלות מעגלית), `LoginPage` ו-`RegisterPage` אמיתיים מול `/api/auth`, והגנת ניתוב ב-`App.js` (מסך שאינו ציבורי בלי token → מסך פתיחה). זרם: פתיחה → הרשמה (שם משתמש/סיסמה) → אשף הגן → בית. 5 טסטי לקוח חדשים; סה"כ 50 עוברים + build ירוק. אומת מקצה-לקצה מול שרת רץ: הרשמה החזירה תוקף 30.8.2027, ‏`/api/vendors` = 401 בלי token ו-200 איתו.
- **למה:** בלי אבטחה כל אחד יכול לגשת לנתוני הוועד; זה גם הבסיס למודל המנוי בתשלום ולתפקיד "מנהל-על" לניהול הספקים (ערוץ ההכנסה).
- **קבצים:** שרת: `backend/Models/User.cs`, `backend/Auth/{PasswordHasher,SubscriptionPolicy}.cs`, `backend/Services/{IAuthService,AuthService,IJwtTokenService,JwtTokenService}.cs`, `backend/Controllers/AuthController.cs`, `backend/DTOs/{RegisterDto,LoginDto,AuthResponseDto}.cs`, `backend/Program.cs` (JWT + FallbackPolicy), `backend/AppDbContext.cs`, Migration `AddUsers`. לקוח: `src/services/authService.js`, `src/services/api.js` (token), `src/pages/{LoginPage,RegisterPage,WelcomePage}.js`, `src/App.js` (הגנת ניתוב), `src/pages/Auth.test.js`, `src/App.test.js`.
- **החלטות:** (1) **אינטגרציה במקום לזרוק** — קוד האבטחה של הסשן המקביל היה מלא, תקין ותואם לאפיון (כלל 30.8 אומת), אז אונטגר במקום להתחיל מאפס; המיגרציה שלו הייתה מוערמת על מיגרציית הספקים שלי, ולכן שתיהן נדחפו יחד (אחרת אחת הייתה שוברת את השנייה). (2) **אין סוד בקוד:** `Jwt:Key` ריק ב-appsettings — בפיתוח יש fallback חלש מסומן, בייצור חובה `Jwt__Key` ב-Railway. (3) הגנת הניתוב בלקוח פשוטה (token קיים/לא) — אימות התוקף האמיתי נעשה בשרת (token פג = המנוי פג). (4) api.js קורא את ה-token ישירות מ-localStorage (לא דרך authService) כדי להימנע מתלות מעגלית.
- **⚠️ לפני פריסה:** להגדיר `Jwt__Key` (מחרוזת אקראית ארוכה) ב-Railway Variables לפני שהבקאנד עולה — אחרת המפתח החלש בפיתוח בשימוש.
- **הצעד הבא:** כניסת Google (OAuth) — הצד הכי גדול שנותר בשלב 10, ותנאי לשלב 8 (Google Drive). אכיפת `SuperAdmin` על ניהול הספקים. אחר כך שלב 11 (הקשחת ייצור).

## 09.07.2026 — שלב 9 הושלם: עוזרת AI (Claude) — שרת + לקוח

- **מה נעשה:** נבנתה עוזרת ה-AI. **שרת:** `AiController` (`POST /api/ai/ask`) דק → `IAiService`/`AiService` הקורא ל-Claude (Anthropic Messages API, דגם `claude-opus-4-8`) דרך HttpClient מוקצה (typed). מפתח ה-API נקרא מ-`Anthropic:ApiKey` (משתני סביבה בלבד); כשחסר — 503 עם הודעה ידידותית במקום שגיאה. persona בעברית, `max_tokens` מוגבל, טיפול בסירוב בטיחותי (`stop_reason=refusal`), וחילוץ טקסט בטוח מהתשובה. **לקוח:** `AiAssistantPage` (UI_SPEC ס' 14) — פתיח "במה ברצונך לעזור?", צ'יפים לשאלות מהירות (ניסוח תזכורת/הזמנה, רעיונות מתנה, טיפים לתקציב), שדה שאלה חופשית, ותשובה עם מצבי טעינה/שגיאה; `AiFab` — כפתור AI צף (UI_SPEC ס' 10) בנתיב `/assistant`; `aiService` עובר דרך שכבת ה-API. 3 טסטי לקוח + build ירוק; ה-endpoint אומת מקצה-לקצה מול שרת רץ (503 ללא מפתח, 400 על שאלה ריקה).
- **למה:** בעלת המוצר בחרה את שלב 9 כהמשך. העוזרת עוזרת לחברות הוועד לנסח הודעות ולקבל רעיונות — עומס שיורד מהכתפיים, לפי חזון VaddyGo.
- **קבצים:** שרת: `backend/Controllers/AiController.cs`, `backend/DTOs/AiDtos.cs`, `backend/Services/{IAiService,AiService}.cs`, `backend/Program.cs` (רישום HttpClient), `backend/appsettings.json` (סקשן `Anthropic` עם מפתח ריק — בלי סוד). לקוח: `src/pages/AiAssistantPage.js` + טסט, `src/components/AiFab.js`, `src/services/aiService.js`, `src/styles/ai.css`, `src/App.js` (נתיב + כפתור צף).
- **החלטות:** (1) **קריאה גולמית ב-HttpClient ולא NuGet SDK** — .NET 10 חדש מאוד, והוספת תלות ל-Anthropic SDK עלולה לסכן את בניית ה-CI/Railway; קריאת Messages בודדת ב-HttpClient+System.Text.Json יציבה ב-100% וללא תלות חדשה. (2) **פרטיות:** לא נשלחים לבינה שמות/טלפונים — רק טקסט השאלה; רקע כללי לא-מזהה אפשרי בעתיד (יעד/נגבה/תקציבי חגים) בלי שמות. (3) הדגם `claude-opus-4-8` כברירת מחדל (ניתן לשינוי דרך `Anthropic:Model`). (4) המפתח נשאר ריק ב-appsettings — הערך האמיתי במשתני סביבה בלבד.
- **⚠️ הערה לתיאום (סוכנים מקבילים):** במקביל נחת שלב 10 (אבטחה/JWT) שהוסיף `FallbackPolicy` הדורש משתמש מזוהה בכל endpoint. משמעות: `/api/ai/ask` יידרש token ברגע שהאבטחה תיכנס לתוקף — וזה **תקין ורצוי** (חברת ועד מזוהה). לא הוספתי `[AllowAnonymous]`. חיבור הפרונט לשליחת token הוא באחריות שלב 10.
- **הצעד המומלץ הבא:** בעלת המוצר מוסיפה מפתח Anthropic (User Secrets בפיתוח, Railway Variables בייצור) ובודקת את העוזרת. בעתיד: לצרף לעוזרת רקע כספי כללי (מ-Dashboard) לשאלות "מצב כספי".

## 09.07.2026 — שלב 7 שדרוג: מודל עסקי לספקים (תמונות, וואטסאפ, רשתות חברתיות)

- **מה נעשה:** מימשתי את החלטת בעלת המוצר (09.07.2026) על מודל הספקים — ספקים בתשלום שהמנהלת מוסיפה ידנית (ערוץ הכנסה). זו התשובה לשאלה הפתוחה על "ספקים מומלצים". **שרת:** ל-`VendorProduct` נוסף `ImageUrl`, ל-`Vendor` נוסף `WhatsApp`, ונוספה ישות Owned חדשה `VendorSocialLink` (label + url) + Migration‏ `AddVendorContactAndImages`. `VendorService` מחליף גם את רשימת הרשתות החברתיות בעדכון (כמו המוצרים). **לקוח:** `VendorForm` הורחב (וואטסאפ, קישור תמונה לכל מוצר, ורשימת רשתות חברתיות דינמית), `VendorPanel` מציג תמונות מוצרים, כפתור וואטסאפ ירוק וצ'יפים לרשתות. נוצר עוזר `whatsapp.js` שבונה קישור wa.me ממספר טלפון (0 מוביל→972) או מקישור מלא. טסט חדש (5 במסך המתנות, 43 בסך הכל) + build ירוק. אומת מקצה-לקצה מול שרת רץ: POST עם וואטסאפ+תמונה+רשת חזר 201 ונשמר נכון, וספק ישן קיבל שדות ריקים (המיגרציה additive, לא שברה נתונים).
- **למה:** זו מטרת ההכנסה המרכזית של בעלת המוצר — ספקים משלמים כדי להיות מוצגים במערכת עם המוצרים, הוואטסאפ והרשתות שלהם.
- **קבצים:** שרת: `backend/Models/{Vendor,VendorProduct,VendorSocialLink}.cs`, `backend/DTOs/VendorDtos.cs`, `backend/Services/VendorService.cs`, `backend/AppDbContext.cs`, Migration `AddVendorContactAndImages`. לקוח: `src/pages/gifts/{VendorForm,VendorPanel}.js`, `src/services/whatsapp.js`, `src/pages/gifts/GiftsPage.test.js`, `src/styles/gifts.css`.
- **החלטות:** (1) וואטסאפ נשמר כקלט חופשי (טלפון או קישור); העוזר `whatsapp.js` מזהה ובונה wa.me — לא שוכפל מ-`paymentsService` כדי לא לגעת בקובץ של סוכן אחר, ניקוי-DRY עתידי אפשרי. (2) תמונות כ-URL בשלב זה (העלאת קובץ אמיתית תבוא עם תשתית ה-Drive, שלב 8) — מתועד ב-UI_SPEC ס' 12. (3) `VendorSocialLink` הוא Owned עם label חופשי (אינסטגרם/פייסבוק/אתר) — גמיש בלי לקבע רשתות בקוד.
- **הצעד הבא:** שלב 10 (אבטחה/כניסה) אופיין (UI_SPEC ס' 2: שם משתמש/סיסמה + Google, תוקף מנוי עד 30.8) — פתוח לביצוע ויפתח את הגבלת ניהול הספקים ל"מנהל-על"; שלב 8 (Drive) אופיין אך תלוי בשלב 10.

## 09.07.2026 — שלב 7 הושלם: מתנות וספקים (שרת + לקוח)

- **מה נעשה:** נבנה מסך המתנות והספקים לפי UI_SPEC ס' 12, שרת ולקוח. **שרת:** מודלים `Vendor` (עם `VendorProduct` כישות Owned) ו-`Gift` (שם, מזהה חג, תקציב, סטטוס planned/buying/done, קישור לספק) + Migration‏ `AddGiftsAndVendors`; שכבות מלאות — `GiftsController`/`VendorsController` דקים → `IGiftService`/`IVendorService`. Gift עובד דרך ה-Repository הגנרי (אין לו Owned); Vendor עובד דרך DbContext עם Include למוצרים (כמו GroupService). ולידציה בעברית, כולל הגבלת הסטטוס ל-3 הערכים בלבד. **לקוח:** `GiftsPage` — ספירה לאחור לחג הקרוב (מחושבת מלוח השנה העברי דרך `upcomingHoliday.js`, שנשען על `holidays.js` הקיים), רשימת מתנות עם סטטוס צבעוני ותקציב, טופס הוספה/עריכה, עוזרת תקציבית שמחשבת "נוצל X מתוך Y" מול תקציב החג, ודף ספק (מודאל) עם מוצרים וקטלוג. 4 טסטים חדשים (סה"כ 41 עוברים) + build ירוק.
- **למה:** מסך המתנות סוגר את הלולאה עם תקציבי החגים שהוגדרו בלוח השנה (שלב 6) — מה שתוקצב לחג מתחבר עכשיו למתנות בפועל דרך העוזרת התקציבית.
- **קבצים:** שרת: `backend/Models/{Vendor,VendorProduct,Gift}.cs`, `backend/DTOs/{GiftDtos,VendorDtos}.cs`, `backend/Services/{IGiftService,GiftService,IVendorService,VendorService}.cs`, `backend/Controllers/{GiftsController,VendorsController}.cs`, `backend/AppDbContext.cs`, `backend/Program.cs` (DI), Migration `AddGiftsAndVendors`. לקוח: `src/pages/GiftsPage.js`, `src/pages/gifts/` (CountdownBanner, GiftCard, GiftForm, BudgetAssistant, VendorPanel, VendorForm + טסטים), `src/services/{giftsService,vendorsService,upcomingHoliday,giftStatus}.js`, `src/styles/gifts.css`.
- **החלטות:** (1) **DRY:** מזהה החג ("שם|שנה עברית") זהה למזהה תקציב החג (`holidayBudgetKey`) ולחישוב החגים ב-`holidays.js` — שלושתם מדברים באותה שפה, בלי לשכפל לוגיקת תאריכים. (2) הספירה לאחור לא ממציאה תאריכים — נשענת על לוח השנה העברי המובנה בדפדפן. (3) המתנות והספקים עובדים גם בלי שרת (fallback ל-localStorage, כמו שאר המסכים) — רשומות מקומיות מסומנות `isLocal`. (4) `VendorProduct` הוא Owned — עריכת ספק מחליפה את כל רשימת המוצרים (RemoveRange + הוספה), פשוט ותואם לטופס.
- **⛔ שאלה לבעלת המוצר:** באפיון (UI_SPEC ס' 12) יש "ספקים מומלצים" עם דוגמה "מתנות בלב❓" — לא ברור אם זו רשימת המלצות קבועה של VaddyGo או סתם דוגמה. כרגע מימשתי שהספקים הם אלה שהמשתמשת מוסיפה בעצמה. אם רוצים רשימת המלצות מובנית — צריך להגדיר מאיפה היא מגיעה.
- **הצעד הבא:** שלב 8 (קבצים/Google Drive) ושלב 9 (עוזרת AI) פתוחים אך דורשים אפיון מדויק עם בעלת המוצר לפני ביצוע; שלב 10 (אבטחה/כניסה) פתוח לביצוע.

## 09.07.2026 — שלב 6 הושלם: צד-שרת ללוח השנה (EventsController) + חיבור הלקוח

- **מה נעשה:** נבנה צד-השרת של לוח השנה לפי הארכיטקטורה: מודל `Event` הורחב בשדה `Reminder` (bool) עם Migration‏ `AddEventReminder` (עמודה additive, ברירת מחדל false); `EventsController` דק → `IEventService`/`EventService` (BL) → `IRepository<Event>` גנרי (DAL), עם DTOs (`EventCreateDto`/`EventUpdateDto`/`EventResponseDto` על בסיס `EventWriteDto` משותף — DRY), ולידציה ב-DataAnnotations בעברית (שם חובה עד 100, תיאור עד 500, מיקום עד 100), ולוגים במזהים בלבד. נרשם ב-DI ב-Program.cs. בצד הלקוח `eventsService` חובר ל-`/api/events` עם נפילה חיננית ל-localStorage כשהשרת לא זמין. אומת מקצה-לקצה מול שרת רץ (POST עם reminder=true, GET, ולידציה 400 על שם חסר, DELETE). 10 טסטי לקוח + build עוברים.
- **למה:** משלים את שלב 6 — האירועים ייכתבו למסד האמיתי כשהבקאנד יפרס, בלי לשנות אף מסך. עד אז הלוח עובד מקומית בדפדפן.
- **קבצים:** backend: `Models/Event.cs`, `DTOs/EventDtos.cs`, `Services/IEventService.cs`, `Services/EventService.cs`, `Controllers/EventsController.cs`, `Program.cs`, `Migrations/*AddEventReminder*` + `AppDbContextModelSnapshot.cs`. frontend: `src/services/eventsService.js`.
- **החלטות:** (1) הפעלת השירות עקבית ל-StaffService/StudentService (אותה תבנית בדיוק — Repository גנרי, מיפוי DTO↔מודל, ApplyWrite משותף). (2) התאריך נשמר כתאריך בלבד (Date, בלי שעה) כמו ב-StaffMember. (3) נמצא שרת backend זומבי שרץ 9.3 שעות ונעל את ה-bin — נעצר (שרת פיתוח מקומי, SQLite נשמר לדיסק) כדי לאפשר build/migration. (4) תקציבי החגים (מהמשימה הקודמת) עדיין ב-localStorage — יחוברו ל-API כשייבנה endpoint לתקציבים.
- **הצעד המומלץ הבא:** פריסת הבקאנד ל-Railway (שלב 0, אצל בעלת המוצר) כדי שהאירועים והתשלומים יעבדו באתר החי; אחריו אפשר לחבר גם את תקציבי החגים ל-API.

## 09.07.2026 — שלב 5 הושלם: תשלומים (שרת + לקוח) + חיבור הדשבורד לנתונים אמיתיים

- **מה נעשה:** **בשרת** — מודל `Payment` (תלמיד, קטגוריה, סכום, אמצעי, שולם/לא, תאריך תשלום) המקושר ל-`CollectionCategory` הקיים; ‏Migration‏ `AddPayments` (FK ל-Students ול-CollectionCategories, מחיקה מדורגת); שכבות מלאות — `PaymentsController` דק מקונן תחת תלמיד (`GET/PUT api/students/{id}/payments[/{categoryId}]`) ← `IPaymentService`/`PaymentService`. ה-GET מסנתז שורה לכל קטגוריית גבייה של הגן (רשומה קיימת או ברירת-מחדל "טרם שולם"); ה-PUT הוא upsert. ולידציה (`IValidatableObject`): אמצעי בתוך bit/paybox/cash, אמצעי חובה כשמסמנים ששולם, סכום לא שלילי — הודעות בעברית. **בנוסף:** `DashboardService` חובר לתשלומים האמיתיים — הנגבה בפועל, הפירוק לפי אמצעי תשלום והפירוק לפי קטגוריה מחושבים כעת מהתשלומים ששולמו (השלמת ה-handoff שהושאר בשלב 4). **בלקוח** — `StudentPaymentsPage` בנתיב `/students/:id/payments` (נגיש מכפתור "תשלומים 💰" בכרטיס תלמיד): שורה לכל קטגוריה עם `PaymentRow` — בחירת אמצעי, סימון "שולם" ידני, סטטוס צבעוני ירוק, וביטול סימון; כפתור "שלחי תזכורת בוואטסאפ" שפותח `wa.me/<טלפון בפורמט בינלאומי>` עם הודעה מוכנה המפרטת את הקטגוריות שטרם שולמו והסכום הכולל. נוצר רכיב `paymentMethods` משותף (תוויות + אפשרויות בחירה) ו-`CollectionCard` עודכן להשתמש בו במקום מפה מקומית (DRY). 8 טסטים חדשים.
- **למה:** התשלומים הם ליבת המערכת — מכאן הוועד יודע מי שילם ומי לא, והדשבורד (שלב 4) סוף-סוף מציג נתונים אמיתיים במקום אפסים.
- **קבצים:** שרת: `backend/Models/Payment.cs`, `backend/DTOs/PaymentResponseDto.cs` + `PaymentUpsertDto.cs`, `backend/Services/IPaymentService.cs` + `PaymentService.cs`, `backend/Controllers/PaymentsController.cs`, `backend/Migrations/*AddPayments*`, `backend/AppDbContext.cs`, `backend/Program.cs`, `backend/Services/DashboardService.cs`. לקוח: `src/pages/StudentPaymentsPage.js` + טסט, `src/pages/payments/PaymentRow.js`, `src/services/paymentsService.js` + טסט, `src/services/paymentMethods.js`, `src/services/studentsService.js` (getStudent), `src/pages/StudentsPage.js` + טסט, `src/components/StudentCard.js`, `src/pages/home/CollectionCard.js`, `src/App.js`, `src/styles/payments.css`.
- **החלטות:** ‏(1) **`CollectionCategory` במקום `PaymentCategory` נפרד** (ה-ROADMAP הזכיר `PaymentCategory`, אך קטגוריות הגבייה כבר קיימות משלב 3 — שכפול היה מפר DRY). (2) הגן היחיד: הקטגוריות נלקחות מהגן הראשון, כמו ב-Dashboard. (3) MVP — רשומת תשלום אחת לכל (תלמיד, קטגוריה) עם שולם/לא; מעקב פר-תשלום (1/2/3) הוא הרחבה עתידית (מספר התשלומים כבר שמור בקטגוריה). (4) הבנייה, המיגרציה וה-smoke-test רצו בעותק מבודד ב-scratchpad כי שרת אחר (פורט 7017, VS/סוכן) נעל את ה-bin — נמנעה הפרעה; אומת מקצה לקצה (יצירה, סימון, ולידציה 400, דשבורד מציג נגבה=1200/24%, ביטול, 404).
- **הצעד המומלץ הבא:** צד השרת של שלב 6 (`EventsController` בשכבות + חיבור `eventsService` ל-API), שלב 7 (מתנות וספקים), או פריסת Railway (שלב 0) אצל בעלת המוצר כדי שהאתר החי יעבוד מול נתונים אמיתיים.

## 09.07.2026 — שלב 4 הושלם: מסך הבית — דשבורד מלא (שרת + לקוח)

- **מה נעשה:** נבנה מסך הבית לפי UI_SPEC ס' 8, שרת ולקוח. **שרת:** endpoint חדש `GET /api/dashboard` — כל החישובים ב-`DashboardService` (יעד גבייה, נגבה, יתרת קופה, חוב פתוח, פירוק לפי אמצעי תשלום ולפי קטגוריה, התראות, ימי הולדת קרובים); מודל `StaffMember` חדש עם CRUD מלא ב-`/api/staff` (שם, תפקיד, תאריך לידה — UI_SPEC ס' 8); נוסף שדה `Year` ל-`Group` עם כלל שנת-לימודים משותף (`SchoolYear.cs`) ו-Migration‏ `AddYearAndStaffMembers`. **לקוח:** `HomePage` חדש — כותרת "שם הגן + שנה עברית" (המרה אוטומטית: 2026→תשפ"ז), פעמון עם מונה התראות, כרטיס גבייה עם החלפה בין יעד ליתרה/חוב, בר התקדמות, פירוק ביט/פייבוקס/מזומן, תשלומים לפי קטגוריות, וימי הולדת של הצוות עם הוספה ועריכה במודאל. נוספו 4 טסטים (סה"כ 27 עוברים) + build ירוק.
- **למה:** זה המסך הראשון שבעלת המוצר רואה בכל כניסה — תמונת מצב של הוועד במבט אחד, לפי האפיון מהפנקס.
- **קבצים:** שרת: `backend/Models/StaffMember.cs`, `backend/Models/Group.cs` (Year), `backend/DTOs/DashboardResponseDto.cs`, `backend/DTOs/StaffMemberDtos.cs`, `backend/DTOs/GroupCreateDto.cs`+`GroupResponseDto.cs` (Year), `backend/Services/{IDashboardService,DashboardService,IStaffService,StaffService,SchoolYear}.cs`, `backend/Services/GroupService.cs`, `backend/Controllers/{DashboardController,StaffController}.cs`, `backend/AppDbContext.cs`, `backend/Program.cs` (DI), Migration חדשה. לקוח: `src/pages/HomePage.js`, `src/pages/home/` (CollectionCard, AlertsList, CategoryList, StaffBirthdays, StaffForm + טסטים), `src/services/{dashboardService,staffService,schoolYear,format}.js`, `src/styles/home.css`.
- **החלטות:** (1) הנגבה-בפועל ופירוק אמצעי התשלום מוחזרים כ-0 עד שמודל Payment ייבנה בשלב 5 — מבנה התשובה כבר סופי כדי ששלב 5 לא ישבור את הלקוח. (2) מסך הבית עובד גם בלי שרת: fallback מקומי שבונה את אותו מבנה מנתוני האשף (חשוב כל עוד הבקאנד לא פרוס). (3) אנשי צוות שנוצרו בלי שרת נשמרים ב-localStorage ויסונכרנו בעתיד; רשומות מקומיות מסומנות `isLocal`. (4) גנים שנוצרו לפני עמודת Year מקבלים את השנה הנוכחית (fallback בשרת). (5) תוקן באג מנוסחת הקריאה של האשף: `/groups` → `/api/groups` (הוסכם עם ממצא הסשן המקביל).
- **הצעד הבא:** שלב 5 (תשלומים) כבר נתפס — כשיושלם, לחבר את הנגבה-בפועל בדשבורד לנתוני אמת (ה-TODO מסומן ב-`DashboardService`). לבעלת המוצר: פריסת הבקאנד לפי DEPLOYMENT.md תדליק את הסנכרון המלא.

## 09.07.2026 — שלב 0: הבקאנד מוכן לפריסה ב-Railway (הצד הטכני)

- **מה נעשה:** הבקאנד הוכן לענן: `backend/Dockerfile` (בנייה דו-שלבית, ‎.NET 10) + `.dockerignore`; ‏`Program.cs` מאזין למשתנה `PORT` שמזריק Railway ומדלג על הפניית https בענן (Railway מסיים TLS); ל-CI נוסף job שבונה את הבקאנד על כל push — שרת שבור חוסם פריסה. ‏DEPLOYMENT.md הורחב במדריך צעד-אחר-צעד: שירות שני מהריפו (Root Directory=`backend`), ‏Volume קבוע ב-`/data` למסד ה-SQLite, משתני סביבה (`ConnectionStrings__Default`, ‏`Cors__AllowedOrigins__0`), דומיין על פורט 8080, וחיבור הפרונט עם `REACT_APP_API_URL`. אומת ש-dotnet build עובר אחרי השינויים.
- **למה:** בלי בקאנד באוויר האתר החי מציג רק את הודעת השגיאה; אחרי הפריסה כל המסכים יעבדו מול נתונים אמיתיים.
- **קבצים:** backend/Dockerfile (חדש), backend/.dockerignore (חדש), backend/Program.cs, ‏.github/workflows/ci.yml, DEPLOYMENT.md.
- **החלטות:** (1) Dockerfile ולא זיהוי אוטומטי — דטרמיניסטי ושקוף. (2) SQLite על Volume ב-`/data` — בלי Volume הנתונים נמחקים בכל פריסה; המעבר ל-SQL Server נשאר החלפת connection string (שלב 11). (3) `PORT=8080` כברירת מחדל בקונטיינר, תואם להוראת הדומיין במדריך.
- **הצעד המומלץ הבא:** בעלת המוצר מבצעת את פעולות הדשבורד לפי DEPLOYMENT.md (סעיף "פריסת הבקאנד"). אחר כך: שלב 4 (מסך הבית) או צד השרת של שלב 6 (EventsController).

## 09.07.2026 — לוח השנה: מבנה חדש + תקציב לכל חג (בקשת בעלת המוצר)

- **מה נעשה:** מסך לוח השנה אורגן מחדש לשלושה מדורים לפי הנחיית אביבית: (1) הלוח עם פאנל "הוספת אירוע" בצד (במסך רחב; בנייד — מתחת), (2) מדור **חגים** — שם ותאריך של כל חג בחודש, וליד כל חג כפתור תקציב שפותח חלון עם הנוסח המדויק: "שלום לך, לפני שנמשיך לנהל נכון בעזרת **VaddyGo** נא לציין את התקציב שייצא בחג זה" + שדה סכום בש"ח; הסכום מוצג ליד החג וניתן לעריכה, (3) מדור **האירועים שלי** עם מחיקה באישור. נוספו 3 טסטים (סה"כ 10 בלוח).
- **למה:** הנחיה ישירה מבעלת המוצר (09.07.2026), כולל תשובותיה לשאלות: כפתור ליד כל חג (לא חלון אוטומטי), סכום מוצג + ניתן לעריכה, רשימת האירועים נשארת.
- **קבצים:** `src/pages/CalendarPage.js`, `src/pages/calendar/{HolidaysSection,HolidayBudgetDialog}.js` (חדשים), `src/services/holidayBudgetsService.js` (חדש), `src/data/holidays.js` (מופעי חגים עם שנה עברית), `src/styles/calendar.css`, טסטים.
- **החלטות:** מזהה תקציב = שם החג + השנה העברית — כך חנוכה תשפ"ז הוא מופע אחד גם בדצמבר וגם בינואר. התקציבים נשמרים זמנית ב-localStorage (כמו האירועים) עם service אסינכרוני מוכן להחלפה ל-API; בשרת קיים מודל `Budget` שיוכל לשמש לזה. הסכומים ייצרכו את "העוזרת התקציבית" של מסך המתנות (שלב 7).
- **הצעד המומלץ הבא:** צד-שרת של שלב 6 (EventsController + API לתקציבי חגים) כשקבצי ה-backend יתפנו; מסך המתנות (שלב 7) יקרא את תקציבי החגים.

## 09.07.2026 — שלב 2 הושלם: מסך תלמידים מלא (לקוח + שרת)

- **מה נעשה:** **בשרת** — ‏`StudentsController` יושר לארכיטקטורה המחייבת: Controller דק ← `IStudentService` (BL) ← `IRepository<Student>` גנרי (DAL), עם שלושה DTOs (בסיס ולידציה משותף `StudentWriteDto`), ולידציה ב-DataAnnotations (חובה/אורך/פורמט טלפון 05X-XXXXXXX), ‏Middleware שגיאות מרכזי (לוג מלא, הודעה ידידותית בעברית ללקוח), ‏ILogger במזהים בלבד (בלי שמות/טלפונים), ומעבר מרשימה זמנית בזיכרון ל-SQLite האמיתי — מיגרציות רצות אוטומטית בעלייה, ו-connection string + CORS עברו ל-appsettings (פיתוח: localhost:3000 בלבד). **בלקוח** — ‏`StudentsPage` מלא: כרטיסים עם מונה "X תלמידים", חיפוש חופשי, סינון לפי כיתה, הוספה ועריכה באותו `StudentForm` במודאל (ולידציה זהה לשרת, כפתור נעול בשליחה), מחיקה עם `ConfirmDialog`. נוספו רכיבים גנריים: `Select`, ‏`ConfirmDialog`, ‏hook ‏`useForm`; ‏`api.js` מציג כעת את הודעת השגיאה שהשרת שלח. 7 טסטים חדשים + build ירוק. אומת מקצה לקצה מול שרת רץ (יצירה, ולידציה 400, עדכון, מחיקה 204).
- **למה:** מסך התלמידים הוא הבסיס לגבייה (שלב 5) — כל תלמיד עם טלפון הורה לתזכורות; והשכבות בשרת הן התבנית לכל ה-API-ים הבאים (שלב 3 שרת כבר נבנה עליהן).
- **קבצים:** שרת: `backend/DTOs/` (4), `backend/Repositories/` (2), `backend/Services/` (2), `backend/Middleware/ErrorHandlingMiddleware.cs`, `backend/Controllers/StudentsController.cs`, `backend/Program.cs`, `backend/appsettings.json`, `backend/Models/` (תיקוני nullable). לקוח: `src/pages/StudentsPage.js` + `StudentsPage.test.js`, `src/components/{StudentForm,StudentCard,ConfirmDialog,Select}.js`, `src/hooks/useForm.js`, `src/services/{studentsService,api}.js`, `src/styles/theme.css`.
- **החלטות:** ‏(1) טלפון נשמר מנורמל בלי מקף; הוולידציה מקבלת את שני הפורמטים — זהה בלקוח ובשרת. (2) ‏DELETE מחזיר 204; ‏NotFound מחזיר `{ message }` בעברית ש-api.js יודע להציג. (3) שדה `Grade` נשאר במודל אך לא נחשף ב-DTOs — החלוקה לקבוצות מוסדרת בקשר Student→Group (שלב 3 שרת). (4) תאריך לידה ואופן תשלום מ-UI_SPEC ס' 11 יתווספו בשלבים 3/5 (תלויים בקטגוריות גבייה) — לא הומצאו שדות. (5) הלוגים באנגלית ובמזהים בלבד — פרטי ילדים והורים לא נכתבים ללוג.
- **הצעד המומלץ הבא:** שלב 4 (מסך הבית) או שלב 5 (תשלומים) — שניהם פתוחים; וחיבור Railway (שלב 0) אצל בעלת המוצר.

## 09.07.2026 — שלב 3 הושלם: Group API בשרת + חיבור אשף ההרשמה

- **מה נעשה:** נבנה צד השרת של שלב 3 לפי הארכיטקטורה: מודלים `Group` ו-`CollectionCategory` (+ קשר אופציונלי Student→Group), Migration‏ `AddGroupAndCollectionCategories`, שכבות מלאות (`GroupsController` דק → `IGroupService`/`GroupService`) עם DTOs וולידציה בעברית. יעד הגבייה מחושב בשרת. `onboardingService` בפרונט חובר ל-`POST /api/groups` עם נפילה חיננית לשמירה מקומית כשהשרת לא זמין (כפתור "כניסה לאפליקציה" ננעל בזמן השמירה). אומת מקצה לקצה מול שרת רץ: יצירה, שליפה, ושגיאות ולידציה. 23 טסטים עוברים, build ירוק.
- **למה:** משלים את אשף ההרשמה — הנתונים שהמשתמשת מזינה נשמרים עכשיו במסד האמיתי.
- **קבצים:** backend: Models/Group.cs, Models/CollectionCategory.cs, Models/Student.cs, AppDbContext.cs, Program.cs, DTOs/GroupCreateDto.cs, DTOs/GroupResponseDto.cs, Services/IGroupService.cs, Services/GroupService.cs, Controllers/GroupsController.cs, Migrations/*AddGroupAndCollectionCategories*; frontend: src/services/onboardingService.js, src/pages/onboarding/OnboardingWizard.js + טסט.
- **החלטות:** (1) `GroupService` ניגש ל-DbContext ישירות (לא דרך ה-Repository הגנרי) כי גן נטען תמיד עם הקטגוריות שלו (Include) — חריגה מתועדת. (2) קבוצות הגן נשמרות כמחרוזת מופרדת בפסיקים (KISS) — ינורמלו לטבלה אם יקבלו התנהגות. (3) ועדים נוספים (ריבוי ועדים) נשארו מקומיים בלבד — ממתין לתשובת בעלת המוצר (שאלה 3) לפני עיצוב רב-ועדים. (4) שם קטגוריה נשלח לשרת בלי הסיומת "(אופציונלי)".
- **הצעד המומלץ הבא:** שלב 0 — פריסת הבקאנד ל-Railway (+ `REACT_APP_API_URL` ו-CORS לדומיין הייצור), כדי שהאתר החי יעבוד מול נתונים אמיתיים. לחלופין: צד שרת של שלב 6 (EventsController) שהתפנה, או שלב 4 (מסך הבית).

## 08.07.2026 — שלב 6 (צד לקוח): לוח שנה עברי עם חגים ואירועים

- **מה נעשה:** נבנה `CalendarPage` מלא — תצוגת חודש בעברית (רשת ראשון-שבת, ניווט בין חודשים, כותרת לועזית + טווח חודשים עבריים), חגי ישראל מסומנים בלוח וברשימה חודשית, הוספת אירוע במודאל (שם, תאריך, מיקום, תיאור, תזכורת 🔔) עם ולידציה וכפתור נעול, ומחיקת אירוע עם מודאל אישור. נוספו 7 טסטים (כולל אימות שחנוכה 2026 וראש השנה 2026 מחושבים נכון).
- **למה:** משימת צד-הלקוח של שלב 6. שלבים 2-3 היו תפוסים על ידי סוכנים מקבילים, ושלב 6 הוא הבלתי-תלוי הראשון שהיה פנוי.
- **קבצים:** `src/pages/CalendarPage.js` (הוחלף מ-placeholder), `src/pages/calendar/` (MonthGrid, EventForm, טסטים), `src/data/holidays.js`, `src/services/eventsService.js`, `src/styles/calendar.css`, `src/components/Checkbox.js` (גנרי חדש).
- **החלטות:** (1) **חגים בלי להמציא תאריכים:** כל חג מוגדר לפי התאריך העברי הקבוע שלו (עובדה), וההמרה ללועזי נעשית במנוע התאריכים המובנה בדפדפן (Intl בלוח עברי) — מדויק לכל שנה, כולל כללי ההזזה החוקיים של יום העצמאות/הזיכרון. עדיף על רשימה מוקלדת ידנית שעלולה לטעות. (2) **אין עדיין `/api/events` בשרת** — האירועים נשמרים זמנית ב-localStorage דרך `eventsService` עם אותם שדות כמו מודל `Event` בשרת; כשצד-השרת ייבנה מחליפים רק את גוף הפונקציות ב-service, בלי לגעת במסכים. (3) צד-השרת של שלב 6 ממתין בכוונה — הסוכן של שלב 2 עורך כרגע את קבצי ה-backend, ועבודה מקבילה שם תיצור התנגשויות.
- **הצעד המומלץ הבא:** כשקבצי ה-backend יתפנו — משימת צד-השרת של שלב 6 (EventsController בשכבות + שדה Reminder + Migration) וחיבור `eventsService` ל-API האמיתי.

## 08.07.2026 — שלב 3 (צד לקוח): מסכי פתיחה, כניסה ואשף הרשמה

- **מה נעשה:** נבנו כל מסכי הכניסה לפי UI_SPEC ס' 1-6: `WelcomePage` (נוסח הפתיחה המדויק מהפנקס + שני כפתורים), `LoginPage` (מייל, סיסמה עם עין — UI בלבד), ואשף `OnboardingWizard` ב-5 צעדים (פרטי גן → קבוצות גן/בי"ס → ועדים נוספים 1-4 → הגדרת גבייה ב-4 קטגוריות עם 1/2/3 תשלומים וחישוב יעד אוטומטי → סיכום "הכל מוכן!"). כניסה ראשונה ל-`/` מפנה ל-`/welcome`; אחרי סיום האשף נכנסים לאפליקציה. נוספו 3 טסטים (סה"כ 9 עוברים) + build ירוק.
- **למה:** אלה המסכים הראשונים שכל משתמשת חדשה פוגשת, לפי האפיון מהפנקס.
- **קבצים:** src/pages/WelcomePage.js, src/pages/LoginPage.js, src/pages/onboarding/ (OnboardingWizard, GanDetailsStep, GroupsStep, CommitteesStep, CollectionStep, SummaryStep + טסט), src/services/onboardingService.js, src/styles/onboarding.css, src/App.js (נתיבים + הסתרת ניווט במסכי פתיחה), src/App.test.js (סימון onboarding בטסטים).
- **החלטות:** נתוני האשף נשמרים ב-localStorage דרך onboardingService עד שיהיה Group API — ההחלפה תהיה בשירות בלבד, בלי לגעת במסכים. סכומי הפנקס (1,200/500/400/125) הם placeholders, לא ערכי ברירת מחדל. לא מוצגת הודעת "נשלחה הזמנה ל-X משתמשים" כי הזמנות עוד לא באמת נשלחות. עיר בהקלדה חופשית עד שבעלת המוצר תאשר מקור לרשימת יישובים. פריטי ❓ נשארו בקובץ השאלות.
- **הצעד המומלץ הבא:** צד השרת של שלב 3 (מודל Group + CollectionCategory + חיבור onboardingService ל-API) — כדאי על בסיס הארכיטקטורה החדשה שיושרה בבקאנד. במקביל: פריסת הבקאנד ל-Railway (שלב 0).

## 08.07.2026 — הבקאנד הוכנס לריפו (mono-repo, תיקיית `backend/`)

- **מה נעשה:** לפי החלטת בעלת המוצר לעבוד ב-**mono-repo** (הכל בריפו אחד), קוד השרת `ParentCommitteeAPI` הוכנס לתיקיית `backend/` בתוך ריפו `vaddygo`, נדחף ל-main. הועברו **רק קבצי המקור שגיט עוקב אחריהם** (18 קבצים) באמצעות `git archive HEAD` → חילוץ ל-`backend/` — כך שקבצי `bin/`, `obj/`, ו-SQLite (`*.db`) לא נכנסו. `backend/.gitignore` נשמר (מחריג build/db/secrets גם בתת-התיקייה).
- **למה:** בעלת המוצר בחרה mono-repo כשהתבקשה להכריע (הכתובת שנתנה הייתה הריפו הקיים של הפרונט; הובהר שדחיפת הבקאנד לשם הייתה דורסת את הפרונט, ולכן במקום זה — תיקיית משנה). זה מבטל את הצורך בריפו GitHub שני ובאישור ליצירתו.
- **קבצים:** `backend/` (18 קבצי מקור: Program.cs, AppDbContext.cs, Controllers, Models, Migrations, appsettings*, launchSettings, csproj, slnx, .gitignore), `ARCHITECTURE.md`, `ROADMAP.md`, `PROJECT_LOG.md`.
- **החלטות:** מקור האמת לקוד השרת הוא מעתה `backend/` בריפו. העותק ב-`C:\Vaddygo\ParentCommitteeAPI` הוא עותק פיתוח מקומי ישן — כדי למנוע שני עותקים שמתפצלים, יש לפתוח/לערוך את השרת מ-`backend/`. ה-`git init` המקומי שנוצר קודם ב-`C:\Vaddygo` הוסר (מיותר ב-mono-repo). לא נגעתי בקוד השרת עצמו — יישורו לארכיטקטורה (Service/Repository/DTOs) הוא שלב 2.
- **הצעד המומלץ הבא:** שלב 2 — מסך תלמידים מלא (לקוח, לפי UI_SPEC ס' 11) + יישור `StudentsController` בבקאנד לארכיטקטורה (Controller דק → `IStudentService` → `IRepository<Student>` + DTOs + ולידציה + Middleware). בנוסף: פריסת הבקאנד ל-Railway מתיקיית `backend/` + עדכון `REACT_APP_API_URL`.

## 08.07.2026 — אפיון מסכים מלא מהפנקס + פונט מותג Rubik

- **מה נעשה:** בעלת המוצר שלחה 11 צילומי פנקס עם אפיון כל המסכים. נוצר UI_SPEC.md — תרגום מלא ומחייב של האפיון (פתיחה, כניסה, אשף הרשמה, הגדרת גבייה, מסך בית, צוות, התראות, תלמידים, מתנות, קבצים, עוזרת AI). הוחל פונט מותג Rubik בכל המערכת (index.html + theme.css). ROADMAP עודכן (שלב 3 הורחב למסכי פתיחה/הרשמה/גבייה; שלבים 4, 7 פורטו). נוצר קובץ שאלות לבעלת המוצר: שולחן העבודה → "VaddyGo - שאלות אפיון.doc".
- **למה:** האפיון בכתב יד הוא מקור האמת למוצר; בעלת המוצר ביקשה שינוי פונט מיידי בגרסה המפורסמת ושהאפיון ייכנס למשימות הסוכנים.
- **קבצים:** UI_SPEC.md (חדש), public/index.html, src/styles/theme.css, ROADMAP.md, AGENT_PROMPT.md, PROJECT_LOG.md
- **החלטות ארכיטקטוניות:** Rubik נבחר כפונט מותג (ממתין לאישור סופי — שאלה 1 בקובץ השאלות); כל משימת UI מחויבת לקרוא את UI_SPEC.md; פריטי ❓ ב-UI_SPEC לא מממשים לפני תשובת בעלת המוצר.
- **הצעד המומלץ הבא:** כשבעלת המוצר עונה על קובץ השאלות — לעדכן את UI_SPEC.md ולהסיר ❓. משימת הפיתוח הבאה: שלב 2 (מסך תלמידים מלא) לפי UI_SPEC ס' 11.

## 08.07.2026 — שלב 0: הכנת הבקאנד להעלאה לריפו נפרד (בדיקת secrets + git מקומי)

- **מה נעשה:** הוכנה תיקיית הבקאנד `C:\Vaddygo\ParentCommitteeAPI` להעלאה לריפו GitHub נפרד. (1) אומת ידנית שאין secrets: `appsettings.json` ו-`appsettings.Development.json` מכילים רק הגדרות Logging + AllowedHosts; `launchSettings.json` רק פורטים; ה-connection string ב-`Program.cs` הוא SQLite מקומי בלי סיסמה. (2) נוצר `.gitignore` תקני ל-.NET שמחריג `bin/`, `obj/`, `*.user`, קבצי SQLite (`*.db`, `-shm`, `-wal`) וכל `.env`/secrets. (3) בוצע `git init` + קומיט ראשון מקומי (ענף `main`, 18 קבצי מקור בלבד — קבצי ה-DB והבנייה לא נכנסו, אומת).
- **למה:** משימת שלב 0 ב-ROADMAP. הכנה מקומית מלאה מקטינה את הצעד החיצוני (יצירת ריפו + דחיפה) לפעולה קטנה ומלווה לבעלת המוצר, ומבטיחה שסודות לא יעלו לענן.
- **קבצים:** `C:\Vaddygo\ParentCommitteeAPI\.gitignore` (חדש) + git מקומי חדש שם; בריפו הפרונט: `ROADMAP.md`, `PROJECT_LOG.md`.
- **החלטות:** לא בוצעה יצירת ריפו GitHub / דחיפה / פריסת Railway — אלה פעולות חיצוניות שדורשות אישור בעלת המוצר. `WeatherForecast.cs`/הבקר שלו (שאריות תבנית) הושארו כפי שהם — הסרתם היא שינוי קוד שיטופל ביישור הבקאנד בשלב 2.
- **הצעד המומלץ הבא:** בעלת המוצר: ליצור ריפו GitHub פרטי לבקאנד ולדחוף (`git remote add origin <URL>` → `git push -u origin main`). לאחר מכן — שלב 2: יישור `StudentsController` לארכיטקטורה (Controller דק → `IStudentService` → `IRepository<Student>` + DTOs + ולידציה + Middleware שגיאות).

## 07.07.2026 — שלב 1 הושלם: תשתית פרונטאנד מלאה

- **מה נעשה:** נבנתה כל תשתית הפרונטאנד לפי ARCHITECTURE.md: מבנה תיקיות מלא (`components/ pages/ services/ hooks/ styles/`), שכבת API יחידה (`services/api.js` — כתובת מ-`REACT_APP_API_URL`, שגיאות אחידות בעברית), hook גנרי `useApi` (טעינה/שגיאה/נתונים), ערכת עיצוב `styles/theme.css` (סגול `#7C3AED`, RTL גלובלי, פינות מעוגלות, אזורי מגע 44px), 9 רכיבי בסיס (Button, Input, Card, Modal, Table, Spinner, EmptyState, ErrorMessage, BrandName), ניווט תחתון `BottomNav` עם React Router v6 וחמישה עמודים (בית, תלמידים, לוח שנה, מתנות, קבצים). מסך התלמידים הקיים הועבר ל-`StudentsPage` עם שלושת המצבים המחייבים. נוספו 6 טסטים. `index.html` עודכן ל-`lang="he" dir="rtl"` וכותרת VaddyGo.
- **למה:** זהו הבסיס שכל המסכים הבאים ייבנו עליו — בלי לשכפל עיצוב או לוגיקת רשת.
- **קבצים:** `src/components/` (10 קבצים), `src/pages/` (5), `src/services/` (2), `src/hooks/useApi.js`, `src/styles/theme.css`, `src/App.js`, `src/index.js`, `src/App.test.js`, `public/index.html`, `.env.development`, `.env.test`, `package.json`. נמחקו קבצי CRA מתים: `App.css`, `index.css`, `logo.svg`.
- **החלטות:** React Router **v6** (ולא v7) — יציב עם Create React App, עם דגלי future של v7 כדי שלא יהיו אזהרות והמעבר העתידי יהיה קל. כתובת השרת בפיתוח יושבת ב-`.env.development` (לא סוד); בייצור תוגדר ב-Railway Variables כשהבקאנד יעלה (שלב 0). פקודת ההגשה `npx serve -s build` כבר תומכת בניתוב צד-לקוח.
- **הצעד המומלץ הבא:** שלב 2 — מסך תלמידים מלא (טופס הוספה/עריכה, מחיקה, חיפוש) + יישור הבקאנד לארכיטקטורה. במקביל, בעלת המוצר יכולה להשלים את חיבור Railway לפי DEPLOYMENT.md כדי שהאתר יהיה באוויר.

## 07.07.2026 — כלל עיצוב חדש: השם VaddyGo תמיד מודגש ב-UI

- **מה נעשה:** נוסף כלל UI/UX ל-ARCHITECTURE.md: השם **VaddyGo** מוצג תמיד מודגש (bold) בכל מקום שהוא מופיע ב-UI.
- **למה:** הנחיה של בעלת המוצר (אביבית) — חיזוק המותג בכל המסכים.
- **קבצים:** ARCHITECTURE.md, PROJECT_LOG.md
- **החלטות:** לא נדרש שינוי קוד כרגע — המופע היחיד היום הוא ב-`<h1>` ב-App.js, שכבר מודגש כברירת מחדל. הכלל מחייב מכאן והלאה כל מסך חדש.
- **הצעד המומלץ הבא:** בשלב 1 של ה-ROADMAP (ערכת עיצוב), לעגן את הכלל ברכיב גנרי (למשל `<BrandName />`) כדי שלא יישכח.

## 07.07.2026 — תשתית: GitHub, תיעוד מלא, CI

- **מה נעשה:** הפרויקט נדחף לראשונה ל-GitHub‏ (https://github.com/avivit-viskin/vaddygo). נוצר מערך תיעוד מלא (README, ARCHITECTURE, AI_RULES, ROADMAP, PROJECT_LOG, DEPLOYMENT, AGENT_PROMPT). הוקם CI ב-GitHub Actions שמריץ בדיקות ו-build על כל push. תוקן הטסט הדיפולטי של CRA שנכשל. נוספה חבילת `serve` להגשת ה-build ב-Railway.
- **למה:** לפי האפיון, PROJECT_LOG הוא הזיכרון של הפרויקט וכל סוכן מתחיל ממנו; CI מבטיח שכל דחיפה ל-main נבדקת לפני פריסה אוטומטית ל-Railway.
- **קבצים:** README.md, AGENT_PROMPT.md, ARCHITECTURE.md, AI_RULES.md, ROADMAP.md, PROJECT_LOG.md, DEPLOYMENT.md, ‎.github/workflows/ci.yml, src/App.test.js, package.json, ‎.gitignore
- **החלטות ארכיטקטוניות:** עבודה ישירה על main (אישור קבוע מבעלת המוצר) עם CI כשער איכות; Railway ב-Wait for CI כך שפריסה קורית רק אחרי בדיקות ירוקות; כתובת ה-API תעבור ל-`REACT_APP_API_URL` בשלב 1.
- **הצעד המומלץ הבא:** בעלת המוצר מחברת את הריפו ל-Railway ומפיקה דומיין לפי DEPLOYMENT.md. אחר כך: שלב 1 ב-ROADMAP — מבנה תיקיות + api.js + רכיבי בסיס.

## 07.07.2026 — קומיט ראשון ודחיפה ל-GitHub

- **מה נעשה:** קומיט ראשון של שלד CRA עם מסך רשימת תלמידים (App.js קורא ל-GET /api/students). תוקן remote שגוי, הוצא settings.local.json מהריפו.
- **למה:** נקודת פתיחה מגובה לפרויקט.
- **קבצים:** כל קבצי הפרויקט הראשוניים.
- **הצעד המומלץ הבא:** בוצע — ראו הרשומה שמעל.
