# ROADMAP.md — מסמך המשימות של החזון

זהו **קובץ המשימות שכל סוכן AI עובד לפיו**. כל שלב מפורט בשתי רמות: **צד לקוח (UI)** ו**צד שרת (API)**. כשאין משימה ספציפית — לוקחים את השלב הפתוח הראשון לפי הסדר. אחרי כל משימה שהושלמה מעדכנים כאן ☐ → ✅.

**מקרא:** ✅ הושלם · 🔄 בתהליך `[מי, מתי]` · ☐ פתוח

**עבודה במקביל:** התפיסה היא ברמת **שלב שלם** — כל סוכן תופס שלב אחד (מסמן 🔄 עם שמו והתאריך בכותרת השלב, commit + push מיד) ועובד רק על משימות מתוכו. שלב עם 🔄 של סוכן אחר — לא נוגעים בו ולא בקבצים שלו. הפרוטוקול המלא ב-[AGENT_PROMPT.md](AGENT_PROMPT.md).

**רקע:** מסמכי החזון המלאים נמצאים אצל בעלת המוצר; המסמך הזה הוא התרגום המבצעי שלהם. **לכל משימת UI חובה לקרוא קודם את [UI_SPEC.md](UI_SPEC.md)** — אפיון המסכים המלא מהפנקס של בעלת המוצר (מסכים, נוסחים, שדות). שאלה על כוונת המוצר? שואלים את בעלת המוצר, לא מנחשים.

**עקרון-על מחייב (09.07.2026):** VaddyGo היא פלטפורמה **גנרית לכל ארגון** (גן/ועד בית/משרד/מחלקה/בית חולים/בנק…), והגן הוא Vertical #1 בלבד. בונים תחילה עבור הגן, אך כל ישות/מסך נבנים גנריים עם תוויות לפי סוג הארגון. הפירוט המלא ב-[ARCHITECTURE.md](ARCHITECTURE.md) → "עקרון-על: מערכת גנרית לכל ארגון". המרה מלאה לרב-ורטיקלי (בחירת סוג ארגון + מפת תוויות) = שלב עתידי ייעודי.

---

## שלב 0 — תשתית פרויקט ✅

- ✅ ריפו Git + דחיפה ל-GitHub‏ (https://github.com/avivit-viskin/vaddygo)
- ✅ מערך תיעוד מלא בריפו + פרומפט גנרי לסוכנים
- ✅ CI ב-GitHub Actions: בדיקות + build על כל push
- ✅ **המערכת באוויר מקצה-לקצה** [בעלת המוצר עם ליווי, 09.07.2026]: פרויקט Railway אחד `superb-hope` עם שני שירותים — **הפרונט** (`vaddygo`, כתובת `https://vaddygo-production.up.railway.app`) ו-**הבקאנד** (`soothing-clarity`, כתובת `https://soothing-clarity-production.up.railway.app`, Root Directory=`backend`, Builder=Dockerfile). ‏Volume ב-`/data`; משתנים: `ConnectionStrings__Default=Data Source=/data/vaadygo.db`, `Cors__AllowedOrigins__0`=כתובת הפרונט, `Jwt__Key` הוגדר. הפרונט מחובר דרך `REACT_APP_API_URL`=כתובת הבקאנד. אומת: `/api/health` מחזיר `database:ok`, והרשמה+הוספת תלמיד נשמרים ומשׂרדים ריענון (נתונים אמיתיים בשרת). ניקוי: נמחקו 5 פרויקטי Railway כפולים שנוצרו בטעות.
- ✅ הבקאנד עלה לריפו `[Claude Code, 08.07.2026]` — **החלטת בעלת המוצר: mono-repo** (הכל בריפו אחד). קוד השרת נמצא כעת בתיקיית `backend/` בתוך ריפו `vaddygo`. אומת שאין secrets (appsettings.json / appsettings.Development.json / launchSettings.json); `backend/.gitignore` מחריג bin/obj, `*.user`, קבצי SQLite `*.db`, env/secrets. הועברו קבצי מקור בלבד (18) — לא bin/obj/db.

## שלב 1 — תשתית פרונטאנד ✅

**צד לקוח:**
- ✅ מבנה תיקיות: `components/` `pages/` `services/` `hooks/` `styles/` — כל קומפוננטה בקובץ נפרד
- ✅ `services/api.js` יחיד: כתובת בסיס מ-`REACT_APP_API_URL`, פונקציות get/post/put/del, טיפול שגיאות אחיד
- ✅ `styles/theme.css`: משתני צבע (ראשי `#7C3AED`, רקעים בהירים), טיפוגרפיה, RTL גלובלי, פינות מעוגלות
- ✅ רכיבי בסיס גנריים: `Button`, `Input` (עם תווית ושגיאה ליד השדה), `Card`, `Modal`, `Table`, `Spinner`, `EmptyState`, `ErrorMessage`, `BrandName` (השם **VaddyGo** תמיד מודגש)
- ✅ React Router + `BottomNav` — ניווט תחתון קבוע (בית, תלמידים, לוח שנה, מתנות, קבצים) עם אזורי מגע 44px+
- ✅ פונט מותג Rubik בכל המערכת (Google Fonts, נטען ב-index.html, מוגדר ב-theme.css)

**צד שרת:** אין — שלב זה כולו בלקוח.

## שלב 2 — מסך תלמידים מלא ✅

**צד לקוח:**
- ✅ `StudentsPage`: רשימת תלמידים בכרטיסים (שם מלא, כיתה/קבוצה, טלפון הורה לחיץ לחיוג) + מונה "X תלמידים" (UI_SPEC ס' 11), עם מצבי טעינה/שגיאה/ריק ("עדיין אין תלמידים — הוסיפי את הראשונה!")
- ✅ טופס הוספה (`StudentForm` במודאל): שם פרטי, שם משפחה, כיתה, טלפון הורה — ולידציה בלקוח (שדות חובה, פורמט טלפון ישראלי 05X-XXXXXXX), כפתור נעול בזמן שליחה, שגיאת שרת מוצגת בתוך הטופס
- ✅ עריכה (אותו טופס במצב עריכה) ומחיקה עם מודאל אישור ("למחוק את דנה כהן? אי אפשר לבטל") — `ConfirmDialog` גנרי לכל מחיקה עתידית
- ✅ חיפוש חופשי + סינון לפי כיתה (רכיב `Select` גנרי חדש + hook ‏`useForm` גנרי לכל הטפסים)

**צד שרת (`ParentCommitteeAPI`):**
- ✅ יישור `StudentsController` הקיים לארכיטקטורה: Controller דק → `IStudentService` → `IRepository<Student>`, עם DTOs‏ (`StudentCreateDto`, `StudentUpdateDto`, `StudentResponseDto`) + מעבר מרשימה בזיכרון למסד SQLite האמיתי (מיגרציות רצות אוטומטית בעלייה)
- ✅ ולידציה בשרת (DataAnnotations: שדות חובה, אורך, פורמט טלפון) + Middleware שגיאות מרכזי + ILogger

## שלב 3 — פתיחה, הרשמה והגדרת גן וגבייה ✅ (אפיון מלא: UI_SPEC סעיפים 1-7)

**צד לקוח:** ✅ הושלם [Claude Fable, 08.07.2026]
- ✅ מסך פתיחה (Welcome) עם נוסח הפתיחה מהאפיון + "שנתחיל?" / "כבר יש לי חשבון — כניסה" (UI_SPEC ס' 1). כניסה ראשונה ל-`/` מפנה אוטומטית ל-`/welcome`.
- ✅ מסך כניסה: מייל, סיסמה עם עין — UI בלבד; "שכחתי סיסמה" ו"כניסה עם Google" מסומנים "יופעלו בשלב האבטחה" (UI_SPEC ס' 2)
- ✅ אשף הרשמה ב-5 צעדים: פרטי גן (עיר בהקלדה חופשית עד לאישור מקור רשימת יישובים) → קבוצות (גן/בי"ס) → ועדים נוספים (1-4) → גבייה → סיכום (UI_SPEC ס' 3-6)
- ✅ מסך הגדרת גבייה: 4 הקטגוריות מהאפיון, סכומי הפנקס כ-placeholders, חלוקה ל-1/2/3 תשלומים, סה"כ לתלמיד ויעד כולל מחושבים אוטומטית
- ✅ האשף מחובר לשרת: `onboardingService` שולח ל-`POST /api/groups`; אם השרת לא זמין — נשמר מקומית (`syncedWithServer=false`) וממשיכים

**צד שרת:** ✅ הושלם [Claude Fable, 08-09.07.2026]
- ✅ מודלים `Group` (שם, עיר, מס' ילדים, צוות, קבוצות) + `CollectionCategory` (שם, סכום לתלמיד, 1-3 תשלומים) + Migration‏ `AddGroupAndCollectionCategories` + קשר Student→Group (אופציונלי)
- ✅ שכבות מלאות: `GroupsController` דק → `IGroupService`/`GroupService` → EF, עם DTOs וולידציה בעברית; יעד הגבייה מחושב בשרת (סה"כ לתלמיד × ילדים)
- ✅ אומת מקצה לקצה: POST יצר גן עם קטגוריות (יעד 37,400 ₪ ל-22 ילדים), GET מחזיר, ולידציה מחזירה 400 עם הודעות בעברית

## שלב 4 — מסך הבית ✅ [Claude Fable B, 09.07.2026]

**צד שרת:** ✅ `GET /api/dashboard` — הסיכום כולו מחושב ב-`DashboardService` (יעד, נגבה, יתרה/חוב, פירוק לפי אמצעי תשלום וקטגוריה, התראות, ימי הולדת). מודל `StaffMember` + CRUD ב-`/api/staff`. נוסף `Year` ל-Group (כלל `SchoolYear` משותף) + Migration. ⏳ הנגבה-בפועל ופירוק האמצעים יתמלאו כשמודל Payment ייבנה (שלב 5) — מבנה התשובה כבר סופי.
**צד לקוח:** ✅ `HomePage` לפי UI_SPEC ס' 8: שם גן + שנה עברית (2026→תשפ"ז), פעמון עם מונה, כרטיס גבייה עם החלפה יעד↔יתרה/חוב, בר התקדמות, פירוק ביט/פייבוקס/מזומן, התראות, תשלומים לפי קטגוריות, וימי הולדת של הצוות (רשימה, הוספה, עריכה). עובד גם בלי שרת — fallback מקומי מנתוני האשף. 4 טסטים חדשים.

## שלב 5 — תשלומים ✅ [Claude Code, 09.07.2026]

**צד שרת:** ✅ מודל `Payment` (תלמיד, קטגוריה, סכום, אמצעי ביט/פייבוקס/מזומן, שולם/לא, תאריך תשלום) + Migration‏ `AddPayments` (מפתחות זרים ל-Students ול-CollectionCategories) + שכבות מלאות (`PaymentsController` דק → `IPaymentService`/`PaymentService`). **החלטה (DRY): שימוש ב-`CollectionCategory` הקיים משלב 3 במקום `PaymentCategory` נפרד** — אותן קטגוריות הן קטגוריות התשלום. `GET api/students/{id}/payments` מחזיר שורה לכל קטגוריה (קיימת או ברירת-מחדל "טרם שולם"); `PUT api/students/{id}/payments/{categoryId}` מסמן/מעדכן. ולידציה: אמצעי חובה כשמסמנים ששולם. **בונוס:** `DashboardService` חובר לתשלומים האמיתיים — הנגבה, פירוק האמצעים והקטגוריות מחושבים מהתשלומים ששולמו (השלמת ה-handoff משלב 4). הארכיטקטורה משאירה נקודת הרחבה לספק סליקה עתידי.
**צד לקוח:** ✅ `StudentPaymentsPage` (‏`/students/:id/payments`, נגיש בכפתור "תשלומים 💰" בכרטיס תלמיד): שורה לכל קטגוריה עם סימון ידני "שולם", סטטוס צבעוני (ירוק=שולם), בחירת אמצעי, וכפתור "שלחי תזכורת בוואטסאפ" שפותח `wa.me/<טלפון>` עם הודעה מוכנה המפרטת את הקטגוריות שטרם שולמו — בלי סליקה בתוך המערכת. רכיב `paymentMethods` משותף (תוויות ביט/פייבוקס/מזומן), ‏`CollectionCard` עודכן להשתמש בו. 8 טסטים חדשים.
- ✅ **בקשת תשלום מכרטיס התלמיד** (בקשת בעלת המוצר, 09.07.2026): כפתור "בקשת תשלום 💸" בכל כרטיס תלמיד → חלון עם החוב הפתוח ושלושה אמצעים. ביט/פייבוקס פותחים וואטסאפ להורה עם הסכום + **קישור התשלום הקבוע של הוועד** (המשתמשת מדביקה אותו פעם אחת — לא ממציאים קישורים); מזומן שולח תזכורת בלבד. רכיב עצמאי `PaymentRequestButton` (בלי לגעת ב-StudentsPage — נמנעה התנגשות עם סוכן מקביל). ⏳ קישורי הוועד נשמרים בדפדפן (localStorage) עד שיהיה שדה במודל Group.
- ✅ **תזכורת גורפת לכל החייבים** (בקשת בעלת המוצר, 09.07.2026): כפתור "📣 תזכורת לחייבים" ליד כותרת מסך התלמידים → חלון עם הודעה גנרית ניתנת לעריכה ורשימת כל ההורים שטרם שילמו, כפתור וואטסאפ אישי לכל אחד (וואטסאפ לא שולח לרבים בקישור אחד). רכיב עצמאי `BulkReminderButton`; נשען על סיכומי התשלום ש-StudentsPage כבר טוען. ✅ מעקב "נשלח": לחיצה על "שליחה" מסמנת את ההורה (רקע ירוק + ✅) ומעדכנת מונה "נשלחו X מתוך Y" — כדי לא לתזכר פעמיים.

## שלב 6 — לוח שנה ✅ [Claude Code B, 08-09.07.2026]

**צד שרת:** ✅ מודל `Event` הורחב בשדה `Reminder` + Migration‏ `AddEventReminder`; `EventsController` דק → `IEventService`/`EventService` → `IRepository<Event>` גנרי, עם DTOs (‏`EventCreateDto`/`EventUpdateDto`/`EventResponseDto` על בסיס `EventWriteDto` משותף) וולידציה בעברית. אומת מקצה-לקצה מול שרת רץ (יצירה עם תזכורת, רשימה, 400 על שם חסר, מחיקה).
**צד לקוח:** ✅ `CalendarPage`: תצוגת חודש בעברית, אירועי הגן + חגי ישראל (מחושבים מהתאריך העברי הקבוע של כל חג דרך Intl — לא ממציאים תאריכים), הוספת אירוע ותזכורת + מחיקה באישור. `eventsService` מחובר ל-`/api/events` עם נפילה חיננית ל-localStorage כשהשרת לא זמין (כך הלוח עובד בפרודקשן עד שהבקאנד יפרס).
- ✅ מבנה מסך לפי בקשת בעלת המוצר (09.07.2026): הלוח + הוספת אירוע בצד · מדור "חגים" (שם + תאריך) עם כפתור תקציב ליד כל חג שפותח חלון "שלום לך... נא לציין את התקציב שייצא בחג זה" (סכום בש"ח, מוצג וניתן לעריכה) · מדור "האירועים שלי". ⏳ התקציבים נשמרים זמנית בדפדפן; ישמשו את מסך המתנות (שלב 7).

## שלב 7 — מתנות וספקים ✅ [Claude Fable B, 09.07.2026]

**צד שרת:** ✅ מודלים `Vendor` (עם `VendorProduct` כ-Owned) ו-`Gift` (סטטוס planned/buying/done, תקציב, קישור לחג ולספק) + Migration‏ `AddGiftsAndVendors` + שכבות מלאות: `GiftsController`/`VendorsController` דקים → `IGiftService`/`IVendorService` (Gift דרך Repository גנרי; Vendor דרך DbContext עם Include למוצרים) + DTOs וולידציה בעברית (סטטוס מוגבל ל-3 הערכים). אומת מקצה-לקצה: POST/PUT/DELETE, החלפת מוצרי ספק, ו-400 על סטטוס/שם לא תקינים.
**צד לקוח:** ✅ `GiftsPage` לפי UI_SPEC ס' 12: ספירה לאחור לחג הקרוב (מחושבת מלוח השנה העברי, לא מומצאת), רשימת מתנות עם סטטוס צבעוני ותקציב, טופס הוספה/עריכה (שם, אירוע-חג, תקציב, סטטוס, ספק), עוזרת תקציבית שמחשבת מנוצל מול תקציב החג (מתחבר לתקציבי לוח השנה משלב 6), ודף ספק עם מוצרים וקטלוג. עובד גם בלי שרת (fallback מקומי). 4 טסטים חדשים.
**שדרוג ספקים — מודל עסקי:** ✅ [Claude Fable B, 09.07.2026] נפתרה שאלת "ספקים מומלצים" (UI_SPEC ס' 12): הספקים מנוהלים ידנית ע"י מנהלת המערכת (ערוץ הכנסה — ספקים בתשלום). לכל ספק נוספו: תמונות מוצרים (URL לכל מוצר), קישור וואטסאפ (כפתור wa.me), וקישורים לרשתות חברתיות. שרת: `VendorProduct.ImageUrl`, `Vendor.WhatsApp`, ישות `VendorSocialLink` + Migration‏ `AddVendorContactAndImages`. לקוח: טופס ודף ספק מורחבים + עוזר `whatsapp.js`. טסט חדש (5 במסך המתנות). ⏳ הגבלת הניהול ל"מנהל-על" עם התפקידים בשלב 10; העלאת תמונה ישירה עם תשתית ה-Drive (שלב 8).

## שלב 8 — קבצים (Google Drive) ✅ [Claude Fable B, 09.07.2026] — **קישורי תיקיות ידניים**

**החלטת בעלת המוצר (09.07.2026):** במקום חיבור OAuth אוטומטי — גישה פשוטה של **קישורי תיקיות ידניים**: המשתמשת פותחת תיקייה בגוגל דרייב שלה (שם חופשי/גנרי — "גן הפרחים", "יום המשפחה"...), מעתיקה את קישור השיתוף, ומדביקה אותו במסך הקבצים עם שם. המערכת שומרת שם+קישור ומציגה כפתור שפותח את התיקייה. **יתרון:** לא דורש שלב 10 / OAuth — עובד כבר עכשיו.
**צד שרת:** ✅ מודל `DriveFolder` (שם + קישור) + Migration‏ `AddDriveFolders` + שכבות מלאות (`FoldersController` דק → `IDriveFolderService`/`DriveFolderService` → `IRepository<DriveFolder>`) עם DTOs וולידציה בעברית (קישור חייב להתחיל ב-http/https). ‏build נקי. (אימות e2e נדחה לפריסה — שרת מקומי היה תפוס ע"י סוכן אחר; הדפוס זהה ל-Vendor/Gift המאומתים בייצור.)
**צד לקוח:** ✅ `FilesPage` — רשימת תיקיות כקישורים שנפתחים בדרייב (טאב חדש), הוספה/עריכה (`FolderForm`) ומחיקה באישור. עובד גם בלי שרת (fallback מקומי). 3 טסטים חדשים.
**עתידי (לא עכשיו):** חיבור OAuth אמיתי לגוגל דרייב + העלאת קבצים ישירה — כשתהיה כניסת Google (שלב 10).

## שלב 9 — עוזרת AI ✅ [Claude Code B, 09.07.2026]

- ✅ **צד שרת:** `AiController` (`POST /api/ai/ask`) דק → `IAiService`/`AiService`. **ספק ה-AI: Google Gemini במסלול חינמי** (החלטת בעלת המוצר, 09.07.2026; הוחלף מ-Claude/Anthropic) — קריאה ל-`generateContent`, דגם `gemini-2.5-flash` (ניתן להחליף דרך `Gemini__Model`). **מפתח ה-API רק ממשתני סביבה** (`Gemini:ApiKey`) — לעולם לא בקוד; כשהמפתח חסר מוחזר 503 עם הודעה ידידותית. persona בעברית, טיפול בחסימת בטיחות (blockReason/finishReason=SAFETY), לוגים בלי גוף התשובה.
- ✅ **צד לקוח:** `AiAssistantPage` (UI_SPEC ס' 14): פתיח "במה ברצונך לעזור?", צ'יפים לשאלות מהירות, שדה שאלה חופשית, ותשובה עם מצבי טעינה/שגיאה; כפתור AI צף (`AiFab`, UI_SPEC ס' 10) בנתיב `/assistant`. `aiService` דרך שכבת ה-API.
- ✅ **פרטיות:** לא נשלחים לבינה שמות/טלפונים של ילדים או הורים — רק טקסט השאלה (ורקע כללי לא-מזהה אם יצורף בעתיד).
- ⏳ **נותר לבעלת המוצר:** ליצור מפתח חינמי ב-Google AI Studio ולהוסיף `Gemini__ApiKey` ל-Railway Variables (ייצור) / User Secrets (פיתוח) כדי להפעיל את העוזרת. 3 טסטי לקוח + build ירוק; endpoint אומת מקצה-לקצה (503 ללא מפתח).

## שלב 10 — אבטחה וכניסה 🔄 [Claude Fable, 09.07.2026] — **בסיס הושלם; נותר Google OAuth**

**מודל מנוי (החלטת בעלת המוצר):** בעת הרכישה הלקוח בוחר שם משתמש + סיסמה; מתחבר אחר כך בשם-משתמש/סיסמה **או** ב-Google (אותו חשבון). **תוקף המנוי גורף עד 30.8 של השנה שאחרי הרכישה** (רכישה 07.2026 → תוקף עד 30.8.2027).
**צד שרת:** ✅ JWT + משתמשות (סיסמאות עם PBKDF2‏ — `PasswordHasher`) + `SubscriptionPolicy` (תוקף עד 30.8 של השנה שאחרי) + Migration‏ `AddUsers`. `AuthController` (`register`/`login`, ‏`[AllowAnonymous]`) → `IAuthService`; `JwtTokenService` (תוקף ה-token = תוקף המנוי, מפתח מ-`Jwt:Key` env בלבד). `FallbackPolicy` דורש token בכל endpoint. תפקיד `SuperAdmin` במודל (לניהול ספקים). אומת מקצה-לקצה (הרשמה→תוקף 30.8.2027, ‏401 בלי token, ‏200 איתו). ☐ נותר: **כניסת Google (OAuth)** + אכיפת `SuperAdmin` על ניהול ספקים.
**צד לקוח:** ✅ `authService` (login/register/logout/token) + `api.js` מצרף `Authorization: Bearer` לכל בקשה + `LoginPage`/`RegisterPage` אמיתיים + הגנת ניתוב (מסך לא-ציבורי בלי token → מסך פתיחה). 5 טסטים חדשים. ☐ נותר: כפתור "כניסה עם Google" (מושבת עד שייבנה OAuth) + הודעת "המנוי פג" ייעודית.

**⚠️ לפני פריסת הבקאנד:** ‏main כולל אכיפת אבטחה גורפת בשרת. הפרונט כבר שולח token, אז זרם הרשמה→כניסה→שימוש עובד; אבל חובה להגדיר `Jwt__Key` (מחרוזת אקראית ארוכה) ב-Railway Variables — אחרת נעשה שימוש במפתח פיתוח חלש.

## שלב 11 — הקשחת ייצור ☐ (Health Checks ✅)

☐ מעבר ל-SQL Server (החלפת connection string) · ☐ גיבויים אוטומטיים · ✅ Monitoring + Health Checks [Claude Code, 09.07.2026] · ☐ CORS לדומיין הייצור בלבד

- ✅ **Health Check** — `GET /api/health` (`HealthController`, ‏`[AllowAnonymous]` כדי שבודק הבריאות של Railway לא ייחסם ע"י אכיפת ה-token משלב 10): מחזיר 200 כשהשרת והמסד זמינים (בודק `Database.CanConnectAsync`), ו-503 אם אין גישה למסד. אומת מקצה-לקצה: 200 בלי token מול 401 ל-`/api/students` בלי token. **מומלץ להגדיר את הנתיב הזה כ-Health Check Path ב-Railway.**
- ☐ שאר ההקשחה (SQL Server, גיבויים, CORS לדומיין הייצור) — תלוי בפריסה בפועל ובדומיין, לביצוע יחד עם עליית הבקאנד ל-Railway.

---

## מחוץ לתחום (Out of Scope)

סליקת תשלומים בתוך המערכת *(רצון עתידי של בעלת המוצר 09.07.2026 — העברת כסף דרך האפליקציה; יטופל כשלב עתידי נפרד, לא כעת)* · תמיכה בריבוי בתי ספר · אפליקציות נייטיב · פורטל הורים ציבורי
