# נוהל אירוע אבטחת מידע — VaddyGo

נוהל זה נכתב כדי שברגע אמת לא נצטרך להחליט מה לעשות. הוא קצר בכוונה: מי
שקורא אותו נמצא באמצע אירוע.

**אחראית:** אביבית ויסקין (בעלת המוצר) — נכון להיום היחידה בעלת גישה טכנית.
**כתובת לדיווח מבחוץ:** hello@vaddygo.com

---

## מה נחשב אירוע

כל אחד מאלה, גם אם רק חשוד:

- גישה של גורם לא מורשה לחשבון ועד, לחשבון ספק, או לחשבון Railway / GitHub
- דליפה של קישור עריכה של ספק, קישור הזמנה לגן, או טוקן אחר
- שינוי או מחיקה של נתונים שלא על ידי בעליהם
- חשד שקובץ מסד הנתונים או גיבוי הועתק החוצה
- פרסום בטעות של מפתח סודי (‏`Jwt__Key`, מפתח Gemini, מפתח סליקה) — למשל
  ב-commit לריפו ציבורי
- דיווח מלקוחה שהיא רואה נתונים של גן אחר

---

## שלב 1 — עצירה (מיד, לפני כל דבר אחר)

המטרה: לעצור את הדימום. לא לחקור עדיין.

| מה קרה | מה עושים |
|---|---|
| דלף קישור של ספק | במסך המתנות → הספק → **הפקת קישור** — פעולה זו מבטלת את הקישור הישן |
| דלף מפתח סודי | להחליף אותו ב-Railway → Variables, ולפרוס מחדש |
| חשד לחשבון ועד שנפרץ | לאפס לה סיסמה; לוודא שהמנוי/הגישה מוגבלים עד לבירור |
| חשד לחשבון Railway / GitHub | להחליף סיסמה **ולהפעיל אימות דו-שלבי**, ולבטל סשנים פתוחים |
| דליפת נתונים רחבה | לשקול השבתה זמנית של השירות ב-Railway — עדיף שירות מושבת על נתונים דולפים |

---

## שלב 2 — תיעוד (באותו יום)

לפתוח קובץ אחד ולרשום בו, בזמן אמת:

1. **מתי** התגלה, ועל ידי מי
2. **מה** קרה, במילים פשוטות
3. **איזה מידע** היה חשוף — ובפרט: האם כלל נתוני ילדים (שם, ת״ז, אלרגיות,
   כתובת) או פרטי הורים
4. **כמה** נושאי מידע נוגעים בדבר (כמה גנים, כמה ילדים בקירוב)
5. **מה נעשה** בשלב 1 ומתי
6. צילומי מסך, שורות לוג רלוונטיות

התיעוד חשוב גם אם האירוע התברר כשווא — הוא מוכיח שהופעל נוהל.

---

## שלב 3 — הערכה משפטית (תוך 24 שעות)

**ליצור קשר עם עו״ד מיכל כחלון** ולמסור את התיעוד משלב 2.

הנקודות שיקבעו את חובת הדיווח:

- האם נחשף **מידע רגיש** — אצלנו: תעודות זהות ואלרגיות של **קטינים**. אם כן,
  ההנחה צריכה להיות שקיימת חובת דיווח, עד שייקבע אחרת.
- האם המידע היה מוצפן או נגיש בפועל
- מספר האנשים שנפגעו

**לא מחליטים לבד** אם לדווח לרשות להגנת הפרטיות או ליידע לקוחות — זו החלטה
משפטית.

---

## שלב 4 — יידוע לקוחות

רק לאחר ההערכה המשפטית, ולפי הנוסח שתאשר עורכת הדין.

עקרונות לנוסח: מה קרה, איזה מידע נגע, מה כבר עשינו, ומה הלקוחה צריכה לעשות
(למשל להחליף סיסמה). בלי המעטה ובלי הסתרה — יידוע מאוחר או חלקי מחמיר את
החשיפה.

---

## שלב 5 — סגירה ולקח

לאחר שהאירוע נסגר, לרשום ב-`PROJECT_LOG.md`:

- שורש הבעיה (לא רק התסמין)
- מה שונה בקוד או בתצורה כדי שזה לא יחזור
- אם נדרש שינוי בנוהל הזה — לעדכן אותו

---

## מה כבר קיים במערכת ומסייע באירוע

- **לוגים** ב-Railway: מזהים מספריים וחותמות זמן — מאפשרים לשחזר מי עשה מה
  ומתי (בלי פרטים אישיים בלוג עצמו)
- **גיבויים**: שבעת האחרונים, בכל עלייה ובנוסף כל 24 שעות — מאפשרים שחזור
  אחרי מחיקה או שיבוש
- **ביטול קישור ספק**: הפקת קישור חדש מבטלת את הישן
- **הגבלת קצב**: 10 ניסיונות התחברות לדקה לכל כתובת IP
- **בידוד נתונים**: נאכף בשרת לפי בעלות — ועד אינו יכול לגשת לגן אחר

## חולשות ידועות שיש להביא בחשבון באירוע

- **נקודת כשל יחידה:** גישה לחשבון Railway או GitHub = גישה לכל המידע של כל
  הוועדים. לכן אימות דו-שלבי בשני החשבונות הוא ההגנה החשובה ביותר.
- **הגיבויים באותו נפח** של המסד — אינם מגנים מפני אובדן הנפח כולו.
- **אין הצפנה ברמת האפליקציה** — מי שמשיג את קובץ המסד רואה את התוכן.
- **הגבלת הקצב היא לפי IP** — אינה עוצרת מתקפה מבוזרת.

---

*עודכן לאחרונה: 17.08.2026*
