# ---- .NET build output ----
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.dll
*.pdb

# ---- User / IDE files ----
*.user
*.suo
.vs/
.vscode/
*.code-workspace

# ---- SQLite database files (runtime data, never in source control) ----
*.db
*.db-shm
*.db-wal

# ---- Local secrets / environment (never commit) ----
# appsettings.Development.json currently holds no secrets, but keep real
# secrets out of source control — use User Secrets or environment variables.
secrets.json
*.env
.env
appsettings.*.Local.json

# ---- OS noise ----
Thumbs.db
.DS_Store
