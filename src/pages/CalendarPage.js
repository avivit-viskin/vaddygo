import EmptyState from "../components/EmptyState";

/*
  CalendarPage — לוח השנה של הגן. ייבנה בשלב 6 של ה-ROADMAP.
*/
function CalendarPage() {
  return (
    <EmptyState icon="📅" message="לוח השנה בדרך אלייך — יגיע באחד השלבים הקרובים!" />
  );
}

export default CalendarPage;
