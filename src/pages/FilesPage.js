import EmptyState from "../components/EmptyState";

/*
  FilesPage — קבצי הוועד (Google Drive). ייבנה בשלב 8 של ה-ROADMAP.
*/
function FilesPage() {
  return (
    <EmptyState icon="📁" message="מסך הקבצים בדרך אלייך — יגיע באחד השלבים הקרובים!" />
  );
}

export default FilesPage;
