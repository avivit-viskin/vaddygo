import EmptyState from "../components/EmptyState";

/*
  GiftsPage — מתנות וספקים. ייבנה בשלב 7 של ה-ROADMAP.
*/
function GiftsPage() {
  return (
    <EmptyState icon="🎁" message="מסך המתנות בדרך אלייך — יגיע באחד השלבים הקרובים!" />
  );
}

export default GiftsPage;
